// AUTO-GENERATED by scripts/skill/extract-source.mjs — DO NOT EDIT BY HAND.
//
// Verbatim byte slices of mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js
//   sha256 3a888487166a91bd070d1b967447a42b7c50d14b4071957be643e7835313d6c0
// concatenated in source order: 30 slices, 9580 source lines.
// Regenerate with:
//   node scripts/skill/extract-source.mjs --slices scripts/slices.config.mjs
// Verify against the source with:
//   node scripts/skill/extract-source.mjs --slices scripts/slices.config.mjs --check
//
// Verbatim application-region slices of the OFF+BRAND bundle (pretty coordinates).
// Vendor regions (three/gsap/lenis/rive/taxi/msdf/lil-gui/loaders) are the pinned npm
// packages in package.json, not sliced. Symbol map: docs/rename-map.json.
//
// Nothing below this header was retyped, reformatted or corrected. Source-site
// bugs, dead code and odd spellings are present ON PURPOSE — they are the port
// (porting-discipline.md §1.3; register them in REBUILD_PLAN §Q).

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L1-L32 =====
// esbuild runtime helpers: __create/__defProp/__getOwnPropNames/__hasOwnProp, dJ=__toESM, DK=__commonJS, QD=__export, VA=__esm — bundler plumbing the app slices call
var YV = Object.create;
var {
    getPrototypeOf: $V,
    defineProperty: OZ,
    getOwnPropertyNames: DV
} = Object;
var WV = Object.prototype.hasOwnProperty;
var dJ = (A, Q, B) => {
    B = A != null ? YV($V(A)) : {};
    let E = Q || !A || !A.__esModule ? OZ(B, "default", {
        value: A,
        enumerable: !0
    }) : B;
    for (let I of DV(A))
        if (!WV.call(E, I)) OZ(E, I, {
            get: () => A[I],
            enumerable: !0
        });
    return E
};
var DK = (A, Q) => () => (Q || A((Q = {
    exports: {}
}).exports, Q), Q.exports);
var QD = (A, Q) => {
    for (var B in Q) OZ(A, B, {
        get: Q[B],
        enumerable: !0,
        configurable: !0,
        set: (E) => Q[B] = () => E
    })
};
var VA = (A, Q) => () => (A && (Q = A(A = 0)), Q);

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L33-L89 =====
// esbuild helpers tail + utils: BD(34) ED(41) ID(46) CD(56) KD(75) — safari/iphone flags, Webflow destroy+ready, w--current, breakpoint reload

function BD() {
    let A = /^((?!chrome|android).)*safari/i.test(navigator.userAgent),
        Q = /iPhone/i.test(navigator.userAgent);
    if (A) document.documentElement.classList.add("is-safari");
    if (Q) document.documentElement.classList.add("is-iphone")
}

function ED() {
    let A = window.Webflow;
    A.destroy(), A.ready()
}

function ID() {
    var A = window.location.pathname,
        Q = document.querySelectorAll("a");
    Q.forEach(function(B) {
        B.classList.remove("w--current")
    }), Q.forEach(function(B) {
        if (B.getAttribute("href") === A) B.classList.add("w--current")
    })
}

function CD() {
    console.log(`%c
   ██╗      █████╗ ███╗   ██╗██████╗  ██████╗ 
   ██║     ██╔══██╗████╗  ██║██╔══██╗██╔═══██╗
   ██║     ███████║██╔██╗ ██║██║  ██║██║   ██║
   ██║     ██╔══██║██║╚██╗██║██║  ██║██║   ██║
   ███████╗██║  ██║██║ ╚████║██████╔╝╚██████╔╝
   ╚══════╝╚═╝  ╚═╝╚═╝  ╚═══╝╚═════╝  ╚═════╝ 
  %c
   ███╗   ██╗ ██████╗ ██████╗ ██████╗ ██╗███████╗
   ████╗  ██║██╔═══██╗██╔══██╗██╔══██╗██║██╔════╝
   ██╔██╗ ██║██║   ██║██████╔╝██████╔╝██║███████╗
   ██║╚██╗██║██║   ██║██╔══██╗██╔══██╗██║╚════██║
   ██║ ╚████║╚██████╔╝██║  ██║██║  ██║██║███████║
   ╚═╝  ╚═══╝ ╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═╝╚═╝╚══════╝
  
   %c%c`, "color: #d2ff00; font: 400 1em monospace;", "", "background-color: #d2ff00; color: black; font: 400 1em monospace; padding: 0.5em 0; font-weight: bold;", "")
}

function KD() {
    let A = window.innerWidth >= 992,
        Q = !0;

    function B() {
        let I = A;
        if (A = window.innerWidth >= 992, !Q && I !== A) window.location.reload();
        if (Q) Q = !1
    }
    B();
    let E;
    window.addEventListener("resize", function() {
        clearTimeout(E), E = setTimeout(B, 150)
    })
}

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L9407-L10280 =====
// LB (9408) + vC Rive system (9438-10280): runtime aliases, file preload, components, window.loadedRiveFiles

function dR() {
    let Q = window.innerWidth <= 991 ? uR : uR;
    TA.defaults({
        scroller: Q
    }), TA.refresh()
}

function t2(A, Q) {
    let B;
    return function() {
        let E = this,
            I = arguments;
        clearTimeout(B), B = setTimeout(() => A.apply(E, I), Q)
    }
}
var r2, uR = "body",
    m;
var LB = VA(() => {
    DW();
    KR();
    TR();
    gR();
    E1.registerPlugin(TA, u6, CI);
    r2 = {};
    E1.defaults(r2);
    dR();
    window.addEventListener("resize", t2(dR, 250));
    m = E1
});

function g8(A) {
    if (!dK) return;
    let Q = dK.find((B) => B.name === "close");
    if (Q) Q.value = A, document.querySelectorAll("[data-nav-ham]").forEach((E) => {
        E.setAttribute("aria-label", A ? "Close menu" : "Open menu"), E.setAttribute("aria-expanded", A ? "true" : "false")
    }), KI = A
}

function HE(A) {
    if (!dK) return;
    let Q = dK.find((E) => E.name === "color-transparent"),
        B = dK.find((E) => E.name === "color-white");
    if (Q && B) {
        if (A.toLowerCase() === "transparent") Q.value = !0, B.value = !1;
        else if (A.toLowerCase() === "white") Q.value = !1, B.value = !0
    }
}

function kI() {
    let A = Object.keys(p6).map((Q) => cR(Q));
    if (A.length === 0) {
        console.log("No Rive files to preload"), sR();
        return
    }
    JI = A.length, A.forEach((Q) => {
        A4(Q, (B) => {
            let E = Object.keys(p6).find((I) => cR(I) === Q);
            if (E) DB[E] = B;
            JI--, mR()
        }, (B) => {
            console.error(`Failed to load Rive file ${Q}:`, B), JI--, mR()
        })
    }), window.addEventListener("resize", lR), window.matchMedia(`(resolution: ${window.devicePixelRatio}dppx)`).addEventListener("change", lR)
}

function mR() {
    if (JI <= 0 && !s0) {
        s0 = !0, window.loadingComplete = !0, window.loadedRiveFiles = DB;
        let A = new CustomEvent("allriveloaded");
        window.dispatchEvent(A), sR()
    }
}

function sR() {
    let A = new CustomEvent("riveAllLoaded");
    window.dispatchEvent(A)
}

function A4(A, Q, B) {
    if (DB[A]) {
        Q(DB[A]);
        return
    }
    let E = new jQ.default.RiveFile({
        src: A,
        onLoad: () => {
            Q(E)
        },
        onLoadError: B
    });
    E.init().catch(B)
}

function cR(A) {
    if (p6[A]) return pR + p6[A];
    return pR + A
}

function u8(A) {
    let B = {
        contain: jQ.default.Fit.Contain,
        cover: jQ.default.Fit.Cover,
        fill: jQ.default.Fit.Fill,
        fitwidth: jQ.default.Fit.FitWidth,
        fitheight: jQ.default.Fit.FitHeight,
        none: jQ.default.Fit.None,
        scaledown: jQ.default.Fit.ScaleDown,
        layout: jQ.default.Fit.Layout
    } [A?.toLowerCase()];
    return B !== void 0 ? B : jQ.default.Fit.Contain
}

function lR() {
    UI.forEach((A) => {
        if (A) A.resizeDrawingSurfaceToCanvas()
    })
}

function IH(A) {
    return A.riveInstance || null
}

function Q4(A) {
    let Q = IH(A);
    if (Q) {
        if (Q.cleanup(), A.riveScrollControl) {
            if (A.riveScrollControl.scrollTrigger) A.riveScrollControl.scrollTrigger.kill();
            if (A.riveScrollControl.timeline) A.riveScrollControl.timeline.kill();
            A.riveScrollControl = null
        }
        if (A.lenisScrollHandler && window.lenis) window.lenis.off("scroll", A.lenisScrollHandler), A.lenisScrollHandler = null;
        let B = UI.indexOf(Q);
        if (B > -1) UI.splice(B, 1);
        A.riveInstance = null
    }
}

function CH(A, Q, B, E = "top 80%") {
    if (A.style.opacity = "0", A.style.transition = "opacity 0.1s ease-in-out", Q.play(), setTimeout(() => {
            Q.pause(), A.style.opacity = "0"
        }, 50), typeof TA !== "undefined") TA.create({
        trigger: B,
        start: E,
        onEnter: () => {
            setTimeout(() => {
                A.style.opacity = "1", Q.play()
            }, 0)
        },
        once: !0
    });
    else {
        let I = new IntersectionObserver((C) => {
            C.forEach((K) => {
                if (K.isIntersecting) setTimeout(() => {
                    Q.play()
                }, 100), I.unobserve(K.target)
            })
        }, {
            threshold: 0.1,
            rootMargin: "0px 0px -10% 0px"
        });
        I.observe(B)
    }
}

function oR(A, Q, B) {
    let E = A.getAttribute("data-rive-state-machine"),
        I = null,
        C = Q.stateMachineInputs(E);
    if (C) {
        if (I = C.find((K) => K.name === "scroll"), !I) {
            console.error(`Scroll input not found in state machine ${E}`);
            return
        }
    }
    if (typeof TA !== "undefined") {
        let K = A.getAttribute("data-rive-scrolltrigger-start"),
            J = A.getAttribute("data-rive-scrolltrigger-end"),
            U = m.timeline({
                paused: !0,
                onUpdate: function() {
                    if (I) {
                        let H = m.utils.clamp(0, 1, this.progress());
                        I.value = H * 1000
                    }
                }
            });
        U.to({}, {
            duration: 1
        });
        let G = TA.create({
            trigger: B,
            start: K || "top bottom",
            end: J || "bottom top",
            scrub: 0.5,
            markers: !1,
            onUpdate: (H) => {
                if (U.progress(H.progress), !Q.isPlaying) Q.play()
            },
            onEnter: () => {
                if (Q.play(), I) I.value = 0
            },
            onLeave: () => {
                if (I) I.value = 1000;
                setTimeout(() => Q.pause(), 50)
            },
            onEnterBack: () => {
                Q.play()
            },
            onLeaveBack: () => {
                if (I) I.value = 0;
                setTimeout(() => Q.pause(), 50)
            }
        });
        if (A.riveScrollControl = {
                timeline: U,
                scrollTrigger: G
            }, window.lenis) {
            let H = () => {
                if (G.isActive) {
                    let Z = window.lenis.velocity || 0;
                    if (Math.abs(Z) > 100) {
                        if (Z < 0 && G.progress < 0.1) {
                            if (I) I.value = 0
                        } else if (Z > 0 && G.progress > 0.9) {
                            if (I) I.value = 1000
                        }
                    }
                }
            };
            window.lenis.on("scroll", H), A.lenisScrollHandler = H
        }
    } else {
        let K = window.scrollY,
            J = !1,
            U = (Z) => {
                let q = Z.getBoundingClientRect(),
                    Y = window.innerHeight,
                    W = q.height,
                    $ = q.top;
                if (q.bottom <= 0) return 1;
                if ($ >= Y) return 0;
                let R = W + Y,
                    N = Y - $;
                return Math.min(Math.max(N / R, 0), 1)
            },
            G = () => {
                let Z = U(B);
                if (I) I.value = Z * 1000;
                if (Z > 0 && Z < 1) {
                    if (!Q.isPlaying) Q.play()
                } else if (Q.isPlaying) Q.pause();
                J = !1
            },
            H = () => {
                if (K = window.scrollY, !J) m.ticker.add(G), J = !0
            };
        window.addEventListener("scroll", H), A.scrollListener = H
    }
}

function KH(A) {
    let Q = A;
    while (Q) {
        if (Q.hasAttribute && Q.hasAttribute("data-hero-animation-container")) return !0;
        Q = Q.parentElement
    }
    return !1
}

function B4() {
    let A = () => {
        if (!DB.reef) {
            console.error("Reef file not found in loaded files");
            return
        }
        let Q = document.querySelectorAll('canvas[data-rive-object][data-rive-file="reef"]');
        if (Q.length === 0) {
            console.log("No Reef Rive canvases found on page");
            return
        }
        Q.forEach((B) => {
            let E = B.getAttribute("data-rive-artboard") || "helmet-reef",
                I = B.getAttribute("data-rive-state-machine"),
                C = B.getAttribute("data-rive-scrolltrigger-target"),
                K = B.getAttribute("data-rive-input"),
                J = B.getAttribute("data-rive-instant-play") === "true",
                U = KH(B),
                G = J && !U,
                H = C ? C === "" ? B : document.querySelector(C) : B;
            if (!H) H = B;
            let Z = new jQ.default.Rive({
                riveFile: DB.reef,
                canvas: B,
                artboard: E,
                stateMachines: I,
                autoplay: G,
                layout: new jQ.default.Layout({
                    fit: u8(B.getAttribute("data-rive-fit") || "contain"),
                    alignment: jQ.default.Alignment.center
                }),
                onLoad: () => {
                    if (Z.resizeDrawingSurfaceToCanvas(), K && I) {
                        let q = Z.stateMachineInputs(I);
                        if (q) {
                            let Y = q.find((W) => W.name === K);
                            if (Y && Y.type === jQ.default.StateMachineInputType.Boolean) Y.value = !0;
                            B.riveInputs = q
                        }
                    }
                    if (U) {
                        if (B.dataset.heroAnimation = "true", Z.isPlaying) Z.pause()
                    } else if (I.includes("_play")) {
                        if (!J) CH(B, Z, H)
                    } else if (I.includes("_scroll")) oR(B, Z, H);
                    else if (I === "off-icons") Z.play()
                }
            });
            UI.push(Z), B.riveInstance = Z
        })
    };
    if (s0 && DB.reef) A();
    else if (window.addEventListener("riveAllLoaded", A, {
            once: !0
        }), JI === 0 && !s0) kI()
}

function E4() {
    let A = () => {
        if (!DB.signature) {
            console.error("Signature file not found in loaded files");
            return
        }
        let Q = document.querySelectorAll('canvas[data-rive-object][data-rive-file="signature"]');
        if (Q.length === 0) {
            console.log("No Signature Rive canvases found on page");
            return
        }
        Q.forEach((B) => {
            let E = B.getAttribute("data-rive-state-machine"),
                I = B.getAttribute("data-rive-scrolltrigger-target"),
                C = B.getAttribute("data-rive-input"),
                K = B.getAttribute("data-rive-instant-play") === "true",
                J = KH(B),
                U = K && !J,
                G = I ? I === "" ? B : document.querySelector(I) : B;
            if (!G) G = B;
            let H = new jQ.default.Rive({
                riveFile: DB.signature,
                canvas: B,
                artboard: "signature",
                stateMachines: E,
                autoplay: U,
                layout: new jQ.default.Layout({
                    fit: u8(B.getAttribute("data-rive-fit") || "contain"),
                    alignment: jQ.default.Alignment.center
                }),
                onLoad: () => {
                    if (H.resizeDrawingSurfaceToCanvas(), C && E) {
                        let Z = H.stateMachineInputs(E);
                        if (Z) {
                            let q = Z.find((Y) => Y.name === C);
                            if (q && q.type === jQ.default.StateMachineInputType.Boolean) q.value = !0;
                            B.riveInputs = Z
                        }
                    }
                    if (J) {
                        if (B.dataset.heroAnimation = "true", H.isPlaying) H.pause()
                    } else if (E === "signature_play") {
                        if (!K) CH(B, H, G)
                    } else if (E === "signature_scroll") oR(B, H, G)
                }
            });
            UI.push(H), B.riveInstance = H
        })
    };
    if (s0 && DB.signature) A();
    else if (window.addEventListener("riveAllLoaded", A, {
            once: !0
        }), JI === 0 && !s0) kI()
}

function I4() {
    let A = () => {
        if (!DB.circuits) {
            console.error("Circuits file not found in loaded files");
            return
        }
        let Q = document.querySelectorAll('canvas[data-rive-object][data-rive-file="circuits"]');
        if (Q.length === 0) return;
        Q.forEach((B) => {
            let E = B.getAttribute("data-rive-state-machine"),
                I = B.getAttribute("data-rive-input-track"),
                C = B.getAttribute("data-rive-input-color"),
                K = B.getAttribute("data-rive-input-weight"),
                J = B.getAttribute("data-rive-circuit-hover") === "true",
                U = new jQ.default.Rive({
                    riveFile: DB.circuits,
                    canvas: B,
                    artboard: "circuits",
                    stateMachines: E,
                    autoplay: !0,
                    layout: new jQ.default.Layout({
                        fit: u8(B.getAttribute("data-rive-fit") || "contain"),
                        alignment: jQ.default.Alignment.center
                    }),
                    onLoad: () => {
                        U.resizeDrawingSurfaceToCanvas();
                        let G = U.stateMachineInputs(E);
                        if (!G) {
                            console.error("No inputs found for state machine:", E);
                            return
                        }
                        B.riveInputs = G;
                        let H = (Z) => {
                            if (!Z) return;
                            G.filter((W) => W.type === jQ.default.StateMachineInputType.Boolean && !W.name.startsWith("color_") && !W.name.startsWith("weight_")).forEach((W) => {
                                W.value = !1
                            });
                            let Y = G.find((W) => W.name === Z);
                            if (Y) Y.value = !0
                        };
                        if (C) {
                            let Z = G.find((q) => q.name === C);
                            if (Z && Z.type === jQ.default.StateMachineInputType.Boolean) Z.value = !0
                        }
                        if (K) {
                            let Z = G.find((q) => q.name === K);
                            if (Z && Z.type === jQ.default.StateMachineInputType.Boolean) Z.value = !0
                        }
                        if (I) H(I);
                        if (J) {
                            let Z = document.querySelectorAll("[data-rive-circuit-hover-target]"),
                                q = document.querySelectorAll("[data-rive-circuit-hover-text]");
                            if (Z.length === 0);
                            else Z.forEach((Y) => {
                                Y.addEventListener("mouseenter", () => {
                                    let W = Y.getAttribute("data-rive-circuit-hover-target");
                                    if (H(W), q.length > 0) q.forEach(($) => {
                                        $.textContent = W
                                    })
                                }), Y.addEventListener("mouseleave", () => {
                                    if (I) {
                                        if (H(I), q.length > 0) q.forEach((W) => {
                                            W.textContent = I
                                        })
                                    }
                                })
                            })
                        }
                    }
                });
            UI.push(U), B.riveInstance = U
        })
    };
    if (s0 && DB.circuits) A();
    else if (window.addEventListener("riveAllLoaded", A, {
            once: !0
        }), JI === 0 && !s0) kI()
}

function C4() {
    let A = () => {
        if (!DB["btn-ui"]) {
            console.error("Button UI file not found in loaded files");
            return
        }
        let Q = document.querySelectorAll('canvas[data-rive-object][data-rive-file="btn-ui"]');
        if (Q.length === 0) return;
        Q.forEach((B) => {
            let E = B.getAttribute("data-rive-artboard") || "arrow",
                I = B.getAttribute("data-rive-state-machine") || "arrow",
                C = B;
            while (C && !C.hasAttribute("data-btn-rive-hover")) C = C.parentElement;
            if (!C) C = B;
            let K = new jQ.default.Rive({
                riveFile: DB["btn-ui"],
                canvas: B,
                artboard: E,
                stateMachines: I,
                autoplay: !0,
                layout: new jQ.default.Layout({
                    fit: u8(B.getAttribute("data-rive-fit") || "contain"),
                    alignment: jQ.default.Alignment.center
                }),
                onLoad: () => {
                    K.resizeDrawingSurfaceToCanvas();
                    let J = K.stateMachineInputs(I);
                    if (!J) {
                        console.error(`No inputs found for state machine: ${I}`);
                        return
                    }
                    let U = J.find((G) => G.name === "hover");
                    if (!U) {
                        console.error(`Hover input not found in state machine: ${I}`);
                        return
                    }
                    U.value = !1, B.riveInputs = J, C.addEventListener("mouseenter", () => {
                        U.value = !0
                    }), C.addEventListener("mouseleave", () => {
                        U.value = !1
                    }), C.addEventListener("touchstart", () => {
                        U.value = !0
                    }), C.addEventListener("touchend", () => {
                        U.value = !1
                    })
                }
            });
            UI.push(K), B.riveInstance = K
        })
    };
    if (s0 && DB["btn-ui"]) A();
    else if (window.addEventListener("riveAllLoaded", A, {
            once: !0
        }), JI === 0 && !s0) kI()
}

function iR() {
    let A = () => {
        if (!DB["btn-ui"]) {
            console.error("Button UI file not found in loaded files");
            return
        }
        let Q = document.querySelectorAll("[data-nav-ham]");
        if (Q.length === 0) return;
        Q.forEach((B) => {
            let E = B.querySelector("canvas[data-rive-nav-hamburger]");
            if (!E) {
                console.error("Nav hamburger canvas not found in element:", B);
                return
            }
            let I = E.getAttribute("data-rive-artboard") || "hamburger",
                C = E.getAttribute("data-rive-state-machine") || "hamburger";
            B.setAttribute("aria-label", "Open menu"), B.setAttribute("aria-expanded", "false"), B.setAttribute("tabindex", "0");
            let K = new jQ.default.Rive({
                riveFile: DB["btn-ui"],
                canvas: E,
                artboard: I,
                stateMachines: C,
                autoplay: !0,
                layout: new jQ.default.Layout({
                    fit: u8(E.getAttribute("data-rive-fit") || "contain"),
                    alignment: jQ.default.Alignment.center
                }),
                onLoad: () => {
                    K.resizeDrawingSurfaceToCanvas();
                    let J = K.stateMachineInputs(C);
                    if (!J) {
                        console.error(`No inputs found for state machine: ${C}`);
                        return
                    }
                    let U = J.find((Y) => Y.name === "hover"),
                        G = J.find((Y) => Y.name === "close"),
                        H = J.find((Y) => Y.name === "color-transparent"),
                        Z = J.find((Y) => Y.name === "color-white");
                    if (!U || !G || !H || !Z) {
                        console.error("Required inputs not found for nav hamburger:", J.map((Y) => Y.name));
                        return
                    }
                    E.riveInputs = J, dK = J, e2 = K;
                    let q = document.querySelector("[data-nav-theme]");
                    if (q && q.getAttribute("data-nav-theme") === "light") H.value = !1, Z.value = !0;
                    else H.value = !0, Z.value = !1;
                    B.addEventListener("mouseenter", () => {
                        U.value = !0
                    }), B.addEventListener("mouseleave", () => {
                        U.value = !1
                    }), B.addEventListener("touchstart", (Y) => {
                        Y.preventDefault(), U.value = !0
                    }), B.addEventListener("touchend", (Y) => {
                        Y.preventDefault(), U.value = !1, KI = !KI, G.value = KI, B.setAttribute("aria-label", KI ? "Close menu" : "Open menu"), B.setAttribute("aria-expanded", KI ? "true" : "false")
                    }), B.addEventListener("click", () => {
                        KI = !KI, G.value = KI, B.setAttribute("aria-label", KI ? "Close menu" : "Open menu"), B.setAttribute("aria-expanded", KI ? "true" : "false")
                    }), B.addEventListener("keydown", (Y) => {
                        if (Y.key === "Enter" || Y.key === " ") Y.preventDefault(), B.click()
                    })
                }
            });
            UI.push(K), E.riveInstance = K
        })
    };
    if (s0 && DB["btn-ui"]) A();
    else if (window.addEventListener("riveAllLoaded", A, {
            once: !0
        }), JI === 0 && !s0) kI()
}

function K4() {
    let A = () => {
        if (!DB.phrases) {
            console.error("Phrases file not found in loaded files");
            return
        }
        let Q = document.querySelectorAll('canvas[data-rive-object][data-rive-file="phrases"]');
        if (Q.length === 0) return;
        Q.forEach((B) => {
            let E = B.getAttribute("data-rive-artboard"),
                I = B.getAttribute("data-rive-state-machine"),
                C = B.getAttribute("data-rive-scrolltrigger") === "true",
                K = B.getAttribute("data-rive-scrolltrigger-target"),
                J = B.getAttribute("data-rive-scrolltrigger-start") || "top 80%",
                U = KH(B),
                G = !C && !U;
            if (!E || !I) {
                console.error("Missing required data attributes for phrases Rive canvas");
                return
            }
            let H = K ? K === "" ? B : document.querySelector(K) : B;
            if (!H) H = B;
            let Z = new jQ.default.Rive({
                riveFile: DB.phrases,
                canvas: B,
                artboard: E,
                stateMachines: I,
                autoplay: G,
                layout: new jQ.default.Layout({
                    fit: u8(B.getAttribute("data-rive-fit") || "contain"),
                    alignment: jQ.default.Alignment.center
                }),
                onLoad: () => {
                    if (Z.resizeDrawingSurfaceToCanvas(), U) {
                        if (B.dataset.heroAnimation = "true", Z.isPlaying) Z.pause()
                    } else if (C) CH(B, Z, H, J)
                }
            });
            UI.push(Z), B.riveInstance = Z
        })
    };
    if (s0 && DB.phrases) A();
    else if (window.addEventListener("riveAllLoaded", A, {
            once: !0
        }), JI === 0 && !s0) kI()
}

function nR() {
    let A = () => {
        if (!DB.logo) {
            console.error("LN4 Logo file not found in loaded files");
            return
        }
        let Q = document.querySelectorAll("canvas[data-rive-ln4]");
        if (Q.length === 0) return;
        Q.forEach((B) => {
            let E = new jQ.default.Rive({
                riveFile: DB.logo,
                canvas: B,
                artboard: "logo",
                stateMachines: "logo",
                autoplay: !0,
                layout: new jQ.default.Layout({
                    fit: u8(B.getAttribute("data-rive-fit") || "contain"),
                    alignment: jQ.default.Alignment.center
                }),
                onLoad: () => {
                    E.resizeDrawingSurfaceToCanvas();
                    let I = E.stateMachineInputs("logo");
                    if (!I) {
                        console.error("No inputs found for LN4 logo state machine");
                        return
                    }
                    B.riveInputs = I;
                    let C = I.find((H) => H.name === "logo-active"),
                        K = I.find((H) => H.name === "hover"),
                        J = I.find((H) => H.name === "color_dark-green"),
                        U = I.find((H) => H.name === "color_white"),
                        G = I.find((H) => H.name === "color_lime");
                    if (C) C.value = !0;
                    B.addEventListener("mouseenter", () => {
                        if (K) K.value = !0
                    }), B.addEventListener("mouseleave", () => {
                        if (K) K.value = !1
                    }), B.addEventListener("touchstart", () => {
                        if (K) K.value = !0
                    }), B.addEventListener("touchend", () => {
                        if (K) K.value = !1
                    })
                }
            });
            UI.push(E), JH.push({
                instance: E,
                canvas: B
            }), B.riveInstance = E
        })
    };
    if (s0 && DB.logo) A();
    else if (window.addEventListener("riveAllLoaded", A, {
            once: !0
        }), JI === 0 && !s0) kI()
}

function pK(A) {
    JH.forEach(({
        canvas: Q
    }) => {
        if (!Q.riveInputs) return;
        let B = Q.riveInputs.find((E) => E.name === "logo-active");
        if (B) B.value = A
    })
}

function AC(A) {
    JH.forEach(({
        canvas: Q
    }) => {
        if (!Q.riveInputs) return;
        let B = Q.riveInputs.find((C) => C.name === "color_dark-green"),
            E = Q.riveInputs.find((C) => C.name === "color_white"),
            I = Q.riveInputs.find((C) => C.name === "color_lime");
        if (B) B.value = !1;
        if (E) E.value = !1;
        if (I) I.value = !1;
        switch (A.toLowerCase()) {
            case "lime":
                if (I) I.value = !0;
                break;
            case "white":
                if (E) E.value = !0;
                break;
            case "black":
            default:
                if (B) B.value = !0;
                break
        }
    })
}

function S1() {
    B4(), E4(), I4(), C4(), K4()
}

function o0() {
    if (!window.loadingComplete) {
        console.warn("Cannot fire hero animations - Rive files not fully loaded yet");
        let Q = setInterval(() => {
            if (window.loadingComplete) console.log("Rive files now loaded, firing hero animations"), clearInterval(Q), o0()
        }, 500);
        setTimeout(() => {
            clearInterval(Q)
        }, 1e4);
        return
    }
    let A = document.querySelectorAll("[data-hero-animation-container]");
    if (A.length === 0) {
        console.log("No hero animation containers found on page");
        return
    }
    A.forEach((Q) => {
        let B = Q.querySelectorAll("canvas[data-rive-object]");
        if (B.length === 0) {
            console.log("No Rive canvases found in hero container:", Q);
            return
        }
        B.forEach((E) => {
            let I = IH(E);
            if (!I) {
                console.warn("No Rive instance found for canvas in hero container, will retry");
                let K = () => {
                    let J = IH(E);
                    if (J) {
                        if (console.log("Found Rive instance on retry, playing animation"), !J.isPlaying) J.play();
                        if (E.riveInputs) {
                            let U = E.riveInputs.find((G) => G.name === "play");
                            if (U && U.type === jQ.default.StateMachineInputType.Boolean) U.value = !0
                        }
                    } else setTimeout(K, 200)
                };
                setTimeout(K, 200);
                return
            }
            let C = E.getAttribute("data-rive-state-machine");
            if (C && C.includes("_scroll")) {
                console.log("Skipping scroll-controlled animation:", C);
                return
            }
            if (!I.isPlaying) I.play();
            if (E.riveInputs) {
                let K = E.riveInputs.find((J) => J.name === "play");
                if (K && K.type === jQ.default.StateMachineInputType.Boolean) K.value = !0
            }
        })
    })
}
class aR {
    constructor() {
        this.riveInstance = null, this.canvas = document.querySelector("canvas[data-rive-mob-landscape]"), this.isActive = !1, this.orientationHandler = this.handleOrientationChange.bind(this), this.setupEventListeners(), this.handleOrientationChange()
    }
    setupEventListeners() {
        if (window.screen.orientation) window.screen.orientation.addEventListener("change", this.orientationHandler);
        else window.addEventListener("orientationchange", this.orientationHandler);
        window.addEventListener("resize", this.orientationHandler)
    }
    detectLandscapeOrientation() {
        let A = /Android|webOS|iPhone|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) && !/iPad|Tablet/i.test(navigator.userAgent),
            Q = !1;
        if (window.screen.orientation) Q = window.screen.orientation.type.includes("landscape");
        else if (typeof window.orientation !== "undefined") Q = window.orientation === 90 || window.orientation === -90;
        else Q = window.innerWidth > window.innerHeight;
        return A && Q
    }
    handleOrientationChange() {
        if (this.detectLandscapeOrientation()) this.activateLandscapeMode();
        else this.deactivateLandscapeMode()
    }
    activateLandscapeMode() {
        if (this.isActive || !this.canvas) return;
        let A = document.querySelector("[data-mob-landscape]");
        if (A) A.style.display = "flex";
        if (!this.riveInstance && DB["mob-landscape"]) this.initRiveAnimation();
        else if (this.riveInstance && !this.riveInstance.isPlaying) this.riveInstance.play();
        this.isActive = !0
    }
    deactivateLandscapeMode() {
        if (!this.isActive) return;
        let A = document.querySelector("[data-mob-landscape]");
        if (A) A.style.display = "none";
        if (this.riveInstance && this.riveInstance.isPlaying) this.riveInstance.pause();
        this.isActive = !1
    }
    initRiveAnimation() {
        if (!this.canvas || !DB["mob-landscape"]) {
            console.warn("Canvas or Rive file not available for mobile landscape");
            return
        }
        this.riveInstance = new jQ.default.Rive({
            riveFile: DB["mob-landscape"],
            canvas: this.canvas,
            stateMachines: "mob-landscape",
            autoplay: !0,
            layout: new jQ.default.Layout({
                fit: jQ.default.Fit.Cover,
                alignment: jQ.default.Alignment.center
            }),
            onLoad: () => {
                this.riveInstance.resizeDrawingSurfaceToCanvas()
            }
        }), UI.push(this.riveInstance), this.canvas.riveInstance = this.riveInstance
    }
    destroy() {
        if (window.screen.orientation) window.screen.orientation.removeEventListener("change", this.orientationHandler);
        else window.removeEventListener("orientationchange", this.orientationHandler);
        if (window.removeEventListener("resize", this.orientationHandler), this.riveInstance) Q4(this.canvas), this.riveInstance = null;
        this.isActive = !1
    }
}

function rR() {
    return new aR
}
var jQ, UI, JI = 0,
    s0 = !1,
    pR = "https://assets.itsoffbrand.io/lando/rive/",
    p6, DB, e2 = null,
    dK = null,
    KI = !1,
    JH;
var vC = VA(() => {
    jQ = dJ(cU(), 1);
    LB();
    UI = [], p6 = {
        signature: "signature.riv",
        "btn-ui": "btn-ui.riv",
        circuits: "circuits.riv",
        reef: "reef.riv",
        phrases: "phrases.riv",
        logo: "ln4.riv",
        "mob-landscape": "mob-landscape.riv"
    }, DB = {};
    window.loadedRiveFiles = DB;
    window.transitionRiveInputs = null;
    window.loadingComplete = !1;
    window.riveLoadingStarted = !1;
    JH = []
});

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L10281-L10333 =====
// tR WebGL2 capability detect (three/examples WebGL.js shape, ported verbatim as WebGLSupport) + eR alias + AX init wrapper
class tR {
    static isWebGL2Available() {
        try {
            let A = document.createElement("canvas");
            return !!(window.WebGL2RenderingContext && A.getContext("webgl2"))
        } catch (A) {
            return !1
        }
    }
    static isColorSpaceAvailable(A) {
        try {
            let Q = document.createElement("canvas"),
                B = window.WebGL2RenderingContext && Q.getContext("webgl2");
            return B.drawingBufferColorSpace = A, B.drawingBufferColorSpace === A
        } catch (Q) {
            return !1
        }
    }
    static getWebGL2ErrorMessage() {
        return this.getErrorMessage(2)
    }
    static getErrorMessage(A) {
        let Q = {
                1: "WebGL",
                2: "WebGL 2"
            },
            B = {
                1: window.WebGLRenderingContext,
                2: window.WebGL2RenderingContext
            },
            E = 'Your $0 does not seem to support <a href="http://khronos.org/webgl/wiki/Getting_a_WebGL_Implementation" style="color:#000">$1</a>',
            I = document.createElement("div");
        if (I.id = "webglmessage", I.style.fontFamily = "monospace", I.style.fontSize = "13px", I.style.fontWeight = "normal", I.style.textAlign = "center", I.style.background = "#fff", I.style.color = "#000", I.style.padding = "1.5em", I.style.width = "400px", I.style.margin = "5em auto 0", B[A]) E = E.replace("$0", "graphics card");
        else E = E.replace("$0", "browser");
        return E = E.replace("$1", Q[A]), I.innerHTML = E, I
    }
    static isWebGLAvailable() {
        console.warn("isWebGLAvailable() has been deprecated and will be removed in r178. Use isWebGL2Available() instead.");
        try {
            let A = document.createElement("canvas");
            return !!(window.WebGLRenderingContext && (A.getContext("webgl") || A.getContext("experimental-webgl")))
        } catch (A) {
            return !1
        }
    }
    static getWebGLErrorMessage() {
        return console.warn("getWebGLErrorMessage() has been deprecated and will be removed in r178. Use getWebGL2ErrorMessage() instead."), this.getErrorMessage(1)
    }
}
var eR;
var AX = VA(() => {
    eR = tR
});

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L30144-L30179 =====
// camera / renderer (30144-30175) + u5 init wrapper
class Y9 {
    constructor() {
        this.gl = new RQ, this.instance = new RB(0, this.gl.sizes.width / this.gl.sizes.height, 0.1, 10), this.instance.position.z = 10, this.instance.aspect = this.gl.sizes.width / this.gl.sizes.height, this.instance.fov = 2 * Math.atan(this.gl.sizes.height / 2 / this.instance.position.z) * (180 / Math.PI), this.instance.updateProjectionMatrix()
    }
    resize() {
        this.instance.aspect = this.gl.sizes.width / this.gl.sizes.height, this.instance.fov = 2 * Math.atan(this.gl.sizes.height / 2 / this.instance.position.z) * (180 / Math.PI), this.instance.updateProjectionMatrix()
    }
}
var g5 = VA(() => {
    VQ();
    _B()
});
class $9 {
    constructor() {
        bQ.enabled = !0, this.gl = new RQ, this.instance = new tq({
            powerPreference: "high-performance",
            alpha: !0,
            precision: "lowp"
        }), this.instance.autoClear = !1, this.instance.setPixelRatio(this.gl.sizes.pixelRatio), this.instance.setSize(this.gl.sizes.width, this.gl.sizes.height)
    }
    add() {
        let A = document.querySelector(".gl-wrap");
        if (A) A.appendChild(this.instance.domElement).classList.add("gl")
    }
    update() {
        for (let A in this.gl.world.scenes) this.gl.world.scenes[A].renderPipeline(), this.instance.clear();
        this.instance.setRenderTarget(null), this.instance.render(this.gl.scene, this.gl.camera.instance)
    }
    resize() {
        this.instance.setPixelRatio(this.gl.sizes.pixelRatio), this.instance.setSize(this.gl.sizes.width, this.gl.sizes.height)
    }
}
var u5 = VA(() => {
    VQ();
    _B()
});

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L30715-L30816 =====
// head scene group part 1 (30715-30816): W9 HeadDefault + c5 init wrapper
class W9 {
    constructor(A) {
        this.gl = new RQ, this.settings = A, this.params = {
            movement: {
                intensity: 0.075,
                ease: 0.025
            }
        }, this.mouse = {
            normalized: this.gl.mouse.createEasedNormalized(this.params.movement.ease)
        }, this.geometry = new QB(1, 1, 128, 128), this.material = new Y0({
            displacementMap: this.gl.assets.textures.head.depth,
            displacementScale: 0.25,
            alphaMap: this.gl.assets.textures.head.alpha,
            transparent: !0,
            normalMap: this.gl.assets.textures.head.normal,
            roughnessMap: this.gl.assets.textures.head.roughness,
            metalnessMap: this.gl.assets.textures.head.roughness
        }), this.uniforms = {
            uProjectorMatrix: new YA(this.settings.camera.projectionMatrix),
            uProjectorViewMatrix: new YA(this.settings.camera.matrixWorldInverse),
            uHelmetHover: new YA(0),
            tCursorEffect: new YA(null),
            tDefaultDiffuse: new YA(this.gl.assets.textures.head.diffuse),
            tShadowDiffuse: new YA(this.gl.assets.textures.head.shadow.softerEdit)
        }, this.material.onBeforeCompile = (Q) => {
            Q.defines.USE_UV = "", Q.uniforms.uProjectorMatrix = this.uniforms.uProjectorMatrix, Q.uniforms.uProjectorViewMatrix = this.uniforms.uProjectorViewMatrix, Q.uniforms.uHelmetHover = this.uniforms.uHelmetHover, Q.uniforms.tCursorEffect = this.uniforms.tCursorEffect, Q.uniforms.tDefaultDiffuse = this.uniforms.tDefaultDiffuse, Q.uniforms.tShadowDiffuse = this.uniforms.tShadowDiffuse, Q.vertexShader = Q.vertexShader.replace("varying vec3 vViewPosition;", `
          uniform mat4 uProjectorMatrix;
          uniform mat4 uProjectorViewMatrix;

          varying vec3 vViewPosition;
          varying vec4 vTexCoords;
          varying vec3 worldPosition;
        `), Q.vertexShader = Q.vertexShader.replace("#include <fog_vertex>", `
          #include <fog_vertex>

          vTexCoords = projectionMatrix * mvPosition;
        `), Q.fragmentShader = Q.fragmentShader.replace("#include <common>", `
          #include <common>
          varying vec4 vTexCoords;

          uniform float uHelmetHover;
          
          uniform sampler2D tCursorEffect;
          uniform sampler2D tDefaultDiffuse;
          uniform sampler2D tShadowDiffuse;

          
        `), Q.fragmentShader = Q.fragmentShader.replace("vec4 diffuseColor = vec4( diffuse, opacity );", `

          /* 
            UVs
          */
          vec2 cameraUv = vTexCoords.xy / vTexCoords.w; // Convert to screen space
          cameraUv = cameraUv * 0.5 + 0.5; // Transform to [0,1] range

          /* 
            Helmet Hover
          */
          float hoverTransition = cameraUv.y + sin(cameraUv.x * PI) * sin(uHelmetHover * PI) * 0.2;

          /* 
            Texture Cursor Effect
          */
          vec4 textureCursorEffect = texture2D(tCursorEffect, vec2(cameraUv.x, 0.025 + cameraUv.y * 0.95)); // Gap fix on bottom and top
          textureCursorEffect.rgb = 1.0 - textureCursorEffect.rgb;
          
          float cursorEffect = step(0.1, textureCursorEffect.r);

          /* 
            Texture Shadow Diffuse
          */
          vec4 textureDefaultDiffuse = texture2D(tDefaultDiffuse, vUv);
          vec4 textureShadowDiffuse = texture2D(tShadowDiffuse, vUv);
          // textureShadowDiffuse = sRGBTransferOETF(textureShadowDiffuse);

          /* 
            Color
          */
          vec3 color = mix(textureDefaultDiffuse.rgb, textureShadowDiffuse.rgb, min(cursorEffect + step(1.0 - hoverTransition, uHelmetHover), 1.0));
          // color.b += 0.05;
          // color *= 1.1;

          vec4 diffuseColor = vec4( color, opacity );
          // diffuseColor = vec4(vec3(step(1.0 - hoverTransition, uHelmetHover)), opacity);

          // #include <tonemapping_fragment>
          // #include <colorspace_fragment>
          
        `)
        }, this.instance = new OQ(this.geometry, this.material)
    }
    resize() {}
    update(A, Q) {
        if (this.mouse.normalized.update(this.gl.time.delta), this.instance.rotation.y = this.mouse.normalized.value.x * this.params.movement.intensity * A, this.gl.sizes.width > 768) this.instance.rotation.x = this.mouse.normalized.value.y * -1 * this.params.movement.intensity * A * (1 - Q);
        this.uniforms.tCursorEffect.value = this.gl.world.fluidCursor.sourceTarget.texture
    }
}
var c5 = VA(() => {
    VQ();
    _B()
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L30942-L32010 =====
// head scene group part 2 (30942-32010): FT/R9 google variant, lens-flare/disco materials, Helmet, LightingDefault, O9 HeadScene + composite shader (32011 `var M9;` belongs to the postprocessing module that follows)
var FT, R9;
var EY = VA(() => {
    FT = Math.random() > 0.5 ? !0 : !1, R9 = FT
});

function o5() {
    let A = new RQ,
        Q = new fQ({
            depthTest: !1,
            depthWrite: !1,
            transparent: !0,
            blending: YC,
            uniforms: {
                uHelmetRotation: new YA(new o(0, 0, 0)),
                uCameraRotation: new YA(new o(0, 0, 0)),
                uTransition: new YA(0),
                tLensFlare: new YA(A.assets.textures.disco.lensFlare)
            },
            vertexShader: `
      attribute float aRandom;

      varying vec2 vUv;
      varying float vDot;
      varying vec3 vNormal;

      uniform vec3 uHelmetRotation;
      uniform vec3 uCameraRotation;
      

      void main() {
        vec3 n = normalize(normalMatrix * mat3(instanceMatrix) * normal);
        
        // Scale instance Matrix
        mat4 scaleMatrix = mat4(
          vec4(1.0, 0.0, 0.0, 0.0),
          vec4(0.0, 1.0, 0.0, 0.0),
          vec4(0.0, 0.0, 1.0, 0.0),
          vec4(0.0, 0.0, 0.0, 1.0)
        );

        scaleMatrix[0][0] = aRandom;    

        gl_Position = projectionMatrix * (modelViewMatrix * instanceMatrix * mat4(0.5 + aRandom * 0.5) * vec4(0.0, 0.0, 0.0, 1.0) + vec4(position.x, position.y, 0.0, 0.0));
        // gl_Position = projectionMatrix * (modelViewMatrix * instanceMatrix * vec4(0.0, 0.0, 0.0, 1.0) + vec4(position.x, position.y, 0.0, 0.0));
        // gl_Position = projectionMatrix * modelViewMatrix * instanceMatrix * vec4(position, 1.0);
        

        // vec3 viewPosition = normalize(modelViewMatrix * vec4(position, 1.0)).rgb;
        
        vNormal = n;
        vDot = smoothstep(0.9, 0.95, dot(n, vec3(0.0, 0.0, 1.0)));
        vUv = uv;
      }
    `,
            fragmentShader: `
      varying vec2 vUv;
      varying float vDot;
      varying vec3 vNormal;
      
      uniform sampler2D tLensFlare;
      uniform float uTransition;

      void main() {
        vec4 textureLensFlare = texture2D(tLensFlare, vUv); 

        gl_FragColor = vec4(textureLensFlare.rgb, vDot * textureLensFlare.a * 0.95 * uTransition);
        // gl_FragColor = vec4(textureLensFlare.rgb, textureLensFlare.a);
        // gl_FragColor = vec4(vNormal, 1.0);
      }
    `
        });
    return Q.customProgramCacheKey = () => Math.random().toString(), Q
}

function i5(A = {}) {
    let Q = new RQ,
        B = new Y0({
            color: new pA(16777215),
            envMap: Q.assets.hdri.light,
            envMapIntensity: A.envMapIntensity != null ? A.envMapIntensity : 1.5,
            roughness: 0,
            metalness: 0.9,
            transparent: !0
        }),
        E = {
            tDiscoMask: new YA(Q.assets.textures.disco.mask),
            tDiscoMatcap: new YA(Q.assets.textures.disco.matcap),
            uTime: new YA(0),
            uMatcapIntensity: new YA(A.matcapIntensity != null ? A.matcapIntensity : 0.5),
            uTransitionIn: new YA(0),
            uTransitionOut: new YA(0),
            uBrightness: new YA(A.brightness)
        };
    return B.onBeforeCompile = (I) => {
        I.uniforms.tDiscoMask = E.tDiscoMask, I.uniforms.tDiscoMatcap = E.tDiscoMatcap, I.uniforms.uTime = E.uTime, I.uniforms.uMatcapIntensity = E.uMatcapIntensity, I.uniforms.uTransitionIn = E.uTransitionIn, I.uniforms.uTransitionOut = E.uTransitionOut, I.uniforms.uBrightness = E.uBrightness, I.vertexShader = I.vertexShader.replace("varying vec3 vViewPosition;", `
        varying vec3 vViewPosition;
        varying vec3 vLocalPosition;
        varying vec3 vWorldPosition;
        varying vec2 vUv;
      `), I.vertexShader = I.vertexShader.replace("#include <fog_vertex>", `
        #include <fog_vertex>

        vLocalPosition = position.xyz;
        vWorldPosition = worldPosition.xyz;
        vUv = uv;
      `), I.fragmentShader = I.fragmentShader.replace("#include <common>", `
        #include <common>
        #include <rotateUV>

        uniform sampler2D tDiscoMask;
        uniform sampler2D tDiscoMatcap;

        uniform float uMatcapIntensity;
        uniform float uTime;
        uniform float uTransitionIn;
        uniform float uTransitionOut;
        uniform float uBrightness;

        varying vec2 vUv;
        varying vec3 vLocalPosition;
        varying vec3 vWorldPosition;
      `), I.fragmentShader = I.fragmentShader.replace("#include <dithering_fragment>", `
        #include <dithering_fragment>

        /* 
          Matcap
        */
        vec3 viewDirection = normalize(vViewPosition);
        vec3 x = normalize( vec3( viewDirection.z, 0.0, -viewDirection.x ) );
        vec3 y = cross( viewDirection, x ); 
        vec2 matcapUV = vec2( dot( x, vNormal ), dot( y, vNormal ) ) * 0.495 + 0.5; 
        // matcapUV = rotateUV(matcapUV, uTime * 100.);

        float mask = texture2D(tDiscoMask, vUv).r;
        vec3 matcapTexture = texture2D(tDiscoMatcap, matcapUV).rgb;

        /* 
          Transition
        */
        float transition = vLocalPosition.y;

        /* 
          Color
        */
        vec3 color = mix(max(matcapTexture, vec3(0.0, 0.114, 0.144)) * outgoingLight * uBrightness, vec3(1.0), mask);
        // color += fract(uTime);

        gl_FragColor = vec4(color, step(mix(0.0425, -0.0425, uTransitionIn), transition) - step(mix(0.0425, -0.0425, uTransitionOut), transition));
      `)
    }, B.customProgramCacheKey = () => Math.random().toString(), B.uniforms = E, B
}
var n5 = VA(() => {
    VQ();
    _B()
});
class I8 {
    constructor(A, Q = !1, B = 1) {
        this.gl = new RQ, this.instance = new TB, A.add(this.instance), this.direction = -1, this.invertOut = Q, this.discoMesh = this.gl.assets.models.disco.scene.children[0].clone(), this.discoMesh.visible = !1, this.discoMesh.material = i5({
            brightness: B
        }), this.instance.add(this.discoMesh), this.lensFlareMaterial = o5(), this.lensFlaresEmpties = this.discoMesh.children, this.lensFlareInstancedMesh = new XJ(new QB(0.5, 0.5), this.lensFlareMaterial, this.lensFlaresEmpties.length), this.lensFlareInstancedMesh.renderOrder = 99, this.lensFlareInstancedMesh.visible = !1;
        let E = new Float32Array(this.lensFlaresEmpties.length);
        for (let C = 0; C < this.lensFlaresEmpties.length; C++) E[C] = Math.random();
        let I = new ZC(E, 1);
        this.lensFlareInstancedMesh.geometry.setAttribute("aRandom", I), this.lensFlareDummy = new qB, this.lensFlaresEmpties.forEach((C, K) => {
            this.lensFlareDummy.rotation.copy(C.rotation), this.lensFlareDummy.position.copy(C.position);
            let J = 0.01;
            this.lensFlareDummy.scale.set(J, J, J), this.lensFlareDummy.updateMatrix(), this.lensFlareInstancedMesh.setMatrixAt(K, this.lensFlareDummy.matrix)
        }), this.instance.add(this.lensFlareInstancedMesh), this.targetWord = "disco", this.typedChars = "", this.lastTypedTime = 0, this.timeout = 5000, this.handleKeyPress = this.handleKeyPress.bind(this), this.startListening()
    }
    startListening() {
        document.addEventListener("keypress", this.handleKeyPress)
    }
    stopListening() {
        document.removeEventListener("keypress", this.handleKeyPress)
    }
    handleKeyPress(A) {
        let Q = Date.now();
        if (Q - this.lastTypedTime > this.timeout) this.typedChars = "";
        if (this.lastTypedTime = Q, this.typedChars += A.key.toLowerCase(), this.typedChars.length > this.targetWord.length) this.typedChars = this.typedChars.slice(-this.targetWord.length);
        if (this.typedChars === this.targetWord) this.direction = this.direction == -1 ? 1 : -1, this.transition(this.direction), document.dispatchEvent(new CustomEvent("disco", {
            detail: {
                direction: this.direction
            }
        })), this.typedChars = ""
    }
    transition(A) {
        if (A == 1) document.documentElement.classList.add("gl__is-disco"), this.discoMesh.visible = !0, this.lensFlareInstancedMesh.visible = !0, m.to(this.discoMesh.material.uniforms.uTransitionIn, {
            value: 1,
            duration: 2,
            ease: "expo.inOut"
        }), m.to(this.lensFlareMaterial.uniforms.uTransition, {
            value: 1,
            duration: 2,
            ease: "expo.inOut"
        });
        else {
            if (document.documentElement.classList.remove("gl__is-disco"), this.invertOut) m.to(this.discoMesh.material.uniforms.uTransitionOut, {
                value: 1,
                duration: 2,
                ease: "expo.inOut",
                onComplete: () => {
                    this.discoMesh.visible = !1, this.lensFlareInstancedMesh.visible = !1, this.discoMesh.material.uniforms.uTransitionIn.value = 0, this.discoMesh.material.uniforms.uTransitionOut.value = 0
                }
            });
            else m.to(this.discoMesh.material.uniforms.uTransitionIn, {
                value: 0,
                duration: 2,
                ease: "expo.inOut"
            });
            m.to(this.lensFlareMaterial.uniforms.uTransition, {
                value: 0,
                duration: 2,
                ease: "expo.inOut"
            })
        }
    }
}
var X9 = VA(() => {
    VQ();
    LB();
    n5();
    _B()
});
class F9 {
    constructor(A) {
        if (this.gl = new RQ, this.settings = A, this.instance = this.gl.assets.models.helmet.scene.clone(), this.instance.scale.set(6.9, 6.9, 7.1), this.uniforms = {
                tMask: new YA(this.gl.assets.textures.helmet.mask),
                uTime: new YA(0),
                uHoverReveal: new YA(1),
                uDarkEdges: new YA(0),
                uHelmetTransition: new YA(0),
                tCurrentTexture: new YA(this.gl.assets.textures.helmet.diffuseLime),
                tNextTexture: new YA(this.gl.assets.textures.helmet.diffuseDisco)
            }, this.helmetMaterial = new Y0({
                normalMap: this.gl.assets.textures.helmet.normal,
                metalness: 1,
                roughness: 0.05,
                envMap: this.gl.assets.hdri.light,
                envMapIntensity: 3
            }), this.glassMaterial = new Y0({
                map: this.gl.assets.textures.glass.base,
                roughnessMap: this.gl.assets.textures.glass.roughness,
                normalMap: this.gl.assets.textures.glass.normal,
                metalnessMap: this.gl.assets.textures.helmet.metallic,
                envMap: this.gl.assets.hdri.light,
                envMapIntensity: 1.5
            }), this.plasticMaterial = new rC({
                transparent: !0,
                opacity: 0.25,
                matcap: this.gl.assets.textures.plastic.matcap,
                side: hB
            }), this.instance.children.forEach((Q) => {
                if (Q.name == "helmet") this.helmetMesh = Q, Q.material = this.helmetMaterial, Q.material.onBeforeCompile = (B) => {
                    B.vertexShader = B.vertexShader.replace("varying vec3 vViewPosition;", `
              varying vec3 vViewPosition;
              varying vec3 vLocalPosition;
              varying vec3 vWorldPosition;
              varying vec2 vUv;
            `), B.vertexShader = B.vertexShader.replace("#include <fog_vertex>", `
              vLocalPosition = position.xyz;
              vWorldPosition = worldPosition.xyz;
              vUv = uv;
            `), B.uniforms.uTime = this.uniforms.uTime, B.uniforms.tMask = this.uniforms.tMask, B.uniforms.uHoverReveal = this.uniforms.uHoverReveal, B.uniforms.uDarkEdges = this.uniforms.uDarkEdges, B.uniforms.uHelmetTransition = this.uniforms.uHelmetTransition, B.uniforms.tCurrentTexture = this.uniforms.tCurrentTexture, B.uniforms.tNextTexture = this.uniforms.tNextTexture, B.fragmentShader = B.fragmentShader.replace("#include <common>", `
              const float PI = 3.141592;
              varying vec3 vWorldPosition;
              varying vec3 vLocalPosition;
              varying vec2 vUv;

              uniform float uDarkEdges;
              uniform float uHelmetTransition;

              uniform sampler2D tCurrentTexture;
              uniform sampler2D tNextTexture;

              #include <common>
            `), B.fragmentShader = B.fragmentShader.replace("vec4 diffuseColor = vec4( diffuse, opacity );", `
              vec4 diffuseColor = vec4(vec3(0.0), 1.0);

              vec4 textureCurrent = texture2D(tCurrentTexture, vUv);
              vec4 textureNext = texture2D(tNextTexture, vUv);

              float transition = vLocalPosition.y - sin(vLocalPosition.x * PI) * sin(uHelmetTransition * PI) * 0.1;

              vec3 color = mix(textureNext.rgb, textureCurrent.rgb, step(transition, mix(0.05, -0.05, uHelmetTransition)));

              if (gl_FrontFacing) {
                diffuseColor = vec4(color, opacity);
              }
            `)
                };
                if (Q.name == "glass") this.glassMesh = Q, Q.material = this.glassMaterial, Q.material.onBeforeCompile = (B) => {
                    B.vertexShader = B.vertexShader.replace("varying vec3 vViewPosition;", `
              varying vec3 vViewPosition;
              varying vec3 vLocalPosition;
              varying vec3 vWorldPosition;
              varying vec2 vUv;
            `), B.vertexShader = B.vertexShader.replace("#include <fog_vertex>", `
              vLocalPosition = position.xyz;
              vWorldPosition = worldPosition.xyz;
              vUv = uv;
            `), B.uniforms.uHoverReveal = this.uniforms.uHoverReveal, B.uniforms.uDarkEdges = this.uniforms.uDarkEdges, B.fragmentShader = B.fragmentShader.replace("#include <common>", `
              varying vec3 vWorldPosition;
              varying vec3 vLocalPosition;
              varying vec2 vUv;

              // uniform float uHoverReveal;
              uniform float uDarkEdges;

              #include <common>
            `), B.fragmentShader = B.fragmentShader.replace("vec4 diffuseColor = vec4( diffuse, opacity );", `              
              // vec4 diffuseColor = vec4( diffuse, opacity);
              // vec4 diffuseColor = vec4( diffuse, step(-vLocalPosition.y * 10., uHoverReveal - 0.5));
              // vec4 diffuseColor = vec4(max(vec3(vWorldPosition.y * -1.0) * 100., 1.0), opacity );
              float darkEdges = clamp(1.0 - abs(vLocalPosition.x) * 30., 0.2, 1.0);

              vec4 diffuseColor = vec4(vec3(diffuse * mix(1.0, darkEdges, uDarkEdges)), opacity);
            `)
                };
                if (Q.name == "plastic") this.plasticMesh = Q, Q.renderOrder = 1, Q.material = this.plasticMaterial
            }), this.scene = new rB, this.scene.add(this.instance), this.renderTarget = new XB(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio, {
                samples: 2
            }), this.wireframeMeshMaterial = new fQ({
                wireframe: !0,
                transparent: !0,
                uniforms: {
                    uTime: new YA(0),
                    uIsWireframeAnimating: new YA(window.landoGL.params.headScene.IS_WIREFRAME_ANIMATING),
                    uOpacity: new YA(1)
                },
                vertexShader: `
        varying vec2 vUv;
        varying vec3 vPosition;

        void main()
        {
          gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);

          vUv = uv;
          vPosition = position;
        }
      `,
                fragmentShader: `
        #include <simplex>

        varying vec2 vUv;
        varying vec3 vPosition;

        uniform float uTime;
        uniform float uOpacity;
        uniform bool uIsWireframeAnimating;

        void main() {
          float scanEffect = 0.1;

          if (uIsWireframeAnimating) {
            scanEffect = pow(fract(-vPosition.y * 10. - uTime), 4.) * 0.1;
          } 
          // float scanEffect = 0.5 + snoise(vPosition * 10. + uTime) * 0.5;

          gl_FragColor = vec4(vec3(0.0), scanEffect * uOpacity);
        }
      `
            }), this.wireframeGeometries = [], this.gl.assets.models.helmet.scene.clone().children.forEach((Q) => {
                this.wireframeGeometries.push(Q.geometry)
            }), this.wireframeMeshGeometry = s5(this.wireframeGeometries), this.wireframeMesh = new OQ(this.wireframeMeshGeometry, this.wireframeMeshMaterial), this.wireframeMesh.position.copy(this.instance.position), this.wireframeMesh.scale.copy(this.instance.scale), this.settings.scene.add(this.wireframeMesh), this.disco = new I8(this.instance, !1, 1), document.addEventListener("disco", (Q) => {
                this.animateInDisco(Q.detail.direction)
            }), window.landoGL.params.headScene.VARIANT = R9 ? "Google" : this.gl.time.getVariantAccordingToTime(), this.setVariant(window.landoGL.params.headScene.VARIANT), this.gl.isDebug) this.setDebug()
    }
    animateInDisco(A) {
        if (A == 1) m.to(this.uniforms.uHelmetTransition, {
            value: 1,
            duration: 2,
            ease: "expo.inOut"
        });
        else m.to(this.uniforms.uHelmetTransition, {
            value: 0,
            duration: 2,
            ease: "expo.inOut"
        })
    }
    setVariant(A) {
        if (A == "Lime") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseLime, this.helmetMaterial.envMapIntensity = 3, this.helmetMaterial.iridescence = 0, this.helmetMaterial.needsUpdate = !0;
        else if (A == "Dark") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseDark, this.helmetMaterial.envMapIntensity = 3, this.helmetMaterial.iridescence = 0, this.helmetMaterial.needsUpdate = !0;
        else if (A == "Disco") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseDisco, this.helmetMaterial.envMapIntensity = 1.5, this.helmetMaterial.iridescence = 0, this.helmetMaterial.needsUpdate = !0;
        else if (A == "Grid") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseGrid, this.helmetMaterial.envMapIntensity = 3, this.helmetMaterial.iridescence = 0, this.helmetMaterial.needsUpdate = !0;
        else if (A == "Google") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseGoogle, this.helmetMaterial.envMapIntensity = 3, this.helmetMaterial.iridescence = 0, this.helmetMaterial.needsUpdate = !0
    }
    setHDRI(A) {
        if (A == "Light") this.helmetMaterial.envMap = this.gl.assets.hdri.light, this.helmetMaterial.needsUpdate = !0, this.glassMaterial.envMap = this.gl.assets.hdri.light, this.glassMaterial.needsUpdate = !0;
        else if (A == "Dark") this.helmetMaterial.envMap = this.gl.assets.hdri.dark, this.helmetMaterial.needsUpdate = !0, this.glassMaterial.envMap = this.gl.assets.hdri.dark, this.glassMaterial.needsUpdate = !0
    }
    setDebug() {
        this.gl.world.helmetDebugFolder.add(this.wireframeMeshMaterial.uniforms.uIsWireframeAnimating, "value").name("IS_WIREFRAME_ANIMATING");
        let A = {
            variant: {}
        };
        this.gl.world.helmetDebugFolder.add(A, "variant", ["Lime", "Dark", "Grid", "Disco"]).name("HELMET VARIANT").onChange((U) => {
            this.setVariant(U)
        }).setValue("Lime");
        let B = {
                variant: {}
            },
            E = this.gl.world.helmetDebugFolder.addFolder("Helmet HDRI");
        E.add(B, "variant", ["Light", "Dark"]).name("VARIANT").onChange((U) => {
            this.setHDRI(U)
        }).setValue("Light"), E.add(this.uniforms.uDarkEdges, "value").name("Darken Edges").min(0).max(1).step(0.01);
        let C = {
            rotation: 0
        };
        E.add(C, "rotation").name("HDRI Rotation X").min(0).max(Math.PI * 2).step(0.01).onChange((U) => {
            this.helmetMaterial.envMapRotation = new M0(U, this.helmetMaterial.envMapRotation.y, this.helmetMaterial.envMapRotation.z), this.helmetMaterial.needsUpdate = !0, this.glassMaterial.envMapRotation = new M0(U, this.glassMaterial.envMapRotation.y, this.glassMaterial.envMapRotation.z), this.glassMaterial.needsUpdate = !0
        });
        let K = {
            rotation: 0
        };
        E.add(K, "rotation").name("HDRI Rotation Y").min(0).max(Math.PI * 2).step(0.01).onChange((U) => {
            this.helmetMaterial.envMapRotation = new M0(this.helmetMaterial.envMapRotation.x, U, this.helmetMaterial.envMapRotation.z), this.helmetMaterial.needsUpdate = !0, this.glassMaterial.envMapRotation = new M0(this.glassMaterial.envMapRotation.x, U, this.glassMaterial.envMapRotation.z), this.glassMaterial.needsUpdate = !0
        });
        let J = {
            rotation: 0
        };
        E.add(J, "rotation").name("HDRI Rotation Z").min(0).max(Math.PI * 2).step(0.01).onChange((U) => {
            this.helmetMaterial.envMapRotation = new M0(this.helmetMaterial.envMapRotation.x, this.helmetMaterial.envMapRotation.y, U), this.helmetMaterial.needsUpdate = !0, this.glassMaterial.envMapRotation = new M0(this.glassMaterial.envMapRotation.x, this.glassMaterial.envMapRotation.y, U), this.glassMaterial.needsUpdate = !0
        })
    }
    resize() {
        this.renderTarget.setSize(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio)
    }
    update() {
        this.gl.renderer.instance.setRenderTarget(this.renderTarget), this.gl.renderer.instance.render(this.scene, this.settings.camera), this.gl.renderer.instance.clear(), this.uniforms.uTime.value = this.gl.time.elapsed, this.wireframeMeshMaterial.uniforms.uTime.value = this.gl.time.elapsed, this.wireframeMesh.rotation.copy(this.instance.rotation)
    }
}
var a5 = VA(() => {
    VQ();
    BY();
    LB();
    EY();
    _B();
    X9()
});
class N9 {
    constructor(A) {
        if (this.gl = new RQ, this.settings = A, this.instance = new TB, this.hemisphereLight = new Q9(16777215, 0, 2.5), this.instance.add(this.hemisphereLight), this.pointLight = new LJ(16777215, 2.5), this.pointLight.position.x = -0.75, this.pointLight.position.y = 0.2, this.pointLight.position.z = 0.4, this.instance.add(this.pointLight), this.gl.isDebug) this.pointLightHelper = new K9(this.pointLight, 0.1, 0), this.instance.add(this.pointLightHelper)
    }
    update() {}
}
var r5 = VA(() => {
    VQ();
    _B()
});
class O9 {
    constructor(A) {
        if (this.settings = A, this.id = "head", this.isRendering = !1, this.isDisco = !1, this.scrollProgress = 0, this.target = document.querySelector('[data-sticky-hero="target"]'), this.gl = new RQ, this.easedMouse = {
                normalized: this.gl.mouse.createEasedNormalized(0.025),
                pace: this.gl.mouse.createEasedPace(0.01)
            }, this.params = {
                movement: {
                    intensity: 0.075,
                    ease: 0.025
                }
            }, this.defaultScene = new rB, this.renderPlane = {
                mesh: new OQ(new QB(1, 1), new fQ({
                    uniforms: {
                        uAspect: new YA(this.gl.sizes.width / this.gl.sizes.height),
                        uTime: new YA(0),
                        uReveal: new YA(window.landoGL.reveal),
                        uColorHover: new YA(new pA(0)),
                        uHover: new YA(0),
                        uHelmetHover: new YA(0),
                        uFilter: new YA(0),
                        uCursorIntensity: new YA(0),
                        tFluid: new YA(null),
                        tDefaultDiffuse: new YA(null),
                        tHelmet: new YA(null),
                        tCursorEffect: new YA(null),
                        tBackgroundNoise: new YA(null),
                        tNoise: new YA(this.gl.assets.textures.noise.texture),
                        OUTLINE: new YA(window.landoGL.params.headScene.OUTLINE),
                        SHOW_HELMET_PERMANENTLY: new YA(window.landoGL.params.headScene.SHOW_HELMET_PERMANENTLY),
                        THICKNESS: new YA(window.landoGL.params.headScene.THICKNESS),
                        COLOR_OUTLINE: new YA(new pA(window.landoGL.params.headScene.COLOR_OUTLINE).convertLinearToSRGB()),
                        COLOR_FOREGROUND: new YA(new pA(window.landoGL.params.headScene.COLOR_FOREGROUND).convertLinearToSRGB()),
                        COLOR_BACKGROUND: new YA(new pA(window.landoGL.params.headScene.COLOR_BACKGROUND).convertLinearToSRGB()),
                        COLOR_CURSOR_FOREGROUND: new YA(new pA(window.landoGL.params.headScene.COLOR_CURSOR_FOREGROUND).convertLinearToSRGB()),
                        COLOR_CURSOR_BACKGROUND: new YA(new pA(window.landoGL.params.headScene.COLOR_CURSOR_BACKGROUND).convertLinearToSRGB()),
                        COLOR_CURSOR_OUTLINE: new YA(new pA(window.landoGL.params.headScene.COLOR_CURSOR_OUTLINE).convertLinearToSRGB()),
                        COLOR_FILTER: new YA(new pA(window.landoGL.params.headScene.COLOR_FILTER).convertLinearToSRGB()),
                        SCALE: new YA(window.landoGL.params.headScene.SCALE),
                        SPEED: new YA(window.landoGL.params.headScene.SPEED),
                        DISTORT_SCALE: new YA(window.landoGL.params.headScene.DISTORT_SCALE),
                        DISTORT_INTENSITY: new YA(window.landoGL.params.headScene.DISTORT_INTENSITY),
                        NOISE_DETAIL: new YA(window.landoGL.params.headScene.NOISE_DETAIL),
                        CURSOR_INTENSITY: new YA(window.landoGL.params.headScene.CURSOR_INTENSITY),
                        CURSOR_SCALE: new YA(window.landoGL.params.headScene.CURSOR_SCALE),
                        CURSOR_BOUNCE: new YA(window.landoGL.params.headScene.CURSOR_BOUNCE),
                        REVEAL_SIZE: new YA(window.landoGL.params.headScene.REVEAL_SIZE)
                    },
                    vertexShader: `
            varying vec2 vUv;

            void main() {
              gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);

              vUv = uv;
            }
          `,
                    fragmentShader: `
  

            // Includes
            #include <tonemapping_pars_fragment>

            const float PI = 3.141592;

            // Functions
            vec3 toGrayscale(vec3 color) {
              float gray = dot(color, vec3(0.299, 0.587, 0.114)); // Standard grayscale weights
              return vec3(gray);
            }

            vec3 adjustContrast(vec3 color, float contrast) {
              return (color - 0.5) * contrast + 0.5;
            }

            float blendScreen(float base, float blend) {
              return 1.0-((1.0-base)*(1.0-blend));
            }

            vec3 blendScreen(vec3 base, vec3 blend) {
              return vec3(blendScreen(base.r,blend.r),blendScreen(base.g,blend.g),blendScreen(base.b,blend.b));
            }

            float blendOverlay(float base, float blend) {
              return base<0.5?(2.0*base*blend):(1.0-2.0*(1.0-base)*(1.0-blend));
            }
            
            vec3 blendOverlay(vec3 base, vec3 blend) {
              return vec3(blendOverlay(base.r,blend.r),blendOverlay(base.g,blend.g),blendOverlay(base.b,blend.b));
            }

            vec3 blendHardLight(vec3 base, vec3 blend) {
              return blendOverlay(blend,base);
            }

            // Uniforms & Varyings
            uniform float uAspect;
            uniform float uTime;
            uniform float uReveal;
            uniform float uHover;
            uniform float uHelmetHover;
            uniform float uFilter;
            uniform float uCursorIntensity;
            uniform vec3 uColorHover;
            
            uniform sampler2D tDefaultDiffuse;
            // uniform sampler2D tShadowDiffuse;
            uniform sampler2D tHelmet;
            uniform sampler2D tCursorEffect;
            uniform sampler2D tBackgroundNoise;
            uniform sampler2D tNoise;
            
            uniform bool OUTLINE;
            uniform bool SHOW_HELMET_PERMANENTLY;
            uniform float THICKNESS;
            uniform vec3 COLOR_OUTLINE;
            uniform vec3 COLOR_FOREGROUND;
            uniform vec3 COLOR_BACKGROUND;
            uniform vec3 COLOR_CURSOR_FOREGROUND;
            uniform vec3 COLOR_CURSOR_BACKGROUND;
            uniform vec3 COLOR_CURSOR_OUTLINE;
            uniform vec3 COLOR_FILTER;
            uniform float SCALE;
            uniform float SPEED;
            uniform float DISTORT_SCALE;
            uniform float DISTORT_INTENSITY;
            uniform float NOISE_DETAIL;
            uniform float CURSOR_INTENSITY;
            uniform float CURSOR_SCALE;
            uniform float CURSOR_BOUNCE;
            uniform float REVEAL_SIZE;
            
            varying vec2 vUv;
            
            void main() {
              /* 
                UVs
              */
              vec2 uv = vUv;
              uv.x *= uAspect;
              uv.y += (REVEAL_SIZE + REVEAL_SIZE / 3.) * (1.0 - uReveal);
              uv.y /= 1.0 + (REVEAL_SIZE) * (1.0 - uReveal);
            
              /* 
                Noise
              */
              vec4 textureBackgroundNoise = texture2D(tBackgroundNoise, vUv);

              // float noiseDistort = 0.5 + snoise(vec3(uv.x * DISTORT_SCALE, uv.y * DISTORT_SCALE, uTime * SPEED * 0.1)) * 0.5;
            
              // float noiseFinal = 0.5 + snoise(
              //   vec3(
              //     (uv.x + (cursor * CURSOR_INTENSITY) + (noiseDistort * DISTORT_INTENSITY)) * SCALE, 
              //     (uv.y + (cursor * CURSOR_INTENSITY) + (noiseDistort * DISTORT_INTENSITY)) * SCALE, 
              //     uTime * SPEED
              //   )
              // ) * 0.5;
              // noiseFinal *= NOISE_DETAIL;
              // noiseFinal = fract(noiseFinal); 

              /* 
                Noise Texture
              */
              vec4 textureNoise = texture2D(tNoise, uv * 2.0);
            
              /* 
                Cursor Effect
              */
              vec4 textureCursorEffect = texture2D(tCursorEffect, vec2(0.025 + vUv.x * 0.95, 0.025 + vUv.y * 0.95)); // Gap fix on bottom and top
              textureCursorEffect.rgb = 1.0 - textureCursorEffect.rgb;
              
              float cursorEffect = step(0.1, textureCursorEffect.r);
            
              /* 
                Outline
              */
              float noiseBase = textureBackgroundNoise.r;

              vec3 background = mix(
                COLOR_BACKGROUND, 
                mix(COLOR_BACKGROUND, COLOR_FOREGROUND, uReveal),
                noiseBase
              );

              // vec3 backgroundInverted = mix(
              //   COLOR_BACKGROUND, 
              //   mix(COLOR_FOREGROUND, COLOR_BACKGROUND, uReveal),
              //   noiseBase
              // );

              /* 
                Cursor Overlay
              */
              vec3 cursorBackground = mix(COLOR_CURSOR_BACKGROUND, COLOR_CURSOR_FOREGROUND, noiseBase);

            
              if (OUTLINE) {
                float edge = 0.0;
            
                // Check neighboring pixels for edge detection
                vec4 sampledRight = texture2D(tBackgroundNoise, vUv + vec2(THICKNESS, 0.0));
                vec4 sampledLeft = texture2D(tBackgroundNoise, vUv + vec2(-THICKNESS, 0.0));
                vec4 sampledUp = texture2D(tBackgroundNoise, vUv + vec2(0.0, THICKNESS));
                vec4 sampledDown = texture2D(tBackgroundNoise, vUv + vec2(0.0, -THICKNESS));
                
                if (sampledRight.r != textureBackgroundNoise.r ||
                    sampledLeft.r != textureBackgroundNoise.r ||
                    sampledUp.r != textureBackgroundNoise.r ||
                    sampledDown.r != textureBackgroundNoise.r) {
                  edge = 1.0;
                }
            
                background = mix(
                  COLOR_BACKGROUND, 
                  mix(COLOR_BACKGROUND, COLOR_OUTLINE, uReveal),
                  edge 
                );

                cursorBackground = mix(
                  cursorBackground,
                  COLOR_CURSOR_OUTLINE,
                  edge
                );

                // backgroundInverted = mix(
                //   COLOR_OUTLINE, 
                //   mix(COLOR_OUTLINE, COLOR_BACKGROUND, uReveal),
                //   edge 
                // );
              }
            
              /* 
                Hover
              */
              background = mix(
                background,
                uColorHover,
                step(textureBackgroundNoise.g, -0.1 + uHover * 1.1)
              );
            


              background = mix(
                background,
                cursorBackground,
                cursorEffect * uCursorIntensity
              );
            
              /* 
                Diffuse
              */
              vec4 textureDefaultDiffuse = texture2D(tDefaultDiffuse, vUv);
              // vec4 textureShadowDiffuse = texture2D(tShadowDiffuse, vUv);

              // textureDefaultDiffuse.rgb = mix(textureDefaultDiffuse.rgb, textureShadowDiffuse.rgb, cursorEffect);
            
              /* 
                Helmet
              */
              vec4 textureHelmet = texture2D(tHelmet, vUv);

              /* 
                Hover on helmet
              */
              float hoverTransition = vUv.y + sin(vUv.x * PI) * sin(uHelmetHover * PI) * 0.2;

              cursorEffect += step(1.0 - hoverTransition, uHelmetHover);
              cursorEffect = clamp(cursorEffect * uCursorIntensity, 0.0, 1.0);

              background = mix(background, cursorBackground, step(1.0 - hoverTransition, uHelmetHover));
              // background -= fract(smoothstep(uHelmetHover - 0.1, uHelmetHover, 1.0 - hoverTransition)) * sin(uHelmetHover * PI) * 0.25;
            
              vec3 base = mix(background, textureDefaultDiffuse.rgb, textureDefaultDiffuse.a);

              // float hoverEffect = smoothstep(textureNoise.r, textureNoise.r + 0.05, uHelmetHover);
              // hoverEffect -= step(textureNoise.r, uHelmetHover);

              // textureHelmet.rgb -= hoverEffect * 0.5;
            
              vec3 final = mix(base, textureHelmet.rgb, cursorEffect * textureHelmet.a);
              // vec3 final = mix(base, textureHelmet.rgb, 0.0);

              if (SHOW_HELMET_PERMANENTLY) {
                // base = mix(background, textureShadowDiffuse.rgb, textureDefaultDiffuse.a);
                final = mix(base, textureHelmet.rgb, textureHelmet.a);
              }

              /* 
                Transition
              */
              vec3 end = mix(textureDefaultDiffuse.rgb, vec3(0.05), 1.0 - textureDefaultDiffuse.a);
              end = toGrayscale(end);
              end = adjustContrast(end, 0.8);
              end *= 0.75;
              end = blendHardLight(end, COLOR_FILTER);

              vec3 transition = mix(final, end, uFilter);
            
              gl_FragColor = vec4(transition, 1.0);
              // vec4 test = texture2D(tDefaultDiffuse, vUv);

              // gl_FragColor = vec4(vec3(test.rgb), 1.0);
              
              // gl_FragColor = textureCursorEffect;
            
              #include <tonemapping_fragment>
              #include <colorspace_fragment>
              // gl_FragColor.rgb = LinearToneMapping( gl_FragColor.rgb );
              // gl_FragColor = LinearTransferOETF( gl_FragColor );
            }
          `,
                    transparent: !0
                })),
                bounds: {
                    top: 0,
                    left: 0,
                    width: this.gl.sizes.width,
                    height: this.gl.sizes.height
                }
            }, this.renderPlane.mesh.renderOrder = 3, this.defaultRenderTarget = new XB(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio, {
                samples: 1
            }), this.cameraTransformGroup = new TB, this.camera = new RB(15, this.gl.sizes.width / this.gl.sizes.height, 0.1, 1000), this.cameraTransformGroup.add(this.camera), this.defaultScene.add(this.cameraTransformGroup), this.headDefault = new W9({
                diffuse: this.gl.assets.textures.head.diffuse,
                camera: this.camera
            }), this.defaultScene.add(this.headDefault.instance), this.helmet = new F9({
                scene: this.defaultScene,
                camera: this.camera
            }), document.addEventListener("disco", (Q) => {
                if (this.hoverAnimation(Q.detail.direction), Q.detail.direction == 1) this.isDisco = !0;
                else this.isDisco = !1
            }), this.lightingDefault = new N9, this.defaultScene.add(this.lightingDefault.instance), this.hover(), this.helmetHover(), this.responsive(), this.transition(), this.setScroll(), this.setIsRendering(), this.gl.isDebug) this.setOrbitControls(), this.setDebug()
    }
    setIsRendering() {
        TA.create({
            trigger: '[data-gl-track="head"]',
            start: () => `top-=${this.gl.sizes.height/2} bottom`,
            end: () => `bottom+=${this.gl.sizes.height/2} top`,
            invalidateOnRefresh: !0,
            refreshPriority: -99,
            onEnter: () => {
                this.isRendering = !0
            },
            onEnterBack: () => {
                this.isRendering = !0
            },
            onLeave: () => {
                this.isRendering = !1
            },
            onLeaveBack: () => {
                this.isRendering = !1
            }
        })
    }
    hover() {
        let A = document.querySelectorAll("[data-gl-hover]");
        if (A.length > 0) A.forEach((Q) => {
            Q.addEventListener("mouseenter", () => {
                this.animateColor(this.renderPlane.mesh.material.uniforms.uColorHover.value, new pA(Q.dataset.glHover).convertLinearToSRGB(), 0.25, (B) => {
                    this.renderPlane.mesh.material.uniforms.uColorHover.value = B
                }), m.to(this.renderPlane.mesh.material.uniforms.uHover, {
                    value: 1,
                    duration: 0.5,
                    ease: "power4.inOut"
                })
            }), Q.addEventListener("mouseleave", () => {
                m.to(this.renderPlane.mesh.material.uniforms.uHover, {
                    value: 0,
                    duration: 0.5,
                    ease: "power4.inOut"
                })
            })
        })
    }
    animateColor(A, Q, B = 0.25, E, I) {
        let C = {
                value: 0
            },
            K = new pA;
        m.fromTo(C, {
            value: 0
        }, {
            value: 1,
            ease: E,
            duration: B,
            onUpdate: () => {
                I(K.lerpColors(A, Q, C.value))
            }
        })
    }
    helmetHover() {
        this.helmetRevealValue = 1;
        let A = document.querySelectorAll('[data-gl-helmet="hover"]'),
            Q = null;
        if (A.length > 0) A.forEach((B) => {
            B.addEventListener("mouseenter", () => {
                if (this.isDisco) return;
                this.hoverAnimation(1, !0)
            }), B.addEventListener("mouseleave", () => {
                if (this.isDisco) return;
                this.hoverAnimation(0)
            })
        })
    }
    hoverAnimation(A, Q = !1) {
        if (A == 1) {
            if (m.to(this.renderPlane.mesh.material.uniforms.uHelmetHover, {
                    value: 1,
                    duration: 1.5,
                    ease: "expo.inOut",
                    overwrite: !0,
                    onUpdate: () => {
                        this.headDefault.uniforms.uHelmetHover.value = this.renderPlane.mesh.material.uniforms.uHelmetHover.value
                    }
                }), Q) m.to(this, {
                helmetRevealValue: 0,
                duration: 1.5,
                ease: "power1.inOut",
                overwrite: !0
            })
        } else m.to(this.renderPlane.mesh.material.uniforms.uHelmetHover, {
            value: 0,
            duration: 1,
            ease: "expo.inOut",
            overwrite: !0,
            onUpdate: () => {
                this.headDefault.uniforms.uHelmetHover.value = this.renderPlane.mesh.material.uniforms.uHelmetHover.value
            }
        }), m.to(this, {
            helmetRevealValue: 1,
            duration: 1,
            ease: "power1.inOut",
            overwrite: !0
        })
    }
    setDebug() {
        this.gl.world.helmetDebugFolder.add(this.renderPlane.mesh.material.uniforms.SHOW_HELMET_PERMANENTLY, "value").name("SHOW_HELMET_PERMANENTLY"), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.OUTLINE, "value").name("OUTLINE");
        let A = {
            value: this.renderPlane.mesh.material.uniforms.COLOR_OUTLINE.value.getHex()
        };
        this.gl.world.compositionDebugFolder.addColor(A, "value").name("COLOR OUTLINE").onChange((C) => {
            this.renderPlane.mesh.material.uniforms.COLOR_OUTLINE.value = new pA(C).convertLinearToSRGB()
        });
        let Q = {
            value: this.renderPlane.mesh.material.uniforms.COLOR_FOREGROUND.value.getHex()
        };
        this.gl.world.compositionDebugFolder.addColor(Q, "value").name("COLOR_FOREGROUND").onChange((C) => {
            this.renderPlane.mesh.material.uniforms.COLOR_FOREGROUND.value = new pA(C).convertLinearToSRGB()
        });
        let B = {
            value: this.renderPlane.mesh.material.uniforms.COLOR_BACKGROUND.value.getHex()
        };
        this.gl.world.compositionDebugFolder.addColor(B, "value").name("COLOR_BACKGROUND").onChange((C) => {
            this.renderPlane.mesh.material.uniforms.COLOR_BACKGROUND.value = new pA(C).convertLinearToSRGB()
        });
        let E = {
            value: this.renderPlane.mesh.material.uniforms.COLOR_CURSOR_FOREGROUND.value.getHex()
        };
        this.gl.world.compositionDebugFolder.addColor(E, "value").name("COLOR_CURSOR_FOREGROUND").onChange((C) => {
            this.renderPlane.mesh.material.uniforms.COLOR_CURSOR_FOREGROUND.value = new pA(C).convertLinearToSRGB()
        });
        let I = {
            value: this.renderPlane.mesh.material.uniforms.COLOR_CURSOR_BACKGROUND.value.getHex()
        };
        this.gl.world.compositionDebugFolder.addColor(I, "value").name("COLOR_CURSOR_BACKGROUND").onChange((C) => {
            this.renderPlane.mesh.material.uniforms.COLOR_CURSOR_BACKGROUND.value = new pA(C).convertLinearToSRGB()
        }), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.SCALE, "value").name("SCALE"), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.SPEED, "value").name("SPEED"), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.THICKNESS, "value").name("THICKNESS"), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.DISTORT_SCALE, "value").name("DISTORT_SCALE"), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.DISTORT_INTENSITY, "value").name("DISTORT_INTENSITY"), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.NOISE_DETAIL, "value").name("NOISE_DETAIL"), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.CURSOR_INTENSITY, "value").name("CURSOR_INTENSITY"), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.CURSOR_SCALE, "value").name("CURSOR_SCALE"), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.CURSOR_BOUNCE, "value").name("CURSOR_BOUNCE"), this.gl.world.compositionDebugFolder.add(this.renderPlane.mesh.material.uniforms.REVEAL_SIZE, "value").name("REVEAL_SIZE")
    }
    setOrbitControls() {
        this.controls = new XC(this.camera, this.gl.canvas), this.controls.enableDamping = !0, this.controls.enableZoom = !1
    }
    responsive() {
        if (this.gl.sizes.width > 768) this.camera.position.z = 3, this.headDefault.instance.position.y = 0, this.helmet.instance.position.y = 0.05, this.helmet.wireframeMesh.position.y = 0.05;
        else this.camera.position.z = 3.75, this.headDefault.instance.position.y = -0.05, this.helmet.instance.position.y = 0, this.helmet.wireframeMesh.position.y = 0
    }
    transition() {
        m.registerPlugin(TA), m.fromTo(this.cameraTransformGroup.position, {
            z: 0
        }, {
            z: -1,
            ease: "power1.inOut",
            scrollTrigger: {
                trigger: '[data-gl-track="head"]',
                scrub: !0,
                start: () => "top top",
                end: () => `top+=${this.gl.sizes.height} top`
            }
        }), m.fromTo(this.renderPlane.mesh.material.uniforms.uFilter, {
            value: 0
        }, {
            value: 1,
            duration: 0.25,
            ease: "power1.inOut",
            scrollTrigger: {
                trigger: '[data-gl-track="head"]',
                scrub: !0,
                start: () => "top top",
                end: () => `top+=${this.gl.sizes.height} top`
            }
        }), m.fromTo(this.cameraTransformGroup.position, {
            y: 0
        }, {
            y: 0.1,
            ease: "power1.inOut",
            scrollTrigger: {
                trigger: '[data-gl-track="head"]',
                scrub: !0,
                start: () => "bottom bottom",
                end: () => `bottom+=${this.gl.sizes.height} bottom`
            }
        }), m.fromTo(this.renderPlane.mesh.material.uniforms.uCursorIntensity, {
            value: 1
        }, {
            value: 0,
            duration: 0.25,
            ease: "power1.inOut",
            scrollTrigger: {
                trigger: '[data-gl-track="head"]',
                toggleActions: "play none none reverse",
                start: () => `top+=${this.gl.sizes.height/2.25} top`,
                end: () => `top+=${this.gl.sizes.height/2.25} top`
            }
        }), m.fromTo(this.helmet.wireframeMeshMaterial.uniforms.uOpacity, {
            value: 1
        }, {
            value: 0,
            duration: 0.25,
            ease: "power1.inOut",
            scrollTrigger: {
                trigger: '[data-gl-track="head"]',
                toggleActions: "play none none reverse",
                start: () => `top+=${this.gl.sizes.height/2.25} top`,
                end: () => `top+=${this.gl.sizes.height/2.25} top`
            }
        }), m.fromTo(this.renderPlane.bounds, {
            width: () => this.gl.sizes.width,
            height: () => this.gl.sizes.height
        }, {
            width: () => this.target.offsetWidth,
            height: () => this.target.offsetHeight,
            ease: "power1.inOut",
            scrollTrigger: {
                trigger: '[data-gl-track="head"]',
                scrub: !0,
                invalidateOnRefresh: !0,
                start: () => "top top",
                end: () => `top+=${this.gl.sizes.height} top`,
                onUpdate: (A) => {
                    this.scrollProgress = A.progress, this.setScenePlaneDimensions()
                }
            }
        })
    }
    resize() {
        this.defaultRenderTarget.setSize(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio), this.headDefault.resize(), this.responsive(), this.setScenePlaneDimensions()
    }
    setScenePlaneDimensions() {
        this.renderPlane.mesh.position.set(this.renderPlane.bounds.left - this.gl.sizes.width / 2 + this.gl.sizes.width / 2, -this.renderPlane.bounds.top + this.gl.sizes.height / 2 - this.gl.sizes.height / 2, 0), this.renderPlane.mesh.scale.set(this.renderPlane.bounds.width, this.renderPlane.bounds.height), this.camera.aspect = this.renderPlane.bounds.width / this.renderPlane.bounds.height, this.camera.updateProjectionMatrix()
    }
    setScroll() {
        m.fromTo(this.renderPlane.bounds, {
            top: () => this.gl.sizes.height
        }, {
            top: 0,
            ease: "none",
            scrollTrigger: {
                invalidateOnRefresh: !0,
                scrub: !0,
                trigger: '[data-gl-track="head"]',
                start: () => `top-=${this.gl.sizes.height} top`,
                end: () => "top top",
                onRefresh: () => {
                    this.setScenePlaneDimensions()
                },
                refreshPriority: -99
            },
            onUpdate: (A) => {
                this.setScenePlaneDimensions()
            }
        }), m.fromTo(this.renderPlane.bounds, {
            top: 0
        }, {
            top: () => -this.gl.sizes.height,
            ease: "none",
            scrollTrigger: {
                invalidateOnRefresh: !0,
                scrub: !0,
                trigger: '[data-gl-track="head"]',
                start: () => "bottom bottom",
                end: () => `bottom+=${this.gl.sizes.height} bottom`,
                onRefresh: () => {
                    this.setScenePlaneDimensions()
                },
                refreshPriority: -99
            },
            onUpdate: (A) => {
                this.setScenePlaneDimensions()
            }
        })
    }
    renderPipeline() {
        if (!this.isRendering) return;
        this.gl.renderer.instance.setRenderTarget(this.defaultRenderTarget), this.gl.renderer.instance.render(this.defaultScene, this.camera), this.renderPlane.mesh.material.uniforms.tDefaultDiffuse.value = this.defaultRenderTarget.texture
    }
    update() {
        if (!this.isRendering) return;
        if (this.headDefault.update(this.helmetRevealValue, this.scrollProgress), this.helmet.update(), this.easedMouse.normalized.update(this.gl.time.delta), this.easedMouse.pace.update(this.gl.time.delta), this.helmet.instance.rotation.copy(this.headDefault.instance.rotation), this.helmet.instance.rotation.y = this.helmet.instance.rotation.y / 1.5, this.helmet.instance.rotation.x = this.helmet.instance.rotation.x / 1.5, this.helmet.instance.rotation.x += Math.PI * 0.06, !this.gl.isDebug) {
            if (this.camera.position.x = this.easedMouse.normalized.value.x * 0.02 * this.helmetRevealValue, this.gl.sizes.width > 768) this.camera.position.y = this.easedMouse.normalized.value.y * -1 * 0.02 * this.helmetRevealValue * (1 - this.scrollProgress)
        }
        this.renderPlane.mesh.material.uniforms.uTime.value = this.gl.time.elapsed, this.renderPlane.mesh.material.uniforms.uReveal.value = window.landoGL.reveal, this.renderPlane.mesh.material.uniforms.tHelmet.value = this.helmet.renderTarget.texture, this.renderPlane.mesh.material.uniforms.tCursorEffect.value = this.gl.world.fluidCursor.sourceTarget.texture, this.renderPlane.mesh.material.uniforms.tBackgroundNoise.value = this.gl.world.backgroundNoise.renderTarget.texture
    }
}
var t5 = VA(() => {
    VQ();
    LB();
    XU();
    _B();
    c5();
    a5();
    r5()
});

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L32502-L33061 =====
// tracks scene (32502-33061): 3D circuits, bloom, DOM markers, sprint labels
class V9 {
    constructor(A) {
        this.object = new qB, this.dom = A.dom, this.htmlTarget = document.querySelector(A.htmlTarget), this.index = A.index, this.type = A.type, this.addHTML()
    }
    addHTML() {
        let A = document.createElement("div");
        A.classList.add("lando-gl__point");
        let Q = document.createElement("div");
        Q.classList.add("lando-gl__point__content"), A.appendChild(Q), this.htmlTarget.appendChild(A), this.dom = A
    }
}
var JN = VA(() => {
    VQ()
});
class z9 {
    constructor(A) {
        if (this.options = {
                camera: A.camera,
                scene: A.scene
            }, this.gl = new RQ, this.instance = new TB, this.height = 0.1, this.tracksPositions = {}, this.matchVertices(), this.data = [], this.generateData(), this.current = this.getTrackIndex(window.landoGL.params.tracksScene.CURRENT), this.previousCurrent = window.landoGL.params.tracksScene.CURRENT, this.isTransitioning = !1, this.params = {
                progress: 0,
                duration: 1
            }, this.pointsGroup = new TB, this.instance.add(this.pointsGroup), this.points = [], this.createPoints(), this.setPoints(this.current), this.addTracks(), this.gl.isDebug) this.setDebug()
    }
    generateData() {
        this.gl.assets.models.tracks.scene.children.forEach((A, Q) => {
            let B = A.children.find((E) => E.userData.group == "points");
            this.data.push({
                name: A.name,
                index: Q,
                meshes: this.generateTrack(A),
                points: B
            })
        })
    }
    matchVertices() {
        for (let A in this.gl.assets.models.tracks.scene.children) {
            let Q = this.gl.assets.models.tracks.scene.children[A].name,
                B = this.gl.assets.models.tracks.scene.children[A].children.find((E) => E.userData.group == "mesh");
            this.tracksPositions[Q] = [], this.tracksPositions[Q] = new Float32Array(B.geometry.attributes.position.count * 3);
            for (let E = 0; E < B.geometry.attributes.position.count; E++) {
                let I = E * 3;
                if (E < B.geometry.attributes.position.count - 1) this.tracksPositions[Q][I] = B.geometry.attributes.position.array[I], this.tracksPositions[Q][I + 1] = B.geometry.attributes.position.array[I + 1], this.tracksPositions[Q][I + 2] = B.geometry.attributes.position.array[I + 2];
                else this.tracksPositions[Q][I] = B.geometry.attributes.position.array[0], this.tracksPositions[Q][I + 1] = B.geometry.attributes.position.array[1], this.tracksPositions[Q][I + 2] = B.geometry.attributes.position.array[2]
            }
        }
    }
    generateTrack(A) {
        let Q = new fQ({
                fragmentShader: GN,
                vertexShader: UN,
                side: hB,
                uniforms: {
                    uThickness: new YA(0),
                    uTextureMatcap: new YA(this.gl.assets.textures.matcaps.track),
                    uTime: new YA(0),
                    uProgress: new YA(0),
                    uReverse: new YA(1),
                    uRaceDirection: A.userData.raceDirection == "left" ? new YA(1) : new YA(-1)
                },
                transparent: !0,
                visible: !1
            }),
            B = new fQ({
                vertexShader: UN,
                fragmentShader: GN,
                side: hB,
                uniforms: {
                    uThickness: new YA(0),
                    uTextureMatcap: new YA(this.gl.assets.textures.matcaps.track),
                    uTime: new YA(0),
                    uProgress: new YA(0),
                    uReverse: new YA(1),
                    uRaceDirection: A.userData.raceDirection == "left" ? new YA(1) : new YA(-1)
                },
                transparent: !0,
                visible: !1
            }),
            E = this.gl.assets.models.tracks.scene.getObjectByName(A.name).children.find((K) => K.userData.group == "mesh"),
            I = new QB(1, 1, E.geometry.attributes.position.count - 1, 1),
            C = I.getAttribute("position");
        for (let K = 0; K < E.geometry.attributes.position.count; K++) {
            let J = K * 3;
            C.setXYZ(K, this.tracksPositions[A.name][J], 0, this.tracksPositions[A.name][J + 2]), C.setXYZ(E.geometry.attributes.position.count + K, this.tracksPositions[A.name][J], this.height, this.tracksPositions[A.name][J + 2])
        }
        if (I.computeVertexNormals(!0), this.inner = new OQ(I, Q), this.outer = new OQ(I, B), A.userData.side == "inner") this.outer.material.uniforms.uThickness.value = 0.035;
        else if (A.userData.side == "outer") this.outer.material.uniforms.uThickness.value = -0.035;
        else if (A.userData.side == "center") this.inner.material.uniforms.uThickness.value = -0.0175, this.outer.material.uniforms.uThickness.value = 0.0175;
        return [this.inner, this.outer]
    }
    addTracks() {
        for (let A in this.data) this.data[A].meshes.forEach((Q) => {
            if (this.instance.add(Q), this.current == A) Q.material.visible = !0
        })
    }
    createPoints() {
        let A = 0;
        this.data.forEach((Q, B) => {
            if (Q.points.children.length > A) A = Q.points.children.length
        });
        for (let Q = 0; Q < A; Q++) this.points.push(new V9({
            htmlTarget: '[data-gl="tracks"]'
        }));
        this.points.forEach((Q) => {
            this.pointsGroup.add(Q.object)
        })
    }
    setPoints(A) {
        let Q = 1,
            B = 1;
        this.points.forEach((E, I) => {
            if (this.data[A].points.children[I]) {
                if (E.object.position.copy(new o(this.data[A].points.children[I].position.x, this.height, this.data[A].points.children[I].position.z)), this.data[A].points.children[I].userData.type == "start") {
                    E.dom.innerHTML = `
            <div class="lando-gl__point__content lando-gl__point__start">
              <svg width="24" height="14" viewBox="0 0 24 14" fill="none" xmlns="http://www.w3.org/2000/svg">
                <g clip-path="url(#clip0_125_13388)">
                  <path d="M4.66627 9.33276H0V13.999H4.66627V9.33276Z" fill="white" />
                  <path d="M4.66627 4.6665H0V9.33277H4.66627V4.6665Z" fill="#111718" />
                  <path d="M9.28346 9.33276H4.61719V13.999H9.28346V9.33276Z" fill="#111718" />
                  <path d="M4.66627 0.000244141H0V4.66651H4.66627V0.000244141Z" fill="white" />
                  <path d="M9.28346 4.6665H4.61719V9.33277H9.28346V4.6665Z" fill="white" />
                  <path d="M9.28346 0.000244141H4.61719V4.66651H9.28346V0.000244141Z" fill="#111718" />
                </g>
                <path d="M13.9495 9.33276H9.2832V13.999H13.9495V9.33276Z" fill="white" />
                <path d="M13.9495 4.6665H9.2832V9.33277H13.9495V4.6665Z" fill="#111718" />
                <path d="M13.9495 0.000244141H9.2832V4.66651H13.9495V0.000244141Z" fill="white" />
                <g clip-path="url(#clip1_125_13388)">
                  <path d="M13.9495 9.33276H9.2832V13.999H13.9495V9.33276Z" fill="white" />
                  <path d="M13.9495 4.6665H9.2832V9.33277H13.9495V4.6665Z" fill="#111718" />
                  <path d="M18.5667 9.33276H13.9004V13.999H18.5667V9.33276Z" fill="#111718" />
                  <path d="M13.9495 0.000244141H9.2832V4.66651H13.9495V0.000244141Z" fill="white" />
                  <path d="M18.5667 4.6665H13.9004V9.33277H18.5667V4.6665Z" fill="white" />
                  <path d="M18.5667 0.000244141H13.9004V4.66651H18.5667V0.000244141Z" fill="#111718" />
                </g>
                <path d="M23.2327 9.33276H18.5664V13.999H23.2327V9.33276Z" fill="white" />
                <path d="M23.2327 4.6665H18.5664V9.33277H23.2327V4.6665Z" fill="#111718" />
                <path d="M23.2327 0.000244141H18.5664V4.66651H23.2327V0.000244141Z" fill="white" />
                <defs>
                  <clipPath id="clip0_125_13388">
                    <rect width="9.28352" height="14" fill="white" transform="translate(0 0.000244141)" />
                  </clipPath>
                  <clipPath id="clip1_125_13388">
                    <rect width="9.28352" height="14" fill="white" transform="translate(9.2832 0.000244141)" />
                  </clipPath>
                </defs>
              </svg>
            </div>
          `;
                    let C = E.dom.querySelector("svg");
                    C.style.height = "auto"
                } else if (this.data[A].points.children[I].userData.type == "corner") E.dom.innerHTML = `
            <div class="lando-gl__point__content">
              <p class="lando-gl__point__text--top">${Q.toLocaleString("en-US",{minimumIntegerDigits:2,useGrouping:!1})}</p>
              <p class="lando-gl__point__text--bottom"></p>
            </div>
          `, Q++;
                else if (this.data[A].points.children[I].userData.type == "drs") E.dom.innerHTML = `
            <div class="lando-gl__point__content lando-gl__point__content--drs">
              <p class="lando-gl__point__text--top">DRS</p>
              <p class="lando-gl__point__text--bottom">${B.toLocaleString("en-US",{minimumIntegerDigits:2,useGrouping:!1})}</p>
            </div>
          `, B++;
                E.dom.style.visibility = "visible"
            } else E.dom.style.visibility = "hidden"
        })
    }
    getTrackIndex(A) {
        return this.data.findIndex((Q) => Q.name == A)
    }
    translate(A, Q) {
        return new Promise(async (B) => {
            if (this.isTransitioning) return;
            document.documentElement.classList.add("gl-tracks-isTransitioning"), await this.translateOut(A, Q), await this.translateIn(A, Q), document.documentElement.classList.remove("gl-tracks-isTransitioning"), B()
        })
    }
    translateOut(A, Q) {
        return new Promise((B) => {
            let E = m.timeline({
                onStart: () => {
                    this.isTransitioning = !0
                },
                onComplete: () => {
                    this.data[this.current].meshes.forEach((I) => {
                        I.material.uniforms.uReverse.value = 1, I.material.visible = !1
                    }), this.data[A].meshes.forEach((I) => {
                        I.material.visible = !0
                    }), this.current = A, this.setPoints(this.current), B()
                }
            });
            E.fromTo(this.params, {
                progress: 0
            }, {
                progress: 1,
                duration: window.landoGL.params.tracksScene.TRANSITION_DURATION / 2,
                ease: "expo.in",
                onUpdate: () => {
                    this.data[this.current].meshes.forEach((I) => {
                        I.material.uniforms.uProgress.value = this.params.progress
                    })
                },
                onComplete: () => {}
            }, 0), this.data[this.current].meshes.forEach((I) => {
                E.fromTo(I.rotation, {
                    y: 0
                }, {
                    y: Q == "forward" ? -Math.PI / 4 : Math.PI / 4,
                    duration: window.landoGL.params.tracksScene.TRANSITION_DURATION / 2,
                    ease: "expo.in"
                }, 0), E.fromTo(I.rotation, {
                    y: 0
                }, {
                    y: Q == "forward" ? -Math.PI / 4 : Math.PI / 4,
                    duration: window.landoGL.params.tracksScene.TRANSITION_DURATION / 2,
                    ease: "expo.in"
                }, 0)
            }), E.fromTo(this.pointsGroup.rotation, {
                y: 0
            }, {
                y: Q == "forward" ? -Math.PI / 4 : Math.PI / 4,
                duration: window.landoGL.params.tracksScene.TRANSITION_DURATION / 2,
                ease: "expo.in"
            }, 0), this.points.forEach((I, C) => {
                E.fromTo(I.dom.querySelector(".lando-gl__point__content"), {
                    yPercent: 0,
                    opacity: 1
                }, {
                    yPercent: -25,
                    opacity: 0,
                    duration: window.landoGL.params.tracksScene.TRANSITION_DURATION / 4,
                    delay: C * 0.015,
                    ease: "expo.in"
                }, 0)
            })
        })
    }
    translateIn(A, Q) {
        return new Promise((B) => {
            let E = m.timeline({
                onComplete: () => {
                    this.isTransitioning = !1, B()
                }
            });
            E.fromTo(this.params, {
                progress: 1
            }, {
                progress: 0,
                duration: window.landoGL.params.tracksScene.TRANSITION_DURATION / 2,
                ease: "expo.out",
                onUpdate: () => {
                    this.data[A].meshes.forEach((I) => {
                        I.material.uniforms.uProgress.value = this.params.progress
                    })
                },
                onComplete: () => {
                    this.data[A].meshes.forEach((I) => {
                        I.material.uniforms.uReverse.value = 0
                    })
                }
            }, 0), this.data[A].meshes.forEach((I) => {
                E.fromTo(I.rotation, {
                    y: Q == "forward" ? Math.PI / 4 : -Math.PI / 4
                }, {
                    y: 0,
                    duration: window.landoGL.params.tracksScene.TRANSITION_DURATION / 2,
                    ease: "expo.out"
                }, 0), E.fromTo(I.rotation, {
                    y: Q == "forward" ? Math.PI / 4 : -Math.PI / 4
                }, {
                    y: 0,
                    duration: window.landoGL.params.tracksScene.TRANSITION_DURATION / 2,
                    ease: "expo.out"
                }, 0)
            }), E.fromTo(this.pointsGroup.rotation, {
                y: Q == "forward" ? Math.PI / 4 : -Math.PI / 4
            }, {
                y: 0,
                duration: window.landoGL.params.tracksScene.TRANSITION_DURATION / 2,
                ease: "expo.out"
            }, 0), this.points.forEach((I, C) => {
                E.fromTo(I.dom.querySelector(".lando-gl__point__content"), {
                    yPercent: -25,
                    opacity: 0
                }, {
                    yPercent: 0,
                    opacity: 1,
                    duration: window.landoGL.params.tracksScene.TRANSITION_DURATION / 4,
                    delay: C * 0.015,
                    ease: "expo.out"
                }, 0)
            })
        })
    }
    nextTrack() {
        let A = null;
        if (this.current + 1 > this.data.length - 1) A = 0;
        else A = this.current + 1;
        this.translate(A, "forward").then(() => {
            if (this.gl.isDebug) console.log(this.data[A])
        })
    }
    prevTrack() {
        let A = null;
        if (this.current - 1 < 0) A = this.data.length - 1;
        else A = this.current - 1;
        this.translate(A, "backward").then(() => {
            if (this.gl.isDebug) console.log(this.data[A])
        })
    }
    setDebug() {
        this.gl.world.tracksSceneDebugFolder.add(this, "nextTrack").name("Next Track"), this.gl.world.tracksSceneDebugFolder.add(this, "prevTrack").name("Prev Track");
        let A = {
            value: {}
        };
        this.gl.world.tracksSceneDebugFolder.add(A, "value", this.data.map((Q) => Q.name)).name("Select track").onChange((Q) => {
            window.landoGL.params.tracksScene.CURRENT = Q
        })
    }
    fitObjectToViewport(A, Q) {
        let B = new f0;
        new V0().setFromObject(A).getBoundingSphere(B);
        let E = Q.position.distanceTo(B.center),
            I = Q.fov * (Math.PI / 180),
            C = 2 * Math.tan(I / 2) * E,
            K = C * (this.gl.sizes.width / this.gl.sizes.height),
            J = Math.min(K, C) / (2 * B.radius);
        A.scale.setScalar(J)
    }
    resize() {}
    update(A) {
        if (this.data[this.current].meshes.forEach((Q) => {
                Q.material.uniforms.uTime.value = this.gl.time.elapsed
            }), window.landoGL.params.tracksScene.CURRENT !== this.previousCurrent) {
            let Q = this.getTrackIndex(window.landoGL.params.tracksScene.CURRENT);
            this.translate(Q, "forward")
        }
        this.previousCurrent = window.landoGL.params.tracksScene.CURRENT, this.points.forEach((Q, B) => {
            if (this.data[this.current].points.children[B]) this.points.forEach((E, I) => {
                let C = new o;
                E.object.getWorldPosition(C), C.project(this.options.camera);
                let K = C.x * A.width * 0.5,
                    J = -C.y * A.height * 0.5;
                E.dom.style.transform = `translateX(${K}px) translateY(${J}px)`
            })
        })
    }
}
var UN = `
  varying vec2 vUv;
  varying vec2 vN;

  attribute vec3 aTargetPositions;
  // uniform float uProgress;
  uniform float uThickness;

  void main()
  {

    // vec3 mixed = mix(position + normal * uThickness, aTargetPositions + normal * uThickness, uProgress);

    vec4 p = vec4( position + normal * uThickness, 1. );

    vec3 e = normalize( vec3( modelViewMatrix * p ) );
    vec3 n = normalize( normalMatrix * normal );

    vec3 r = reflect( e, n );
    float m = 2. * sqrt(
      pow( r.x, 2. ) +
      pow( r.y, 2. ) +
      pow( r.z + 1., 2. )
    );
    vN = r.xy / m + .5;

    gl_Position = projectionMatrix * modelViewMatrix * p;

    // gl_Position = projectionMatrix * modelViewMatrix * vec4(position + normal * uThickness, 1.0);

    vUv = uv;
  }
`,
    GN = `
  uniform sampler2D uTextureMatcap;
  uniform float uTime;
  uniform float uProgress;
  uniform float uReverse;
  uniform float uRaceDirection;

  varying vec2 vUv;
  varying vec2 vN;

  void main() {
    // Color 
    vec3 cYellow = vec3(0.824, 1.0, 0.0);
    vec3 cBackground = vec3(0.09, 0.098, 0.063);

    // Animation
    float animation = 0.5 + tan(vUv.x * 10.0 - uTime * uRaceDirection * 2.) * 0.5;
    // float animation = fract(vUv.x * 10.0 - uTime);

    float animationFract = pow(fract(vUv.x * 5.0 - uTime * uRaceDirection), 2.) * 10. - 5.0;

    // Outline
    // vec3 upperOutline = vec3(step(vUv.y, 0.05));
    vec3 upperOutline = vec3(smoothstep(0.075, 0.025, vUv.y));
    upperOutline -= animationFract * upperOutline * 0.3;

    vec3 bottomOutline = vec3(step(0.975, vUv.y));
    // bottomOutline += animation * bottomOutline;
    bottomOutline *= 0.25;

    // Transition
    float transition = smoothstep(uProgress * 1.2, uProgress * 1.2 - 0.2, abs(uReverse - fract(vUv.x * 8.)));
    
    // float transition = animation;

    // Matcap
    vec3 matcap = texture2D( uTextureMatcap, vN ).rgb;

    // Alpha
    float alpha = 0.35;
    alpha += upperOutline.r;
    alpha += bottomOutline.r;
    alpha = clamp(alpha, 0.0, 1.0);
    alpha -= transition;

    // Color
    vec3 color = vec3(0.1, 0.15, 0.05);
    color += matcap;
    color += cYellow * upperOutline;
    color += cYellow * bottomOutline;

    gl_FragColor = vec4(color, alpha );
    // gl_FragColor = vec4(vec3(animation), 1.0);
    // gl_FragColor = vec4(vec3(animationFract), 1.0);
    // gl_FragColor = vec4(vUv.x, vUv.y, 0.0, 1.0);
  }
`;
var ZN = VA(() => {
    VQ();
    LB();
    _B();
    JN()
});
class k9 {
    constructor(A) {
        this.id = "tracks", this.settings = A, this.isRendering = !1, this.gl = new RQ, this.easedMouse = {
            normalized: this.gl.mouse.createEasedNormalized(0.05)
        }, this.scene = new rB, this.scene.background = new pA(1184274).convertLinearToSRGB(), this.renderPlane = {
            mesh: new OQ(new QB(1, 1), new fQ({
                vertexShader: `
            varying vec2 vUv;

            void main() {
              gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
            
              vUv = uv;
            }
          `,
                fragmentShader: `
            varying vec2 vUv;

            uniform sampler2D tDiffuse;

            void main() {
              vec4 textureDiffuse = texture2D(tDiffuse, vUv);

              gl_FragColor = textureDiffuse;
              // gl_FragColor.rgb += vec3(1.0, 0.0, 0.0);

              // #include <tonemapping_fragment>
              // #include <colorspace_fragment>
            }
          `,
                transparent: !0,
                uniforms: {
                    tDiffuse: new YA(null)
                }
            })),
            bounds: {
                top: 0,
                left: 0,
                width: this.gl.sizes.width,
                height: this.gl.sizes.height
            }
        }, this.renderTarget = new XB(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio, {
            samples: 2
        }), this.camera = new RB(1, this.gl.sizes.width / this.gl.sizes.height, 0.1, 1000), this.camera.position.x = 87.5, this.camera.position.y = 70, this.camera.position.z = 175, this.tracks = new z9({
            camera: this.camera
        }), this.scene.add(this.tracks.instance), this.renderPass = new UY(this.scene, this.camera), this.bloomPass = new CK(new kA(this.gl.sizes.width, this.gl.sizes.height), 1.5, 0.5, 0.25), this.composer = new JY(this.gl.renderer.instance, this.renderTarget), this.composer.setSize(this.gl.sizes.width, this.gl.sizes.height), this.composer.setPixelRatio(this.gl.sizes.pixelRatio), this.composer.addPass(this.renderPass), this.composer.addPass(this.bloomPass), this.composer.renderToScreen = !1, this.setOrbitControls(), this.setIsRendering(), this.getBounds(), this.setScroll()
    }
    setOrbitControls() {
        this.controls = new XC(this.camera, this.settings.dom), this.controls.enableDamping = !0, this.controls.enableZoom = !1, this.controls.minPolarAngle = Math.PI / 2.5, this.controls.maxPolarAngle = Math.PI / 2.5, this.controls.autoRotate = !0, this.controls.autoRotateSpeed = window.landoGL.params.tracksScene.AUTOROTATE_SPEED
    }
    getBounds() {
        let A = this.settings.dom.getBoundingClientRect();
        this.renderPlane.bounds.left = A.left, this.renderPlane.bounds.width = A.width, this.renderPlane.bounds.height = A.height
    }
    setScroll() {
        m.fromTo(this.renderPlane.bounds, {
            top: () => this.gl.sizes.height
        }, {
            top: () => -this.gl.sizes.height,
            ease: "none",
            scrollTrigger: {
                invalidateOnRefresh: !0,
                scrub: !0,
                trigger: this.settings.dom,
                start: () => `center-=${this.gl.sizes.height} top+=${this.gl.sizes.height/2}`,
                end: () => `center+=${this.gl.sizes.height} top+=${this.gl.sizes.height/2}`,
                refreshPriority: -99,
                onRefresh: () => {
                    this.getBounds(), this.setScenePlaneDimensions()
                }
            },
            onUpdate: (A) => {
                this.setScenePlaneDimensions()
            }
        })
    }
    setIsRendering() {
        TA.create({
            trigger: this.settings.dom,
            start: () => `top-=${this.gl.sizes.height/2} bottom`,
            end: () => `bottom+=${this.gl.sizes.height/2} top`,
            invalidateOnRefresh: !0,
            refreshPriority: -99,
            onEnter: () => {
                this.isRendering = !0
            },
            onEnterBack: () => {
                this.isRendering = !0
            },
            onLeave: () => {
                this.isRendering = !1
            },
            onLeaveBack: () => {
                this.isRendering = !1
            }
        })
    }
    resize() {
        this.renderTarget.setSize(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio), this.composer.setPixelRatio(this.gl.sizes.pixelRatio), this.composer.setSize(this.gl.sizes.width, this.gl.sizes.height), this.tracks.resize(), this.getBounds(), this.setScenePlaneDimensions()
    }
    setScenePlaneDimensions() {
        this.renderPlane.mesh.position.set(this.renderPlane.bounds.left - this.gl.sizes.width / 2 + this.renderPlane.bounds.width / 2, -this.renderPlane.bounds.top, 0), this.renderPlane.mesh.scale.set(this.renderPlane.bounds.width, this.renderPlane.bounds.height);
        let A = this.renderPlane.bounds.width / this.renderPlane.bounds.height,
            Q = 1 * (Math.PI / 180),
            B = 2 * Math.atan(Math.tan(Q / 2) / A) * (180 / Math.PI);
        this.camera.fov = B, this.camera.aspect = A, this.camera.updateProjectionMatrix()
    }
    renderPipeline() {
        if (!this.isRendering) return;
        this.composer.render(), this.renderPlane.mesh.material.uniforms.tDiffuse.value = this.composer.readBuffer.texture
    }
    update() {
        if (!this.isRendering) return;
        if (this.renderPlane.bounds) this.tracks.update(this.renderPlane.bounds);
        this.controls.update()
    }
}

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L33062-L33178 =====
// background scene (33072-33174) + HN/qN init wrappers
var HN = VA(() => {
    LB();
    VQ();
    XU();
    BN();
    EN();
    KN();
    ZN();
    _B()
});
class w9 {
    constructor() {
        this.id = "background", this.isRendering = !0, this.gl = new RQ, this.previous = {
            backgroundColor: window.landoGL.params.backgroundScene.COLOR_BACKGROUND,
            foregroundColor: window.landoGL.params.backgroundScene.COLOR_FOREGROUND
        }, this.renderPlane = {
            mesh: new OQ(new QB(1, 1), new fQ({
                transparent: !0,
                uniforms: {
                    tBackgroundNoise: new YA(null),
                    tDiffuse: new YA(null),
                    uReveal: new YA(window.landoGL.reveal),
                    OUTLINE: new YA(window.landoGL.params.backgroundScene.OUTLINE),
                    THICKNESS: new YA(window.landoGL.params.backgroundScene.THICKNESS),
                    COLOR_BACKGROUND: new YA(new pA(window.landoGL.params.backgroundScene.COLOR_BACKGROUND).convertLinearToSRGB()),
                    COLOR_FOREGROUND: new YA(new pA(window.landoGL.params.backgroundScene.COLOR_FOREGROUND).convertLinearToSRGB())
                },
                vertexShader: `
            varying vec2 vUv;

            void main() {
              gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);

              vUv = uv;
            }
          `,
                fragmentShader: `


            varying vec2 vUv;

            uniform float uReveal;

            uniform sampler2D tBackgroundNoise;

            uniform bool OUTLINE;
            uniform float THICKNESS;
            uniform vec3 COLOR_BACKGROUND;
            uniform vec3 COLOR_FOREGROUND;
            
            void main() {

              vec4 textureBackgroundNoise = texture2D(tBackgroundNoise, vUv);

              /* 
                Outline
              */
              float noiseBase = textureBackgroundNoise.r;
            
              vec3 background = mix(
                COLOR_BACKGROUND, 
                mix(COLOR_BACKGROUND, COLOR_FOREGROUND, uReveal),
                noiseBase
              );
            
              if (OUTLINE) {
                float edge = 0.0;
            
                // Offsets for neighboring pixels
                vec4 sampledRight = texture2D(tBackgroundNoise, vUv + vec2(THICKNESS, 0.0));
                vec4 sampledLeft = texture2D(tBackgroundNoise, vUv + vec2(-THICKNESS, 0.0));
                vec4 sampledUp = texture2D(tBackgroundNoise, vUv + vec2(0.0, THICKNESS));
                vec4 sampledDown = texture2D(tBackgroundNoise, vUv + vec2(0.0, -THICKNESS));
                
                if (sampledRight.r != textureBackgroundNoise.r ||
                    sampledLeft.r != textureBackgroundNoise.r ||
                    sampledUp.r != textureBackgroundNoise.r ||
                    sampledDown.r != textureBackgroundNoise.r) {
                  edge = 1.0;
                }
            
                background = mix(
                  COLOR_BACKGROUND, 
                  mix(COLOR_BACKGROUND, COLOR_FOREGROUND, uReveal),
                  edge 
                );
              }

              gl_FragColor = vec4(background, 1.0);
            }
          `
            })),
            bounds: {
                top: 0,
                left: 0,
                width: this.gl.sizes.width,
                height: this.gl.sizes.height
            }
        }, this.setScenePlaneDimensions()
    }
    resize() {
        this.setScenePlaneDimensions()
    }
    setScenePlaneDimensions() {
        this.renderPlane.mesh.position.set(this.renderPlane.bounds.left - this.gl.sizes.width / 2 + this.gl.sizes.width / 2, -this.renderPlane.bounds.top + this.gl.sizes.height / 2 - this.gl.sizes.height / 2, 0), this.renderPlane.mesh.scale.set(this.gl.sizes.width, this.gl.sizes.height)
    }
    renderPipeline() {}
    update() {
        if (this.renderPlane.mesh.material.uniforms.tBackgroundNoise.value = this.gl.world.backgroundNoise.renderTarget.texture, window.landoGL.params.backgroundScene.COLOR_BACKGROUND !== this.previous.backgroundColor) this.renderPlane.mesh.material.uniforms.COLOR_BACKGROUND.value = new pA(window.landoGL.params.backgroundScene.COLOR_BACKGROUND).convertLinearToSRGB();
        if (window.landoGL.params.backgroundScene.COLOR_FOREGROUND !== this.previous.foregroundColor) this.renderPlane.mesh.material.uniforms.COLOR_FOREGROUND.value = new pA(window.landoGL.params.backgroundScene.COLOR_FOREGROUND).convertLinearToSRGB();
        this.previous.backgroundColor = window.landoGL.params.backgroundScene.COLOR_BACKGROUND, this.previous.foregroundColor = window.landoGL.params.backgroundScene.COLOR_FOREGROUND
    }
}
var qN = VA(() => {
    VQ();
    _B()
});

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L33997-L34177 =====
// carousel scene (34002-34177) + sN init wrapper
var sN = VA(() => {
    uN();
    HY();
    HY()
});
class _9 {
    constructor() {
        if (this.id = "carousel", this.gl = new RQ, this.isRendering = !1, this.scene = new rB, this.renderPlane = {
                mesh: new OQ(new QB(1, 1), new fQ({
                    transparent: !0,
                    uniforms: {
                        tDiffuse: new YA(null)
                    },
                    vertexShader: `
            varying vec2 vUv;

            void main() {
              gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);

              vUv = uv;
            }
          `,
                    fragmentShader: `
            varying vec2 vUv;

            uniform sampler2D tDiffuse;
            
            void main() {
              vec4 textureDiffuse = texture2D(tDiffuse, vUv);

              gl_FragColor = vec4(textureDiffuse);
            }
          `
                })),
                bounds: {
                    top: 0,
                    topMobile: 0,
                    left: 0,
                    width: this.gl.sizes.width,
                    height: this.gl.sizes.height
                }
            }, this.renderPlane.mesh.renderOrder = 2, this.renderTarget = new XB(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio, {
                samples: 1
            }), this.camera = new RB(75, this.gl.sizes.width / this.gl.sizes.height, 0.1, 1000), this.camera.position.z = 4.5, this.textTop = this.createCarouselText({
                color: new pA(window.landoGL.params.carouselScene.COLOR_TOP).convertLinearToSRGB(),
                size: 0.0115,
                shrink: 1,
                verticalGap: 0,
                letterSpacing: -2,
                side: "top",
                direction: "left",
                yShift: 0.175,
                font: "brier",
                text: window.landoGL.params.carouselScene.TEXT_TOP + " "
            }), this.scene.add(this.textTop.mesh), this.textBottom = this.createCarouselText({
                color: new pA(window.landoGL.params.carouselScene.COLOR_BOTTOM).convertLinearToSRGB(),
                size: 0.02,
                shrink: 0.9,
                verticalGap: -0.175,
                letterSpacing: -1,
                side: "bottom",
                direction: "right",
                yShift: 0.175,
                font: "mona",
                text: window.landoGL.params.carouselScene.TEXT_BOTTOM + " "
            }), this.scene.add(this.textBottom.mesh), this.setScroll(), this.setIsRendering(), this.responsive(), this.gl.isDebug) this.setOrbitControls()
    }
    setIsRendering() {
        TA.create({
            trigger: '[data-gl-track="head"]',
            start: () => `top-=${this.gl.sizes.height/2} bottom`,
            end: () => `bottom+=${this.gl.sizes.height/2} top`,
            invalidateOnRefresh: !0,
            refreshPriority: -99,
            onEnter: () => {
                this.isRendering = !0
            },
            onEnterBack: () => {
                this.isRendering = !0
            },
            onLeave: () => {
                this.isRendering = !1
            },
            onLeaveBack: () => {
                this.isRendering = !1
            }
        })
    }
    createCarouselText(A) {
        let Q = new MU({
                text: A.text.toUpperCase(),
                font: A.font == "brier" ? this.gl.assets.fonts.brier.json.data : this.gl.assets.fonts.mona.json.data,
                flipY: !0,
                letterSpacing: A.letterSpacing
            }),
            B = new VU;
        B.uniforms.uMap.value = A.font == "brier" ? this.gl.assets.fonts.brier.atlas : this.gl.assets.fonts.mona.atlas, B.uniforms.uColor.value = new pA(A.color), B.uniforms.uOpacity.value = 1;
        let E = new TB;
        E.scale.set(A.shrink, 1, 1);
        let I = new OQ(Q, B);
        I.renderOrder = 1, I.scale.set(A.size, A.size, A.size), I.rotation.x = Math.PI;
        let C = new OQ(Q, B);
        C.renderOrder = 1, C.scale.set(A.size, A.size, A.size), C.rotation.x = Math.PI;
        let K = new V0().setFromObject(I),
            J = new o,
            U = 0.1;
        if (K.getCenter(J), I.position.sub(J), C.position.add(J), I.position.x -= U / 2, C.position.x += U / 2, K.max.x += U, A.side == "top") I.position.y = K.max.y / 2 - A.yShift + A.verticalGap, C.position.y = K.max.y / 2 - A.yShift + A.verticalGap;
        else I.position.y = -K.max.y - A.yShift - A.verticalGap, C.position.y = -K.max.y - A.yShift - A.verticalGap;
        return E.add(I), E.add(C), {
            mesh: E,
            update: () => {
                if (A.direction == "left") {
                    if (E.position.x -= this.gl.time.delta * 0.01 + Math.abs(window.lenis.velocity) * 0.001, E.position.x < -K.max.x * A.shrink) E.position.x = 0
                } else if (E.position.x += this.gl.time.delta * 0.01 + Math.abs(window.lenis.velocity) * 0.001, E.position.x > 0) E.position.x = -K.max.x * A.shrink
            }
        }
    }
    setOrbitControls() {
        this.controls = new XC(this.camera, this.gl.canvas), this.controls.enableDamping = !0, this.controls.enableZoom = !1
    }
    resize() {
        this.renderTarget.setSize(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio), this.responsive(), this.setScenePlaneDimensions()
    }
    responsive() {
        if (this.gl.sizes.width > 768) this.camera.position.z = 4.5, this.renderPlane.bounds.topMobile = 0;
        else if (this.gl.sizes.width >= 480 && this.gl.sizes.width <= 768) this.camera.position.z = jB.lerp(11, 6.5, Math.max(this.gl.sizes.width - 480, 0) / 288), this.renderPlane.bounds.topMobile = this.gl.sizes.height * 0.35;
        else this.camera.position.z = jB.lerp(14, 9.5, Math.max(this.gl.sizes.width - 320, 0) / 160), this.renderPlane.bounds.topMobile = this.gl.sizes.height * 0.35
    }
    setScenePlaneDimensions() {
        this.renderPlane.mesh.position.set(this.renderPlane.bounds.left - this.gl.sizes.width / 2 + this.gl.sizes.width / 2, -this.renderPlane.bounds.top + -this.renderPlane.bounds.topMobile + this.gl.sizes.height / 2 - this.gl.sizes.height / 2, 0), this.renderPlane.mesh.scale.set(this.gl.sizes.width, this.gl.sizes.height), this.camera.aspect = this.gl.sizes.width / this.gl.sizes.height, this.camera.updateProjectionMatrix()
    }
    setScroll() {
        m.fromTo(this.renderPlane.bounds, {
            top: () => this.gl.sizes.height
        }, {
            top: 0,
            ease: "none",
            scrollTrigger: {
                invalidateOnRefresh: !0,
                scrub: !0,
                trigger: '[data-gl-track="head"]',
                start: () => `top-=${this.gl.sizes.height} top`,
                end: () => "top top",
                onRefresh: () => {
                    this.setScenePlaneDimensions()
                },
                refreshPriority: -99
            },
            onUpdate: (A) => {
                this.setScenePlaneDimensions()
            }
        }), m.fromTo(this.renderPlane.bounds, {
            top: 0
        }, {
            top: () => -this.gl.sizes.height,
            ease: "none",
            scrollTrigger: {
                invalidateOnRefresh: !0,
                scrub: !0,
                trigger: '[data-gl-track="head"]',
                start: () => "bottom bottom",
                end: () => `bottom+=${this.gl.sizes.height} bottom`,
                onRefresh: () => {
                    this.setScenePlaneDimensions()
                },
                refreshPriority: -99
            },
            onUpdate: (A) => {
                this.setScenePlaneDimensions()
            }
        })
    }
    renderPipeline() {
        if (!this.isRendering) return;
        this.gl.renderer.instance.setRenderTarget(this.renderTarget), this.gl.renderer.instance.render(this.scene, this.camera), this.renderPlane.mesh.material.uniforms.tDiffuse.value = this.renderTarget.texture
    }
    update() {
        if (!this.isRendering) return;
        this.textTop.update(), this.textBottom.update()
    }
}

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L34178-L34413 =====
// helmet-scroll scene (34185-34413) + oN init wrapper
var oN = VA(() => {
    VQ();
    XU();
    LB();
    sN();
    _B()
});
class y9 {
    constructor(A) {
        if (this.id = "helmet-scroll", this.gl = new RQ, this.settings = A, this.isRendering = !1, this.scene = new rB, this.renderPlane = {
                mesh: new OQ(new QB(1, 1), new fQ({
                    transparent: !0,
                    side: hB,
                    uniforms: {
                        tDiffuse: new YA(null)
                    },
                    vertexShader: `
            varying vec2 vUv;

            void main() {
              gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);

              vUv = uv;
            }
          `,
                    fragmentShader: `
            varying vec2 vUv;

            uniform sampler2D tDiffuse;
            
            void main() {
              vec4 textureDiffuse = texture2D(tDiffuse, vUv);

              gl_FragColor = textureDiffuse;
              gl_FragColor.a *= smoothstep(0.5, 0.4, abs(vUv.x - 0.5));
              gl_FragColor.a *= smoothstep(0.5, 0.4, abs(vUv.y - 0.5));

              #include <tonemapping_fragment>
              #include <colorspace_fragment>
            }
          `
                })),
                bounds: null
            }, this.renderPlane.mesh.renderOrder = 2, this.renderTarget = new XB(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio, {
                samples: 1
            }), this.camera = new RB(20, this.gl.sizes.width / this.gl.sizes.height, 0.1, 1000), this.camera.position.z = 2, this.helmet = this.gl.assets.models.helmet.scene.clone(), this.helmet.scale.set(6, 6, 6), this.helmetGroup = new TB, this.scene.add(this.helmetGroup), this.helmetGroup.add(this.helmet), this.uniforms = {
                uTime: new YA(0),
                tMask: new YA(this.gl.assets.textures.helmet.mask),
                uHoverReveal: new YA(1),
                uDarkEdges: new YA(0),
                uHelmetTransition: new YA(0),
                tCurrentTexture: new YA(this.gl.assets.textures.helmet.diffuseLime),
                tNextTexture: new YA(this.gl.assets.textures.helmet.diffuseDisco)
            }, this.helmetMaterial = new Y0({
                normalMap: this.gl.assets.textures.helmet.normal,
                metalness: 1,
                roughness: 0.05,
                envMap: this.gl.assets.hdri.light,
                envMapIntensity: 1.5,
                side: hB
            }), this.glassMaterial = new Y0({
                map: this.gl.assets.textures.glass.base,
                roughnessMap: this.gl.assets.textures.glass.roughness,
                normalMap: this.gl.assets.textures.glass.normal,
                metalnessMap: this.gl.assets.textures.helmet.metallic,
                envMap: this.gl.assets.hdri.faded,
                envMapIntensity: 1.5
            }), this.plasticMaterial = new rC({
                transparent: !0,
                opacity: 0.25,
                matcap: this.gl.assets.textures.plastic.matcap,
                side: hB
            }), this.helmet.children.forEach((Q) => {
                if (Q.name == "helmet") Q.material = this.helmetMaterial, Q.material.onBeforeCompile = (B) => {
                    B.vertexShader = B.vertexShader.replace("varying vec3 vViewPosition;", `
              varying vec3 vViewPosition;
              varying vec3 vLocalPosition;
              varying vec3 vWorldPosition;
              varying vec2 vUv;
            `), B.vertexShader = B.vertexShader.replace("#include <fog_vertex>", `
              vLocalPosition = position.xyz;
              vWorldPosition = worldPosition.xyz;
              vUv = uv;
            `), B.uniforms.uTime = this.uniforms.uTime, B.uniforms.tMask = this.uniforms.tMask, B.uniforms.uHoverReveal = this.uniforms.uHoverReveal, B.uniforms.uDarkEdges = this.uniforms.uDarkEdges, B.uniforms.uHelmetTransition = this.uniforms.uHelmetTransition, B.uniforms.tCurrentTexture = this.uniforms.tCurrentTexture, B.uniforms.tNextTexture = this.uniforms.tNextTexture, B.fragmentShader = B.fragmentShader.replace("#include <common>", `
              const float PI = 3.141592;
              varying vec3 vWorldPosition;
              varying vec3 vLocalPosition;
              varying vec2 vUv;

              uniform float uDarkEdges;
              uniform float uHelmetTransition;

              uniform sampler2D tCurrentTexture;
              uniform sampler2D tNextTexture;

              #include <common>
            `), B.fragmentShader = B.fragmentShader.replace("vec4 diffuseColor = vec4( diffuse, opacity );", `
              vec4 diffuseColor = vec4(vec3(0.0), 1.0);

              vec4 textureCurrent = texture2D(tCurrentTexture, vUv);
              vec4 textureNext = texture2D(tNextTexture, vUv);

              float transition = vLocalPosition.y;

              vec3 color = mix(textureNext.rgb, textureCurrent.rgb, step(transition, mix(0.0425, -0.0425, uHelmetTransition)));

              if (gl_FrontFacing) {
                diffuseColor = vec4(color, opacity);
              }
            `)
                };
                if (Q.name == "glass") Q.material = this.glassMaterial, Q.material.onBeforeCompile = (B) => {
                    B.vertexShader = B.vertexShader.replace("varying vec3 vViewPosition;", `
                  varying vec3 vViewPosition;
                  varying vec3 vLocalPosition;
                  varying vec3 vWorldPosition;
                  varying vec2 vUv;
                `), B.vertexShader = B.vertexShader.replace("#include <fog_vertex>", `
                  vLocalPosition = position.xyz;
                  vWorldPosition = worldPosition.xyz;
                  vUv = uv;
                `), B.uniforms.uHoverReveal = this.uniforms.uHoverReveal, B.uniforms.uDarkEdges = this.uniforms.uDarkEdges, B.fragmentShader = B.fragmentShader.replace("#include <common>", `
                  varying vec3 vWorldPosition;
                  varying vec3 vLocalPosition;
                  varying vec2 vUv;
    
                  // uniform float uHoverReveal;
                  uniform float uDarkEdges;
    
                  #include <common>
                `), B.fragmentShader = B.fragmentShader.replace("vec4 diffuseColor = vec4( diffuse, opacity );", `              
                  // vec4 diffuseColor = vec4( diffuse, opacity);
                  // vec4 diffuseColor = vec4( diffuse, step(-vLocalPosition.y * 10., uHoverReveal - 0.5));
                  // vec4 diffuseColor = vec4(max(vec3(vWorldPosition.y * -1.0) * 100., 1.0), opacity );
                  float darkEdges = clamp(1.0 - abs(vLocalPosition.x) * 30., 0.2, 1.0);
    
                  vec4 diffuseColor = vec4(vec3(diffuse * mix(1.0, darkEdges, uDarkEdges)), opacity);
                `)
                };
                if (Q.name == "plastic") Q.renderOrder = 1, Q.material = this.plasticMaterial
            }), this.disco = new I8(this.helmet), document.addEventListener("disco", (Q) => {
                this.animateInDisco(Q.detail.direction)
            }), window.landoGL.params.headScene.VARIANT = R9 ? "Google" : this.gl.time.getVariantAccordingToTime(), this.setTimeline(), this.setIsRendering(), this.setVariant(window.landoGL.params.headScene.VARIANT), this.gl.isDebug) this.setDebug()
    }
    setIsRendering() {
        TA.create({
            trigger: '[data-heroflip="track"]',
            start: () => `top-=${this.gl.sizes.height/2} bottom`,
            end: () => `bottom+=${this.gl.sizes.height/2} top`,
            invalidateOnRefresh: !0,
            refreshPriority: -99,
            onRefresh: () => {
                this.isRendering = !0
            },
            onEnter: () => {
                this.isRendering = !0
            },
            onEnterBack: () => {
                this.isRendering = !0
            },
            onLeave: () => {
                this.isRendering = !1
            },
            onLeaveBack: () => {
                this.isRendering = !1
            }
        })
    }
    setOrbitControls() {
        this.controls = new OrbitControls(this.camera, this.gl.canvas), this.controls.enableDamping = !0, this.controls.enableZoom = !1
    }
    setTimeline() {
        this.timeline = m.timeline({
            paused: !0
        }), this.timeline.fromTo(this.helmetGroup.rotation, {
            y: Math.PI / 2.2
        }, {
            y: Math.PI * 4,
            duration: 1,
            ease: "power1.inOut"
        }, 0), this.timeline.fromTo(this.helmet.rotation, {
            x: Math.PI / 12
        }, {
            x: -Math.PI / 10,
            duration: 0.5,
            ease: "power1.in"
        }, 0), this.timeline.fromTo(this.helmet.rotation, {
            x: -Math.PI / 10
        }, {
            x: Math.PI / 20,
            duration: 0.5,
            ease: "power1.out"
        }, 0.5)
    }
    setDebug() {
        let A = {
            variant: {}
        };
        this.gl.world.helmetScrollSceneDebugFolder.add(A, "variant", ["Lime", "Dark", "Grid"]).name("HELMET VARIANT").onChange((B) => {
            this.setVariant(B)
        }).setValue("Lime")
    }
    setVariant(A) {
        if (A == "Lime") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseLime, this.helmetMaterial.envMapIntensity = 1.5, this.helmetMaterial.iridescence = 0, this.helmetMaterial.needsUpdate = !0;
        else if (A == "Dark") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseDark, this.helmetMaterial.envMapIntensity = 1.5, this.helmetMaterial.iridescence = 0, this.helmetMaterial.needsUpdate = !0;
        else if (A == "Disco") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseDisco, this.helmetMaterial.envMapIntensity = 1.5, this.helmetMaterial.iridescence = 0, this.helmetMaterial.needsUpdate = !0;
        else if (A == "Grid") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseGrid, this.helmetMaterial.envMapIntensity = 1, this.helmetMaterial.iridescence = 0, this.helmetMaterial.needsUpdate = !0;
        else if (A == "Google") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseGoogle, this.helmetMaterial.envMapIntensity = 0.75, this.helmetMaterial.iridescence = 0, this.helmetMaterial.needsUpdate = !0
    }
    resize() {
        this.renderTarget.setSize(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio), this.setScenePlaneDimensions()
    }
    setScenePlaneDimensions() {
        this.renderPlane.mesh.position.set(window.landoGL.bounds.helmetScroll.left - this.gl.sizes.width / 2, -window.landoGL.bounds.helmetScroll.top + window.lenis.scroll + this.gl.sizes.height / 2, 0), this.renderPlane.mesh.scale.set(window.landoGL.bounds.helmetScroll.width, window.landoGL.bounds.helmetScroll.width), this.camera.aspect = window.landoGL.bounds.helmetScroll.width / window.landoGL.bounds.helmetScroll.width, this.camera.updateProjectionMatrix()
    }
    renderPipeline() {
        if (!this.isRendering) return;
        this.gl.renderer.instance.setRenderTarget(this.renderTarget), this.gl.renderer.instance.render(this.scene, this.camera), this.renderPlane.mesh.material.uniforms.tDiffuse.value = this.renderTarget.texture
    }
    animateInDisco(A) {
        if (A == 1) m.to(this.uniforms.uHelmetTransition, {
            value: 1,
            duration: 2,
            ease: "expo.inOut"
        });
        else m.to(this.uniforms.uHelmetTransition, {
            value: 0,
            duration: 2,
            ease: "expo.inOut"
        })
    }
    update() {
        if (!this.isRendering) return;
        this.timeline.progress(window.landoGL.params.helmetScrollScene.PROGRESS + 0.001), this.setScenePlaneDimensions()
    }
}

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L34414-L34753 =====
// not-found scene (34421-34753) + iN init wrapper
var iN = VA(() => {
    VQ();
    LB();
    EY();
    _B();
    X9()
});
class x9 {
    constructor(A) {
        if (this.id = "not-found", this.settings = A, this.gl = new RQ, this.previousVariant = window.landoGL.params.notFoundScene.VARIANT, this.isTransitioning = !1, this.isRendering = !1, this.autoRotateValue = 0, this.scene = new rB, this.easedMouse = {
                normalized: this.gl.mouse.createEasedNormalized(0.025)
            }, this.renderPlane = {
                mesh: new OQ(new QB(1, 1), new fQ({
                    transparent: !0,
                    side: hB,
                    uniforms: {
                        tDiffuse: new YA(null)
                    },
                    vertexShader: `
            varying vec2 vUv;

            void main() {
              gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);

              vUv = uv;
            }
          `,
                    fragmentShader: `
            // #include <tonemapping_pars_fragment>

            varying vec2 vUv;

            uniform sampler2D tDiffuse;
            
            void main() {
              vec4 textureDiffuse = texture2D(tDiffuse, vUv);

              gl_FragColor = textureDiffuse;
              gl_FragColor.a *= smoothstep(0.5, 0.4, abs(vUv.y - 0.5));
              // gl_FragColor.a *= smoothstep(0.0, 0.05, abs(vUv.y - 0.5));
              // gl_FragColor = vec4(1.0, 0.0, 0.0, 0.25);

              #include <tonemapping_fragment>
              #include <colorspace_fragment>
            }
          `
                })),
                bounds: {
                    top: 0,
                    left: 0,
                    width: this.gl.sizes.width,
                    height: this.gl.sizes.height
                }
            }, this.renderPlane.mesh.renderOrder = 2, this.renderTarget = new XB(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio, {
                samples: 1
            }), this.cameraData = {
                previousAzimuthalAngle: 0,
                totalAzimuthalAngle: 0
            }, this.camera = new RB(20, this.gl.sizes.width / this.gl.sizes.height, 0.1, 1000), this.camera.position.z = 2, this.helmet = this.gl.assets.models.helmet.scene.clone(), this.helmetGroup = new TB, this.helmetGroup.scale.set(6, 6, 6), this.scene.add(this.helmetGroup), this.helmetGroup.add(this.helmet), this.helmetWorldRotation = new o(0, 0, 0), this.uniforms = {
                tMask: new YA(this.gl.assets.textures.helmet.mask),
                tDiscoMatcap: new YA(this.gl.assets.textures.disco.matcap),
                tDiscoMask: new YA(this.gl.assets.textures.disco.mask),
                uTime: new YA(0),
                uHoverReveal: new YA(1),
                uDarkEdges: new YA(0),
                uHelmetTransition: new YA(0),
                tCurrentTexture: new YA(null),
                tNextTexture: new YA(null)
            }, this.helmetMaterial = new Y0({
                normalMap: this.gl.assets.textures.helmet.normal,
                metalness: 1,
                roughness: 0.05,
                envMap: this.gl.assets.hdri.light,
                envMapIntensity: 1.5,
                side: hB
            }), this.glassMaterial = new Y0({
                map: this.gl.assets.textures.glass.base,
                roughnessMap: this.gl.assets.textures.glass.roughness,
                normalMap: this.gl.assets.textures.glass.normal,
                metalnessMap: this.gl.assets.textures.helmet.metallic,
                envMap: this.gl.assets.hdri.faded,
                envMapIntensity: 1.5
            }), this.plasticMaterial = new rC({
                transparent: !0,
                opacity: 0.25,
                matcap: this.gl.assets.textures.plastic.matcap,
                side: hB
            }), this.helmet.children.forEach((Q) => {
                if (Q.name == "helmet") Q.material = this.helmetMaterial, Q.material.onBeforeCompile = (B) => {
                    B.vertexShader = B.vertexShader.replace("varying vec3 vViewPosition;", `
              varying vec3 vViewPosition;
              varying vec3 vLocalPosition;
              varying vec3 vWorldPosition;
              varying vec2 vUv;
            `), B.vertexShader = B.vertexShader.replace("#include <fog_vertex>", `
              vLocalPosition = position.xyz;
              vWorldPosition = worldPosition.xyz;
              vUv = uv;
            `), B.uniforms.uTime = this.uniforms.uTime, B.uniforms.tMask = this.uniforms.tMask, B.uniforms.uHoverReveal = this.uniforms.uHoverReveal, B.uniforms.uDarkEdges = this.uniforms.uDarkEdges, B.uniforms.uHelmetTransition = this.uniforms.uHelmetTransition, B.uniforms.tCurrentTexture = this.uniforms.tCurrentTexture, B.uniforms.tNextTexture = this.uniforms.tNextTexture, B.fragmentShader = B.fragmentShader.replace("#include <common>", `
              // #include <simplex>

              const float PI = 3.141592;

              // #define M_PI 3.14159265358979323846

              // const float DISTORT_SCALE = 50.0;
              // const float DISTORT_INTENSITY = 0.006; // 0.0075

              // const float NOISE_SIZE = 50.0;

              // const float OUTLINE_SCALE = 0.125;
              // const float OUTLINE_THICKNESS = 0.2;

              // const float TIME_SPEED = 0.1;

              varying vec3 vWorldPosition;
              varying vec3 vLocalPosition;
              varying vec2 vUv;

              // uniform sampler2D tMask;
              // uniform float uTime;
              // uniform float uHoverReveal;
              uniform float uDarkEdges;
              uniform float uHelmetTransition;

              uniform sampler2D tCurrentTexture;
              uniform sampler2D tNextTexture;

              #include <common>
            `), B.fragmentShader = B.fragmentShader.replace("vec4 diffuseColor = vec4( diffuse, opacity );", `              
              // vec4 diffuseColor = vec4( diffuse, opacity);
              // vec4 diffuseColor = vec4( diffuse, step(-vLocalPosition.y * 10., uHoverReveal - 0.5));
              // vec4 diffuseColor = vec4(max(vec3(vWorldPosition.y * -1.0) * 100., 1.0), opacity );
              // float darkEdges = clamp(1.0 - abs(vLocalPosition.x) * 30., 0.2, 1.0);

              vec4 diffuseColor = vec4(vec3(0.0), 1.0);

              vec4 textureCurrent = texture2D(tCurrentTexture, vUv);
              vec4 textureNext = texture2D(tNextTexture, vUv);

              float transition = vLocalPosition.y - sin(vLocalPosition.x * PI) * sin(uHelmetTransition * PI) * 0.1;

              vec4 color = mix(textureNext, textureCurrent, step(transition, mix(0.05, -0.05, uHelmetTransition)));

              if (gl_FrontFacing) {
                diffuseColor = vec4(color.rgb, opacity);
              }
            `)
                };
                if (Q.name == "glass") Q.material = this.glassMaterial, Q.material.onBeforeCompile = (B) => {
                    B.vertexShader = B.vertexShader.replace("varying vec3 vViewPosition;", `
              varying vec3 vViewPosition;
              varying vec3 vLocalPosition;
              varying vec3 vWorldPosition;
              varying vec2 vUv;
            `), B.vertexShader = B.vertexShader.replace("#include <fog_vertex>", `
              vLocalPosition = position.xyz;
              vWorldPosition = worldPosition.xyz;
              vUv = uv;
            `), B.uniforms.uHoverReveal = this.uniforms.uHoverReveal, B.uniforms.uDarkEdges = this.uniforms.uDarkEdges, B.fragmentShader = B.fragmentShader.replace("#include <common>", `
              varying vec3 vWorldPosition;
              varying vec3 vLocalPosition;
              varying vec2 vUv;

              // uniform float uHoverReveal;
              uniform float uDarkEdges;
              
              #include <common>
            `), B.fragmentShader = B.fragmentShader.replace("vec4 diffuseColor = vec4( diffuse, opacity );", `              
              // vec4 diffuseColor = vec4( diffuse, opacity);
              // vec4 diffuseColor = vec4( diffuse, step(-vLocalPosition.y * 10., uHoverReveal - 0.5));
              // vec4 diffuseColor = vec4(max(vec3(vWorldPosition.y * -1.0) * 100., 1.0), opacity );
              // float darkEdges = clamp(1.0 - abs(vLocalPosition.x) * 30., 0.2, 1.0);

              vec4 diffuseColor = vec4(diffuse, opacity);
            `)
                };
                if (Q.name == "plastic") Q.renderOrder = 1, Q.material = this.plasticMaterial
            }), this.disco = new I8(this.helmet, !0), document.addEventListener("disco", (Q) => {
                if (Q.detail.direction == 1) this.translate("Disco");
                else this.translate("Lime")
            }), this.foursGroup = new TB, this.scene.add(this.foursGroup), this.fourMeshRight = new OQ(new QB(0.305, 0.305), new a0({
                color: 13827840,
                alphaMap: this.gl.assets.textures.notFound.diffuse,
                transparent: !0
            })), this.foursGroup.add(this.fourMeshRight), this.fourMeshLeft = new OQ(new QB(0.305, 0.305), new a0({
                color: 13827840,
                alphaMap: this.gl.assets.textures.notFound.diffuse,
                transparent: !0
            })), this.foursGroup.add(this.fourMeshLeft), this.gl.isDebug) this.setDebug();
        this.setOrbitControls(), this.setDefaultVariant(), this.getBounds(), this.setScroll(), this.setIsRendering(), this.setFoursDimensions()
    }
    getBounds() {
        let A = this.settings.dom.getBoundingClientRect();
        this.renderPlane.bounds.left = A.left, this.renderPlane.bounds.width = A.width, this.renderPlane.bounds.height = A.height
    }
    setScroll() {
        m.fromTo(this.renderPlane.bounds, {
            top: () => this.gl.sizes.height
        }, {
            top: () => -this.gl.sizes.height,
            ease: "none",
            scrollTrigger: {
                invalidateOnRefresh: !0,
                scrub: !0,
                trigger: this.settings.dom,
                start: () => `center-=${this.gl.sizes.height} center`,
                end: () => `center+=${this.gl.sizes.height} center`,
                refreshPriority: -99,
                onRefresh: () => {
                    this.getBounds(), this.setScenePlaneDimensions()
                }
            },
            onUpdate: (A) => {
                this.setScenePlaneDimensions()
            }
        })
    }
    setFoursDimensions() {
        if (this.gl.sizes.width > 479) this.fourMeshRight.position.x = 0.3275, this.fourMeshLeft.position.x = -0.3275, this.fourMeshRight.scale.set(1, 1), this.fourMeshLeft.scale.set(1, 1);
        else this.fourMeshRight.position.x = 0.245, this.fourMeshLeft.position.x = -0.245, this.fourMeshRight.scale.set(1.75, 1.75), this.fourMeshLeft.scale.set(1.75, 1.75)
    }
    setIsRendering() {
        TA.create({
            trigger: this.settings.dom.parentElement,
            start: () => `top-=${this.gl.sizes.height/2} bottom`,
            end: () => `bottom+=${this.gl.sizes.height/2} top`,
            invalidateOnRefresh: !0,
            refreshPriority: -99,
            onEnter: () => {
                this.isRendering = !0
            },
            onEnterBack: () => {
                this.isRendering = !0
            },
            onLeave: () => {
                this.isRendering = !1
            },
            onLeaveBack: () => {
                this.isRendering = !1
            }
        })
    }
    translate(A) {
        return new Promise(async (Q) => {
            if (this.isTransitioning) return;
            if (document.documentElement.classList.add("gl-not-found-isTransitioning"), A == "Lime") this.uniforms.tNextTexture.value = this.gl.assets.textures.helmet.diffuseLime, m.to(this.helmetMaterial, {
                envMapIntensity: 1.5,
                duration: 2
            }), this.disco.transition(-1, !0), this.disco.direction = -1;
            else if (A == "Dark") this.uniforms.tNextTexture.value = this.gl.assets.textures.helmet.diffuseDark, m.to(this.helmetMaterial, {
                envMapIntensity: 1.5,
                duration: 2
            }), this.disco.transition(-1, !0), this.disco.direction = -1;
            else if (A == "Grid") this.uniforms.tNextTexture.value = this.gl.assets.textures.helmet.diffuseGrid, m.to(this.helmetMaterial, {
                envMapIntensity: 1,
                duration: 2
            }), this.disco.transition(-1, !0), this.disco.direction = -1;
            else if (A == "Disco") this.uniforms.tNextTexture.value = this.gl.assets.textures.helmet.diffuseDisco, m.to(this.helmetMaterial, {
                envMapIntensity: 1,
                duration: 2
            });
            else if (A == "Google") this.uniforms.tNextTexture.value = this.gl.assets.textures.helmet.diffuseGoogle, m.to(this.helmetMaterial, {
                envMapIntensity: 0.75,
                duration: 2
            }), this.disco.transition(-1, !0), this.disco.direction = -1;
            m.to(this.uniforms.uHelmetTransition, {
                value: 1,
                duration: 2,
                ease: "expo.inOut",
                onStart: () => {
                    this.isTransitioning = !0
                },
                onComplete: () => {
                    this.uniforms.tCurrentTexture.value = this.uniforms.tNextTexture.value, this.uniforms.uHelmetTransition.value = 0, this.isTransitioning = !1, document.documentElement.classList.remove("gl-not-found-isTransitioning"), Q()
                }
            }), m.to(this.helmet.rotation, {
                y: Math.PI * 2 + this.controls.getAzimuthalAngle(),
                ease: "back.inOut",
                duration: 2,
                onComplete: () => {
                    this.helmet.rotation.y = 0 + this.controls.getAzimuthalAngle()
                }
            }), m.to(this, {
                autoRotateValue: 0,
                duration: 2,
                ease: "back.inOut"
            })
        })
    }
    setDefaultVariant() {
        if (window.landoGL.params.notFoundScene.VARIANT == "Lime") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseLime;
        else if (window.landoGL.params.notFoundScene.VARIANT == "Dark") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseDark;
        else if (window.landoGL.params.notFoundScene.VARIANT == "Grid") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseGrid;
        else if (window.landoGL.params.notFoundScene.VARIANT == "Disco") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseDisco;
        else if (window.landoGL.params.notFoundScene.VARIANT == "Google") this.uniforms.tCurrentTexture.value = this.gl.assets.textures.helmet.diffuseGoogle
    }
    setOrbitControls() {
        this.controls = new XC(this.camera, document.querySelector(".gl-wrap")), this.controls.enableDamping = !0, this.controls.enableZoom = !1, this.controls.enablePan = !1, this.controls.minPolarAngle = Math.PI / 2, this.controls.maxPolarAngle = Math.PI / 2, this.controls.addEventListener("change", (A) => {
            let Q = this.controls.getAzimuthalAngle(),
                B = Q - this.cameraData.previousAzimuthalAngle;
            if (B > Math.PI) B -= 2 * Math.PI;
            else if (B < -Math.PI) B += 2 * Math.PI;
            this.cameraData.totalAzimuthalAngle += B, this.cameraData.previousAzimuthalAngle = Q, this.helmetMaterial.envMapRotation.y = this.cameraData.totalAzimuthalAngle, this.glassMaterial.envMapRotation.y = this.cameraData.totalAzimuthalAngle, this.foursGroup.lookAt(this.camera.position)
        })
    }
    setDebug() {
        let A = {
            variant: {}
        };
        this.gl.world.notFoundSceneDebugFolder.add(A, "variant", ["Lime", "Dark", "Grid", "Disco", "Google"]).name("HELMET VARIANT").onChange((B) => {
            window.landoGL.params.notFoundScene.VARIANT = B
        }).setValue("Lime"), this.gl.world.notFoundSceneDebugFolder.add(window.landoGL.params.notFoundScene, "HELMET_AUTOROTATE_SPEED").onChange((B) => {
            this.controls.autoRotateSpeed = B
        }), this.gl.world.notFoundSceneDebugFolder.add(window.landoGL.params.notFoundScene, "HELMET_ANGLE").min(-90).max(90).onChange((B) => {
            this.helmet.rotation.x = jB.degToRad(B)
        })
    }
    setVariant(A) {
        if (window.landoGL.params.notFoundScene.VARIANT = A, A == "Lime");
        else if (A == "Dark");
        else if (A == "Grid");
    }
    resize() {
        this.renderTarget.setSize(this.gl.sizes.width * this.gl.sizes.pixelRatio, this.gl.sizes.height * this.gl.sizes.pixelRatio), this.getBounds(), this.setScenePlaneDimensions(), this.setFoursDimensions()
    }
    setScenePlaneDimensions() {
        this.renderPlane.mesh.position.set(this.renderPlane.bounds.left - this.gl.sizes.width / 2 + this.renderPlane.bounds.width / 2, -this.renderPlane.bounds.top + this.renderPlane.bounds.width / 2 - this.renderPlane.bounds.height / 2, 0), this.renderPlane.mesh.scale.set(this.gl.sizes.width, this.renderPlane.bounds.height), this.camera.aspect = this.gl.sizes.width / this.renderPlane.bounds.height, this.camera.updateProjectionMatrix()
    }
    renderPipeline() {
        if (!this.isRendering) return;
        this.gl.renderer.instance.setRenderTarget(this.renderTarget), this.gl.renderer.instance.render(this.scene, this.camera), this.renderPlane.mesh.material.uniforms.tDiffuse.value = this.renderTarget.texture
    }
    update() {
        if (!this.isRendering) return;
        if (this.default = 0, this.controls.update(), this.helmet.rotation.x = -this.easedMouse.normalized.value.y * jB.degToRad(window.landoGL.params.notFoundScene.HELMET_ANGLE), this.helmetGroup.rotation.y = Math.PI / 2.5 + this.easedMouse.normalized.value.x * (jB.degToRad(window.landoGL.params.notFoundScene.HELMET_ANGLE) * 0.5), !this.isTransitioning) this.autoRotateValue += this.gl.time.delta * 0.0025 * window.landoGL.params.notFoundScene.HELMET_AUTOROTATE_SPEED;
        if (this.autoRotateValue = this.autoRotateValue % (Math.PI * 2), this.helmetGroup.rotation.y += this.autoRotateValue, this.easedMouse.normalized.update(this.gl.time.delta), window.landoGL.params.notFoundScene.VARIANT !== this.previousVariant) this.translate(window.landoGL.params.notFoundScene.VARIANT);
        this.previousVariant = window.landoGL.params.notFoundScene.VARIANT
    }
}

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L34754-L35705 =====
// noise / fluid (6 passes) / idle (34761-35625) + MO init wrapper + VO simplex GLSL chunk + LO
var nN = VA(() => {
    VQ();
    XU();
    LB();
    _B();
    X9()
});
class f9 {
    constructor() {
        this.gl = new RQ, this.params = {
            movement: {
                intensity: 0.075,
                ease: 0.025
            }
        }, this.easedMouse = {
            normalized: this.gl.mouse.createEasedNormalized(this.params.movement.ease),
            pace: this.gl.mouse.createEasedPace(0.01)
        }, this.scene = new rB, this.camera = new jI, this.mesh = new OQ(new QB(2, 2), new fQ({
            uniforms: {
                uAspect: new YA(this.gl.sizes.width / this.gl.sizes.height),
                uTime: new YA(0),
                uMouseCoords: new YA(this.easedMouse.normalized.value),
                uMousePace: new YA(this.easedMouse.pace.value),
                uReveal: new YA(window.landoGL.reveal),
                SCALE: new YA(window.landoGL.params.headScene.SCALE),
                SPEED: new YA(window.landoGL.params.headScene.SPEED),
                DISTORT_SCALE: new YA(window.landoGL.params.headScene.DISTORT_SCALE),
                DISTORT_INTENSITY: new YA(window.landoGL.params.headScene.DISTORT_INTENSITY),
                NOISE_DETAIL: new YA(window.landoGL.params.headScene.NOISE_DETAIL),
                CURSOR_INTENSITY: new YA(window.landoGL.params.headScene.CURSOR_INTENSITY),
                CURSOR_SCALE: new YA(window.landoGL.params.headScene.CURSOR_SCALE),
                CURSOR_BOUNCE: new YA(window.landoGL.params.headScene.CURSOR_BOUNCE),
                REVEAL_SIZE: new YA(window.landoGL.params.headScene.REVEAL_SIZE)
            },
            vertexShader: `
          varying vec2 vUv;

          void main() {
            gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);

            vUv = uv;
          }
        `,
            fragmentShader: `
          #include <simplex>

          varying vec2 vUv;

          uniform float uAspect;
          uniform float uTime;
          uniform float uMousePace;
          uniform float uReveal;
          uniform vec2 uMouseCoords;

          uniform float SCALE;
          uniform float SPEED;
          uniform float DISTORT_SCALE;
          uniform float DISTORT_INTENSITY;
          uniform float NOISE_DETAIL;
          uniform float CURSOR_INTENSITY;
          uniform float CURSOR_SCALE;
          uniform float CURSOR_BOUNCE;
          uniform float REVEAL_SIZE;

          void main() {
            /* 
              UVs
            */
            vec2 uv = vUv;
            uv.x *= uAspect;
            uv.y += (REVEAL_SIZE + REVEAL_SIZE / 3.) * (1.0 - uReveal);
            uv.y /= 1.0 + (REVEAL_SIZE) * (1.0 - uReveal);
          
            /* 
              Cursor
            */
            vec2 mouse = uMouseCoords;
            mouse *= vec2(0.5);
            mouse += vec2(0.5);
            mouse.x *= uAspect;
          
            float cursor = 1.0 - distance(mouse, uv) * CURSOR_SCALE;
            cursor *= uMousePace;
            cursor = clamp(cursor, CURSOR_BOUNCE, 1.0);
          
            /* 
              Noise
            */
            float noiseDistort = 0.5 + snoise(vec3(uv.x * DISTORT_SCALE, uv.y * DISTORT_SCALE, uTime * SPEED * 0.1)) * 0.5;
          
            float noiseFinal = 0.5 + snoise(
              vec3(
                (uv.x + (cursor * CURSOR_INTENSITY) + (noiseDistort * DISTORT_INTENSITY)) * SCALE, 
                (uv.y + (cursor * CURSOR_INTENSITY) + (noiseDistort * DISTORT_INTENSITY)) * SCALE, 
                uTime * SPEED
              )
            ) * 0.5;
            noiseFinal *= NOISE_DETAIL;
            noiseFinal = fract(noiseFinal); 

            float noiseBase = step(0.5, noiseFinal);

            gl_FragColor = vec4(vec3(noiseBase, noiseFinal, 0.0), 1.0);
          }
        `
        })), this.scene.add(this.mesh), this.renderTarget = new n8(this.gl.sizes.width, this.gl.sizes.height, {
            samples: 1
        })
    }
    resize() {
        this.renderTarget.setSize(this.gl.sizes.width, this.gl.sizes.height)
    }
    update() {
        this.easedMouse.normalized.update(this.gl.time.delta), this.easedMouse.pace.update(this.gl.time.delta), this.mesh.material.uniforms.uTime.value = this.gl.time.elapsed, this.mesh.material.uniforms.uMouseCoords.value = this.easedMouse.normalized.value, this.mesh.material.uniforms.uMousePace.value = this.easedMouse.pace.value.default * 4, this.mesh.material.uniforms.uReveal.value = window.landoGL.reveal, this.mesh.material.uniforms.uAspect.value = this.gl.sizes.width / this.gl.sizes.height, this.gl.renderer.instance.setRenderTarget(this.renderTarget), this.gl.renderer.instance.render(this.scene, this.camera)
    }
}
var aN = VA(() => {
    VQ();
    _B()
});
var AI = `attribute vec3 position;
uniform vec2 px;
uniform vec2 boundarySpace;
varying vec2 uv;

precision highp float;

void main(){
    vec3 pos = position;
    vec2 scale = 1.0 - boundarySpace * 2.0;
    pos.xy = pos.xy * scale;
    uv = vec2(0.5)+(pos.xy)*0.5;
    gl_Position = vec4(pos, 1.0);
}
`;
var KK = () => {};
var tN = `precision highp float;
uniform sampler2D velocity;
varying vec2 uv;

void main(){
    vec2 vel = texture2D(velocity, uv).xy;
    float len = length(vel);
    vel = vel * 0.5 + 0.5;
    
    vec3 color = vec3(vel.x, vel.y, 1.0);
    color = mix(vec3(1.0), color, len);

    gl_FragColor = vec4(color,  1.0);
}
`;
var rN = () => {};
var AO = `attribute vec3 position;
varying vec2 uv;
uniform vec2 px;


precision highp float;

void main(){
    vec3 pos = position;
    uv = 0.5 + pos.xy * 0.5;
    vec2 n = sign(pos.xy);
    pos.xy = abs(pos.xy) - px * 1.0;
    pos.xy *= n;
    gl_Position = vec4(pos, 1.0);
}`;
var eN = () => {};
var qY = `precision highp float;
uniform sampler2D velocity;
uniform float dt;
uniform float dissipation;
uniform bool isBFECC;

uniform vec2 fboSize;
uniform vec2 px;
varying vec2 uv;

void main(){
    vec2 ratio = max(fboSize.x, fboSize.y) / fboSize;

    if(isBFECC == false){
        vec2 vel = texture2D(velocity, uv).xy;
        vec2 uv2 = uv - vel * dt * ratio;
        vec2 newVel = texture2D(velocity, uv2).xy;
        gl_FragColor = vec4(newVel, 0.0, 0.0);
    } else {
        vec2 spot_new = uv;
        vec2 vel_old = texture2D(velocity, uv).xy;
        
        vec2 spot_old = spot_new - vel_old * dt * ratio;
        vec2 vel_new1 = texture2D(velocity, spot_old).xy;

        
        vec2 spot_new2 = spot_old + vel_new1 * dt * ratio;
        
        vec2 error = spot_new2 - spot_new;

        vec2 spot_new3 = spot_new - error / 2.0;
        vec2 vel_2 = texture2D(velocity, spot_new3).xy;

        
        vec2 spot_old2 = spot_new3 - vel_2 * dt * ratio;
        
        vec2 newVel2 = texture2D(velocity, spot_old2).xy * dissipation; 
        gl_FragColor = vec4(newVel2, 0.0, 0.0);
    }
}
`;
var QO = () => {};
class zE {
    constructor(A) {
        this.gl = new RQ, this.props = A, this.uniforms = this.props.material?.uniforms
    }
    init() {
        if (this.scene = new rB, this.camera = new jI, this.uniforms) this.material = new _I(this.props.material), this.geometry = new QB(2, 2), this.plane = new OQ(this.geometry, this.material), this.scene.add(this.plane)
    }
    update() {
        this.gl.renderer.instance.setRenderTarget(this.props.output), this.gl.renderer.instance.render(this.scene, this.camera), this.gl.renderer.instance.setRenderTarget(null)
    }
}
var JK = VA(() => {
    VQ();
    _B()
});
var b9;
var BO = VA(() => {
    KK();
    eN();
    QO();
    JK();
    VQ();
    b9 = class b9 extends zE {
        constructor(A) {
            super({
                material: {
                    vertexShader: AI,
                    fragmentShader: qY,
                    uniforms: {
                        boundarySpace: {
                            value: A.cellScale
                        },
                        px: {
                            value: A.cellScale
                        },
                        fboSize: {
                            value: A.fboSize
                        },
                        velocity: {
                            value: A.src.texture
                        },
                        dt: {
                            value: A.dt
                        },
                        isBFECC: {
                            value: !0
                        },
                        dissipation: {
                            value: A.dissipation
                        }
                    }
                },
                output: A.dst
            });
            this.init()
        }
        init() {
            super.init(), this.createBoundary()
        }
        createBoundary() {
            let A = new sB,
                Q = new Float32Array([-1, -1, 0, -1, 1, 0, -1, 1, 0, 1, 1, 0, 1, 1, 0, 1, -1, 0, 1, -1, 0, -1, -1, 0]);
            A.setAttribute("position", new hQ(Q, 3));
            let B = new _I({
                vertexShader: AO,
                fragmentShader: qY,
                uniforms: this.uniforms
            });
            this.line = new NJ(A, B), this.scene.add(this.line)
        }
        update({
            dt: A,
            isBounce: Q,
            BFECC: B
        }) {
            this.uniforms.dt.value = A, this.line.visible = Q, this.uniforms.isBFECC.value = B, super.update()
        }
    }
});
var IO = `precision highp float;

attribute vec3 position;
attribute vec2 uv;
uniform vec2 center;
uniform vec2 scale;
uniform vec2 px;
varying vec2 vUv;

void main(){
    vec2 pos = position.xy * scale * 2.0 * px + center;
    vUv = uv;
    gl_Position = vec4(pos, 0.0, 1.0);
}
`;
var EO = () => {};
var KO = `precision highp float;

uniform vec2 force;
uniform vec2 center;
uniform vec2 scale;
uniform vec2 px;
varying vec2 vUv;

void main(){
    vec2 circle = (vUv - 0.5) * 2.0;
    float d = 1.0-min(length(circle), 1.0);
    d *= d;
    gl_FragColor = vec4(force * d, 0, 1);
}
`;
var CO = () => {};
class wJ {
    constructor() {
        this.gl = new RQ, this.mouseMoved = !1, this.coords = new kA, this.coords_old = new kA, this.diff = new kA, this.timer = null, this.count = 0
    }
    init() {
        document.body.addEventListener("mousemove", this.onDocumentMouseMove.bind(this), !1), document.body.addEventListener("touchstart", this.onDocumentTouchStart.bind(this), !1), document.body.addEventListener("touchmove", this.onDocumentTouchMove.bind(this), !1)
    }
    setCoords(A, Q) {
        if (this.timer) clearTimeout(this.timer);
        this.coords.set(A, Q), this.mouseMoved = !0, this.timer = setTimeout(() => {
            this.mouseMoved = !1
        }, 100)
    }
    onDocumentMouseMove(A) {
        this.setCoords(this.gl.mouse.normalized.current.x, this.gl.mouse.normalized.current.y)
    }
    onDocumentTouchStart(A) {
        if (A.touches.length === 1) this.setCoords(this.gl.mouse.normalized.current.x, this.gl.mouse.normalized.current.y)
    }
    onDocumentTouchMove(A) {
        if (A.touches.length === 1) this.setCoords(this.gl.mouse.normalized.current.x, this.gl.mouse.normalized.current.y)
    }
    update() {
        if (this.gl.world.idleState.isMoving) {
            if (this.diff.subVectors(this.coords, this.coords_old), this.coords_old.copy(this.coords), this.coords_old.x === 0 && this.coords_old.y === 0) this.diff.set(0, 0)
        } else this.coords.copy(this.gl.world.idleState.idleCursor), this.diff.subVectors(this.coords, this.coords_old), this.coords_old.copy(this.coords)
    }
}
var YY = VA(() => {
    VQ();
    _B()
});
var h9;
var JO = VA(() => {
    EO();
    CO();
    JK();
    YY();
    VQ();
    h9 = class h9 extends zE {
        constructor(A) {
            super({
                output: A.dst
            });
            this.mouse = new wJ, this.mouse.init(), this.init(A)
        }
        init(A) {
            super.init();
            let Q = new QB(1, 1);
            this.mouseMaterial = new _I({
                vertexShader: IO,
                fragmentShader: KO,
                blending: YC,
                uniforms: {
                    px: {
                        value: A.cellScale
                    },
                    force: {
                        value: new kA(0, 0)
                    },
                    center: {
                        value: new kA(0, 0)
                    },
                    scale: {
                        value: new kA(A.cursor_size, A.cursor_size)
                    }
                }
            }), this.mouseMesh = new OQ(Q, this.mouseMaterial), this.scene.add(this.mouseMesh)
        }
        update(A) {
            this.mouse.update();
            let Q = this.mouse.diff.x / 2 * A.mouse_force,
                B = this.mouse.diff.y / 2 * A.mouse_force,
                E = A.cursor_size * A.cellScale.x,
                I = A.cursor_size * A.cellScale.y,
                C = Math.min(Math.max(this.mouse.coords.x, -1 + E + A.cellScale.x * 2), 1 - E - A.cellScale.x * 2),
                K = Math.min(Math.max(this.mouse.coords.y, -1 + I + A.cellScale.y * 2), 1 - I - A.cellScale.y * 2),
                J = this.mouseMesh.material.uniforms;
            J.force.value.set(Q, B), J.center.value.set(C, K), J.scale.value.set(A.cursor_size, A.cursor_size), super.update()
        }
    }
});
var GO = `precision highp float;
uniform sampler2D velocity;
uniform sampler2D velocity_new;
uniform float v;
uniform vec2 px;
uniform float dt;

varying vec2 uv;

void main(){
    
    vec2 old = texture2D(velocity, uv).xy;
    vec2 new0 = texture2D(velocity_new, uv + vec2(px.x * 2.0, 0)).xy;
    vec2 new1 = texture2D(velocity_new, uv - vec2(px.x * 2.0, 0)).xy;
    vec2 new2 = texture2D(velocity_new, uv + vec2(0, px.y * 2.0)).xy;
    vec2 new3 = texture2D(velocity_new, uv - vec2(0, px.y * 2.0)).xy;

    vec2 new = 4.0 * old + v * dt * (new0 + new1 + new2 + new3);
    new /= 4.0 * (1.0 + v * dt);
    
    gl_FragColor = vec4(new, 0.0, 0.0);
}
`;
var UO = () => {};
var v9;
var ZO = VA(() => {
    KK();
    UO();
    JK();
    VQ();
    v9 = class v9 extends zE {
        constructor(A) {
            super({
                material: {
                    vertexShader: AI,
                    fragmentShader: GO,
                    uniforms: {
                        boundarySpace: {
                            value: A.boundarySpace
                        },
                        velocity: {
                            value: A.src.texture
                        },
                        velocity_new: {
                            value: A.dst_.texture
                        },
                        v: {
                            value: A.viscous
                        },
                        px: {
                            value: A.cellScale
                        },
                        dt: {
                            value: A.dt
                        }
                    }
                },
                output: A.dst,
                output0: A.dst_,
                output1: A.dst
            });
            this.init()
        }
        update({
            viscous: A,
            iterations: Q,
            dt: B
        }) {
            let E, I;
            this.uniforms.v.value = A;
            for (var C = 0; C < Q; C++) {
                if (C % 2 == 0) E = this.props.output0, I = this.props.output1;
                else E = this.props.output1, I = this.props.output0;
                this.uniforms.velocity_new.value = E.texture, this.props.output = I, this.uniforms.dt.value = B, super.update()
            }
            return I
        }
    }
});
var qO = `precision highp float;
uniform sampler2D velocity;
uniform float dt;
uniform vec2 px;
varying vec2 uv;

void main(){
    float x0 = texture2D(velocity, uv-vec2(px.x, 0)).x;
    float x1 = texture2D(velocity, uv+vec2(px.x, 0)).x;
    float y0 = texture2D(velocity, uv-vec2(0, px.y)).y;
    float y1 = texture2D(velocity, uv+vec2(0, px.y)).y;
    float divergence = (x1-x0 + y1-y0) / 2.0;

    gl_FragColor = vec4(divergence / dt);
}
`;
var HO = () => {};
var g9;
var YO = VA(() => {
    KK();
    HO();
    JK();
    g9 = class g9 extends zE {
        constructor(A) {
            super({
                material: {
                    vertexShader: AI,
                    fragmentShader: qO,
                    uniforms: {
                        boundarySpace: {
                            value: A.boundarySpace
                        },
                        velocity: {
                            value: A.src.texture
                        },
                        px: {
                            value: A.cellScale
                        },
                        dt: {
                            value: A.dt
                        }
                    }
                },
                output: A.dst
            });
            this.init()
        }
        update({
            vel: A
        }) {
            this.uniforms.velocity.value = A.texture, super.update()
        }
    }
});
var DO = `precision highp float;
uniform sampler2D pressure;
uniform sampler2D divergence;
uniform float straightness;
uniform vec2 px;
varying vec2 uv;

void main(){    
    
    float p0 = texture2D(pressure, uv+vec2(px.x * 2.0,  0)).r;
    float p1 = texture2D(pressure, uv-vec2(px.x * 2.0, 0)).r;
    float p2 = texture2D(pressure, uv+vec2(0, px.y * 2.0 )).r;
    float p3 = texture2D(pressure, uv-vec2(0, px.y * 2.0 )).r;
    float div = texture2D(divergence, uv).r;
    
    float newP = (p0 + p1 + p2 + p3) / (4.0 + straightness) - div;
    gl_FragColor = vec4(newP);
}
`;
var $O = () => {};
var u9;
var WO = VA(() => {
    KK();
    $O();
    JK();
    u9 = class u9 extends zE {
        constructor(A) {
            super({
                material: {
                    vertexShader: AI,
                    fragmentShader: DO,
                    uniforms: {
                        boundarySpace: {
                            value: A.boundarySpace
                        },
                        pressure: {
                            value: A.dst_.texture
                        },
                        divergence: {
                            value: A.src.texture
                        },
                        px: {
                            value: A.cellScale
                        },
                        straightness: {
                            value: A.straightness
                        }
                    }
                },
                output: A.dst,
                output0: A.dst_,
                output1: A.dst
            });
            this.init()
        }
        update({
            iterations: A
        }) {
            let Q, B;
            for (var E = 0; E < A; E++) {
                if (E % 2 == 0) Q = this.props.output0, B = this.props.output1;
                else Q = this.props.output1, B = this.props.output0;
                this.uniforms.pressure.value = Q.texture, this.props.output = B, super.update()
            }
            return B
        }
    }
});
var XO = `precision highp float;
uniform sampler2D pressure;
uniform sampler2D velocity;
uniform vec2 px;
uniform float dt;
varying vec2 uv;

void main(){
    float step = 1.0;

    float p0 = texture2D(pressure, uv+vec2(px.x * step, 0)).r;
    float p1 = texture2D(pressure, uv-vec2(px.x * step, 0)).r;
    float p2 = texture2D(pressure, uv+vec2(0, px.y * step)).r;
    float p3 = texture2D(pressure, uv-vec2(0, px.y * step)).r;

    vec2 v = texture2D(velocity, uv).xy;
    vec2 gradP = vec2(p0 - p1, p2 - p3) * 0.5;
    v = v - gradP * dt;
    gl_FragColor = vec4(v, 0.0, 1.0);
}
`;
var RO = () => {};
var d9;
var FO = VA(() => {
    KK();
    RO();
    JK();
    d9 = class d9 extends zE {
        constructor(A) {
            super({
                material: {
                    vertexShader: AI,
                    fragmentShader: XO,
                    uniforms: {
                        boundarySpace: {
                            value: A.boundarySpace
                        },
                        pressure: {
                            value: A.src_p.texture
                        },
                        velocity: {
                            value: A.src_v.texture
                        },
                        px: {
                            value: A.cellScale
                        },
                        dt: {
                            value: A.dt
                        }
                    }
                },
                output: A.dst
            });
            this.init()
        }
        update({
            vel: A,
            pressure: Q
        }) {
            this.uniforms.velocity.value = A.texture, this.uniforms.pressure.value = Q.texture, super.update()
        }
    }
});
class p9 {
    constructor(A) {
        this.gl = new RQ, this.props = A, this.fbos = {
            vel_0: null,
            vel_1: null,
            vel_viscous0: null,
            vel_viscous1: null,
            div: null,
            pressure_0: null,
            pressure_1: null
        }, this.options = {
            iterations_poisson: 4,
            iterations_viscous: 4,
            dissipation: 0.96,
            mouse_force: 50,
            resolution: 0.1,
            cursor_size: 18,
            straightness: 1,
            viscous: 30,
            isBounce: !1,
            dt: 0.014,
            isViscous: !1,
            BFECC: !0
        }, this.fboSize = new kA, this.cellScale = new kA, this.boundarySpace = new kA, this.init()
    }
    init() {
        this.calcSize(), this.createAllFBO(), this.createShaderPass()
    }
    createAllFBO() {
        let A = /(iPad|iPhone|iPod)/g.test(navigator.userAgent) ? vB : J0;
        for (let Q in this.fbos) this.fbos[Q] = new XB(this.fboSize.x, this.fboSize.y, {
            type: A
        })
    }
    createShaderPass() {
        this.advection = new b9({
            cellScale: this.cellScale,
            fboSize: this.fboSize,
            dt: this.options.dt,
            src: this.fbos.vel_0,
            dst: this.fbos.vel_1,
            dissipation: this.options.dissipation
        }), this.externalForce = new h9({
            cellScale: this.cellScale,
            cursor_size: this.options.cursor_size,
            dst: this.fbos.vel_1
        }), this.viscous = new v9({
            cellScale: this.cellScale,
            boundarySpace: this.boundarySpace,
            viscous: this.options.viscous,
            src: this.fbos.vel_1,
            dst: this.fbos.vel_viscous1,
            dst_: this.fbos.vel_viscous0,
            dt: this.options.dt
        }), this.divergence = new g9({
            cellScale: this.cellScale,
            boundarySpace: this.boundarySpace,
            src: this.fbos.vel_viscous0,
            dst: this.fbos.div,
            dt: this.options.dt
        }), this.poisson = new u9({
            cellScale: this.cellScale,
            boundarySpace: this.boundarySpace,
            straightness: this.options.straightness,
            src: this.fbos.div,
            dst: this.fbos.pressure_1,
            dst_: this.fbos.pressure_0
        }), this.pressure = new d9({
            cellScale: this.cellScale,
            boundarySpace: this.boundarySpace,
            src_p: this.fbos.pressure_0,
            src_v: this.fbos.vel_viscous0,
            dst: this.fbos.vel_0,
            dt: this.options.dt
        })
    }
    calcSize() {
        let A = Math.round(this.options.resolution * this.gl.sizes.width),
            Q = Math.round(this.options.resolution * this.gl.sizes.height),
            B = 1 / A,
            E = 1 / Q;
        B *= A / (1100 * this.options.resolution), E *= A / (1100 * this.options.resolution), this.cellScale.set(B, E), this.fboSize.set(A, Q)
    }
    resize() {
        this.calcSize();
        for (let A in this.fbos) this.fbos[A].setSize(this.fboSize.x, this.fboSize.y)
    }
    update() {
        if (this.options.isBounce) this.boundarySpace.set(0, 0);
        else this.boundarySpace.copy(this.cellScale);
        this.advection.update(this.options), this.externalForce.update({
            cursor_size: this.options.cursor_size,
            mouse_force: this.options.mouse_force,
            cellScale: this.cellScale
        });
        let A = this.fbos.vel_1;
        if (this.options.isViscous) A = this.viscous.update({
            viscous: this.options.viscous,
            iterations: this.options.iterations_viscous,
            dt: this.options.dt
        });
        this.divergence.update({
            vel: A
        });
        let Q = this.poisson.update({
            iterations: this.options.iterations_poisson
        });
        this.pressure.update({
            vel: A,
            pressure: Q
        })
    }
}
var NO = VA(() => {
    VQ();
    BO();
    JO();
    ZO();
    YO();
    WO();
    FO();
    _B()
});
class m9 {
    constructor() {
        this.gl = new RQ, this.simulation = new p9, this.mouse = new wJ, this.mouse.init(), this.scene = new rB, this.camera = new jI, this.output = new OQ(new QB(2, 2), new _I({
            vertexShader: AI,
            fragmentShader: tN,
            uniforms: {
                velocity: {
                    value: this.simulation.fbos.vel_0.texture
                },
                boundarySpace: {
                    value: new kA
                }
            }
        })), this.scene.add(this.output), this.sourceTarget = new n8(this.gl.sizes.width, this.gl.sizes.height, {
            samples: 2
        }), this.clock = new E8, this.delta = 0, this.interval = 0.016666666666666666
    }
    resize() {
        this.simulation.resize()
    }
    update() {
        if (this.delta += this.clock.getDelta(), this.delta > this.interval) this.mouse.update(), this.simulation.update(), this.gl.renderer.instance.setRenderTarget(this.sourceTarget), this.gl.renderer.instance.render(this.scene, this.camera), this.delta = this.delta % this.interval
    }
}
var OO = VA(() => {
    VQ();
    _B();
    KK();
    rN();
    NO();
    YY()
});
class c9 {
    constructor() {
        this.gl = new RQ, this.isMoving = !0, this.isMovingPrevious = !0, this.idleInterval = null, this.moveTimeout = null, this.elapsed = 0, this.progress = new kA, this.idleCursor = new kA, this.initial = setTimeout(() => {
            this.isMoving = !1
        }, 2500), document.addEventListener("mousemove", this.move.bind(this)), document.addEventListener("touchmove", this.move.bind(this)), this.setTimeline()
    }
    setTimeline() {
        this.timeline = m.timeline({
            paused: !0,
            repeat: -1,
            repeatDelay: 3
        }), this.timeline.fromTo(this.progress, {
            y: 0
        }, {
            y: 1,
            duration: 2.5,
            ease: "none"
        }, 0), this.timeline.fromTo(this.progress, {
            x: 0
        }, {
            x: 1,
            duration: 2.5,
            ease: "power1.inOut"
        }, 0), this.timeline.fromTo(this.progress, {
            y: 1
        }, {
            y: 0,
            duration: 2.5,
            ease: "none"
        }, 4), this.timeline.fromTo(this.progress, {
            x: 1
        }, {
            x: 0,
            duration: 2.5,
            ease: "power1.inOut"
        }, 4)
    }
    move() {
        this.isMoving = !0, clearTimeout(this.initial), clearInterval(this.idleInterval), clearTimeout(this.moveTimeout), this.moveTimeout = setTimeout(() => {
            this.isMoving = !1
        }, 2000)
    }
    update() {
        if (!this.isMoving) this.idleCursor.x = -Math.cos(this.progress.x * Math.PI * 4) * 0.75, this.idleCursor.y = Math.cos(this.progress.y * Math.PI) * 0.5;
        if (this.isMoving != this.isMovingPrevious)
            if (!this.isMoving) this.timeline.seek(0), this.timeline.play();
            else this.timeline.pause();
        this.isMovingPrevious = this.isMoving
    }
}
var MO = VA(() => {
    LB();
    VQ();
    _B()
});
var VO = `


vec4 permute(vec4 x){return mod(((x*34.0)+1.0)*x, 289.0);}
vec4 taylorInvSqrt(vec4 r){return 1.79284291400159 - 0.85373472095314 * r;}

float snoise(vec3 v){ 
  const vec2  C = vec2(1.0/6.0, 1.0/3.0) ;
  const vec4  D = vec4(0.0, 0.5, 1.0, 2.0);


  vec3 i  = floor(v + dot(v, C.yyy) );
  vec3 x0 =   v - i + dot(i, C.xxx) ;


  vec3 g = step(x0.yzx, x0.xyz);
  vec3 l = 1.0 - g;
  vec3 i1 = min( g.xyz, l.zxy );
  vec3 i2 = max( g.xyz, l.zxy );

  
  vec3 x1 = x0 - i1 + 1.0 * C.xxx;
  vec3 x2 = x0 - i2 + 2.0 * C.xxx;
  vec3 x3 = x0 - 1. + 3.0 * C.xxx;


  i = mod(i, 289.0 ); 
  vec4 p = permute( permute( permute( 
             i.z + vec4(0.0, i1.z, i2.z, 1.0 ))
           + i.y + vec4(0.0, i1.y, i2.y, 1.0 )) 
           + i.x + vec4(0.0, i1.x, i2.x, 1.0 ));



  float n_ = 1.0/7.0; 
  vec3  ns = n_ * D.wyz - D.xzx;

  vec4 j = p - 49.0 * floor(p * ns.z *ns.z);  

  vec4 x_ = floor(j * ns.z);
  vec4 y_ = floor(j - 7.0 * x_ );    

  vec4 x = x_ *ns.x + ns.yyyy;
  vec4 y = y_ *ns.x + ns.yyyy;
  vec4 h = 1.0 - abs(x) - abs(y);

  vec4 b0 = vec4( x.xy, y.xy );
  vec4 b1 = vec4( x.zw, y.zw );

  vec4 s0 = floor(b0)*2.0 + 1.0;
  vec4 s1 = floor(b1)*2.0 + 1.0;
  vec4 sh = -step(h, vec4(0.0));

  vec4 a0 = b0.xzyw + s0.xzyw*sh.xxyy ;
  vec4 a1 = b1.xzyw + s1.xzyw*sh.zzww ;

  vec3 p0 = vec3(a0.xy,h.x);
  vec3 p1 = vec3(a0.zw,h.y);
  vec3 p2 = vec3(a1.xy,h.z);
  vec3 p3 = vec3(a1.zw,h.w);


  vec4 norm = taylorInvSqrt(vec4(dot(p0,p0), dot(p1,p1), dot(p2, p2), dot(p3,p3)));
  p0 *= norm.x;
  p1 *= norm.y;
  p2 *= norm.z;
  p3 *= norm.w;


  vec4 m = max(0.6 - vec4(dot(x0,x0), dot(x1,x1), dot(x2,x2), dot(x3,x3)), 0.0);
  m = m * m;
  return 42.0 * dot( m*m, vec4( dot(p0,x0), dot(p1,x1), 
                                dot(p2,x2), dot(p3,x3) ) );
}`;
var LO = () => {};

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L35706-L35888 =====
// World / Time / Sizes (35706-35888); World.destroy scene.remove(name) no-op is load-bearing (Q13)
class l9 {
    constructor() {
        if (this.gl = new RQ, this.gl.isDebug) this.setDebug();
        TQ.simplex = VO, this.fluidCursor = new m9, this.idleState = new c9, this.backgroundNoise = new f9, this.selectors = [], this.scenes = []
    }
    add() {
        this.selectors = document.querySelectorAll("[data-gl]"), this.selectors.forEach((A, Q) => {
            if (A.dataset.gl === "head") this.scenes.push(new O9({
                dom: A
            }));
            else if (A.dataset.gl === "tracks") this.scenes.push(new k9({
                dom: A
            }));
            else if (A.dataset.gl === "background") this.scenes.push(new w9({
                dom: A
            }));
            else if (A.dataset.gl === "carousel") this.scenes.push(new _9({
                dom: A
            }));
            else if (A.dataset.gl === "helmet-scroll") this.scenes.push(new y9({
                dom: A
            }));
            else if (A.dataset.gl === "not-found") console.log(A), this.scenes.push(new x9({
                dom: A
            }))
        });
        for (let A in this.scenes) this.gl.scene.add(this.scenes[A].renderPlane.mesh)
    }
    destroy() {
        if (this.gl.isDebug) console.log("Before destroy:", this.gl.renderer.instance.info);
        for (let A in this.scenes) {
            if (this.gl.scene.remove(this.scenes[A].renderPlane.mesh), this.scenes[A].renderTarget) this.scenes[A].renderTarget.dispose();
            if (this.scenes[A].renderPlane.mesh.geometry.dispose(), this.scenes[A].renderPlane.mesh.material.dispose(), this.scenes[A].scene) this.scenes[A].scene.traverse((Q) => {
                if (this.scenes[A].scene.remove(Q.name), !Q.isMesh) return;
                if (Q.geometry.dispose(), Q.material.isMaterial) this.cleanMaterial(Q.material);
                else
                    for (let B of Q.material) this.cleanMaterial(B)
            })
        }
        if (this.selectors = [], this.scenes = [], this.gl.renderer.instance.setRenderTarget(null), this.gl.renderer.instance.clear(), this.gl.isDebug) this.removeDebug();
        if (this.gl.isDebug) console.log("After destroy:", this.gl.renderer.instance.info)
    }
    cleanMaterial(A) {
        A.dispose();
        for (let Q of Object.keys(A)) {
            let B = A[Q];
            if (B && typeof B === "object" && "minFilter" in B) B.dispose()
        }
    }
    setDebug() {
        this.headSceneDebugFolder = this.gl.debug.gui.addFolder("Head Scene"), this.tracksSceneDebugFolder = this.gl.debug.gui.addFolder("Tracks Scene"), this.helmetDebugFolder = this.headSceneDebugFolder.addFolder("Helmet"), this.helmetScrollSceneDebugFolder = this.gl.debug.gui.addFolder("Helmet Scroll Scene"), this.notFoundSceneDebugFolder = this.gl.debug.gui.addFolder("Not Found Scene"), this.compositionDebugFolder = this.headSceneDebugFolder.addFolder("Composition")
    }
    removeDebug() {
        this.gl.debug.gui.removeFolder(this.headSceneDebugFolder), this.gl.debug.gui.removeFolder(this.tracksSceneDebugFolder), this.gl.debug.gui.removeFolder(this.helmetScrollSceneDebugFolder), this.gl.debug.gui.removeFolder(this.notFoundSceneDebugFolder)
    }
    setScenePlanesDimensions() {
        this.selectors.forEach((A, Q) => {
            if (!this.scenes[Q].isRendering) return;
            if (A.dataset.gl != "helmet-scroll") {
                let B = A.getBoundingClientRect();
                this.scenes[Q].renderPlane.bounds = B
            }
            this.scenes[Q].setScenePlaneDimensions()
        })
    }
    resize() {
        this.fluidCursor.resize(), this.backgroundNoise.resize();
        for (let A in this.scenes) this.scenes[A].resize()
    }
    update() {
        this.backgroundNoise.update(), this.idleState.update(), this.fluidCursor.update();
        for (let A in this.scenes) this.scenes[A].update()
    }
}
var zO = VA(() => {
    VQ();
    _B();
    t5();
    HN();
    qN();
    oN();
    iN();
    nN();
    aN();
    OO();
    MO();
    LO()
});
class PJ {
    constructor() {
        this.callbacks = {}, this.callbacks.base = {}
    }
    on(A, Q) {
        if (typeof A === "undefined" || A === "") return console.warn("wrong names"), !1;
        if (typeof Q === "undefined") return console.warn("wrong callback"), !1;
        return this.resolveNames(A).forEach((E) => {
            let I = this.resolveName(E);
            if (!(this.callbacks[I.namespace] instanceof Object)) this.callbacks[I.namespace] = {};
            if (!(this.callbacks[I.namespace][I.value] instanceof Array)) this.callbacks[I.namespace][I.value] = [];
            this.callbacks[I.namespace][I.value].push(Q)
        }), this
    }
    off(A) {
        if (typeof A === "undefined" || A === "") return console.warn("wrong name"), !1;
        return this.resolveNames(A).forEach((B) => {
            let E = this.resolveName(B);
            if (E.namespace !== "base" && E.value === "") delete this.callbacks[E.namespace];
            else if (E.namespace === "base") {
                for (let I in this.callbacks)
                    if (this.callbacks[I] instanceof Object && this.callbacks[I][E.value] instanceof Array) {
                        if (delete this.callbacks[I][E.value], Object.keys(this.callbacks[I]).length === 0) delete this.callbacks[I]
                    }
            } else if (this.callbacks[E.namespace] instanceof Object && this.callbacks[E.namespace][E.value] instanceof Array) {
                if (delete this.callbacks[E.namespace][E.value], Object.keys(this.callbacks[E.namespace]).length === 0) delete this.callbacks[E.namespace]
            }
        }), this
    }
    trigger(A, Q) {
        if (typeof A === "undefined" || A === "") return console.warn("wrong name"), !1;
        let B = null,
            E = null,
            I = !(Q instanceof Array) ? [] : Q,
            C = this.resolveNames(A);
        if (C = this.resolveName(C[0]), C.namespace === "base") {
            for (let K in this.callbacks)
                if (this.callbacks[K] instanceof Object && this.callbacks[K][C.value] instanceof Array) this.callbacks[K][C.value].forEach(function(J) {
                    if (E = J.apply(this, I), typeof B === "undefined") B = E
                })
        } else if (this.callbacks[C.namespace] instanceof Object) {
            if (C.value === "") return console.warn("wrong name"), this;
            this.callbacks[C.namespace][C.value].forEach(function(K) {
                if (E = K.apply(this, I), typeof B === "undefined") B = E
            })
        }
        return B
    }
    resolveNames(A) {
        let Q = A;
        return Q = Q.replace(/[^a-zA-Z0-9 ,/.]/g, ""), Q = Q.replace(/[,/]+/g, " "), Q = Q.split(" "), Q
    }
    resolveName(A) {
        let Q = {},
            B = A.split(".");
        if (Q.original = A, Q.value = B[0], Q.namespace = "base", B.length > 1 && B[1] !== "") Q.namespace = B[1];
        return Q
    }
}
var s9;
var kO = VA(() => {
    VQ();
    s9 = class s9 extends PJ {
        constructor() {
            super();
            this.clock = new E8, this.elapsed = 0, this.delta = 0, window.requestAnimationFrame(() => {
                this.tick()
            })
        }
        tick() {
            this.delta = Math.min(this.clock.getDelta(), 0.03333333333333333) * 100, this.elapsed = this.clock.getElapsedTime(), this.trigger("tick"), window.requestAnimationFrame(() => {
                this.tick()
            })
        }
        getVariantAccordingToTime() {
            let A = new Date().getHours(),
                Q = "";
            if (A >= 6 && A < 18) Q = "Lime";
            else Q = "Dark";
            return Q
        }
    }
});
var o9;
var wO = VA(() => {
    o9 = class o9 extends PJ {
        constructor() {
            super();
            this.width = window.innerWidth, this.height = window.innerHeight, this.pixelRatio = this.width > 768 ? Math.min(window.devicePixelRatio, 1.25) : Math.min(window.devicePixelRatio, 2), window.addEventListener("resize", () => {
                this.width = window.innerWidth, this.height = window.innerHeight, this.pixelRatio = this.width > 768 ? Math.min(window.devicePixelRatio, 1.25) : Math.min(window.devicePixelRatio, 2), this.trigger("resize")
            })
        }
    }
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L38004-L38126 =====
// Debug / Mouse / Chunks (38004-38126)
class t9 {
    constructor() {
        this.gl = new RQ, this.gui = new sO({
            width: 300
        }), this.stats = new aO({
            trackGPU: !0
        }), this.stats.init(this.gl.renderer.instance), document.body.appendChild(this.stats.dom)
    }
    update() {
        this.stats.update()
    }
}
var tO = VA(() => {
    oO();
    rO();
    _B()
});
class e9 {
    constructor(A) {
        this.gl = new RQ, this.dom = A, this.isMouseHolding = !1, this.isMouseMoved = !1, this.default = new kA, this.normalized = {
            current: new kA,
            previous: new kA
        }, this.direction = new kA, this.pace = {
            default: 0,
            separated: new kA
        }, this.drag = {
            start: new kA,
            distance: 0,
            side: "left",
            pace: {
                default: 0,
                separated: new kA
            }
        }, this.dom.addEventListener("mousemove", this.mousemove.bind(this)), this.dom.addEventListener("touchmove", this.touchmove.bind(this)), this.dom.addEventListener("mousedown", this.down.bind(this)), this.dom.addEventListener("touchstart", this.down.bind(this)), this.dom.addEventListener("mouseup", this.up.bind(this)), this.dom.addEventListener("touchend", this.up.bind(this))
    }
    mousemove(A) {
        if (this.isMouseMoved = !0, this.default.x = A.clientX, this.default.y = A.clientY, this.normalized.current.x = A.clientX / this.gl.sizes.width * 2 - 1, this.normalized.current.y = -(A.clientY / this.gl.sizes.height) * 2 + 1, this.isMouseHolding)
            if (this.drag.distance = this.drag.start.distanceTo(this.default), this.drag.start.x < this.default.x) this.drag.side = "right";
            else this.drag.side = "left"
    }
    touchmove(A) {
        if (A.touches) this.isMouseMoved = !0, this.normalized.current.x = A.touches[0].pageX / this.gl.sizes.width * 2 - 1, this.normalized.current.y = -(A.touches[0].pageY / this.gl.sizes.height) * 2 + 1
    }
    down(A) {
        if (this.isMouseHolding = !0, this.drag.start.copy(this.default), A.touches) this.normalized.current.x = A.touches[0].pageX / this.gl.sizes.width * 2 - 1, this.normalized.current.y = -(A.touches[0].pageY / this.gl.sizes.height) * 2 + 1
    }
    up(A) {
        this.isMouseHolding = !1
    }
    update() {
        if (this.pace.default = this.normalized.current.distanceTo(this.normalized.previous), this.pace.separated.x = this.normalized.current.x - this.normalized.previous.x, this.pace.separated.y = this.normalized.current.y - this.normalized.previous.y, this.direction.subVectors(this.normalized.current, this.normalized.previous).normalize(), this.isMouseHolding) this.drag.pace.default = this.normalized.current.distanceTo(this.normalized.previous), this.drag.pace.separated.x = this.normalized.current.x - this.normalized.previous.x, this.drag.pace.separated.y = this.normalized.current.y - this.normalized.previous.y;
        else this.drag.pace.default = 0, this.drag.pace.separated.x = 0, this.drag.pace.separated.y = 0;
        this.normalized.previous.copy(this.normalized.current)
    }
    createEasedMovement(A) {
        let Q = new kA;
        return {
            value: Q,
            update: (E) => {
                Q.x = jB.damp(Q.x, this.default.x, A, E), Q.y = jB.damp(Q.y, this.default.y, A, E)
            }
        }
    }
    createEasedNormalized(A) {
        let Q = new kA;
        return {
            value: Q,
            update: (E) => {
                Q.x = jB.damp(Q.x, this.normalized.current.x, A, E), Q.y = jB.damp(Q.y, this.normalized.current.y, A, E)
            }
        }
    }
    createEasedDirection(A) {
        let Q = new kA;
        return {
            value: Q,
            update: (E) => {
                Q.x = jB.damp(Q.x, this.direction.x, A, E), Q.y = jB.damp(Q.y, this.direction.y, A, E)
            }
        }
    }
    createEasedPace(A) {
        let Q = {
            default: 0,
            separated: new kA
        };
        return {
            value: Q,
            update: (E) => {
                Q.default = jB.damp(Q.default, this.pace.default, A, E), Q.separated.x = jB.damp(Q.separated.x, this.pace.separated.x, A, E), Q.separated.y = jB.damp(Q.separated.y, this.pace.separated.y, A, E)
            }
        }
    }
    createEasedDrag(A) {
        let Q = {
            distance: 0,
            pace: {
                default: 0,
                separated: new kA
            }
        };
        return {
            value: Q,
            update: (E) => {
                Q.distance = jB.damp(Q.distance, this.drag.distance, A, E), Q.pace.default = jB.damp(Q.pace.default, this.drag.pace.default, A, E), Q.pace.separated.x = jB.damp(Q.pace.separated.x, this.drag.pace.separated.x, A, E), Q.pace.separated.y = jB.damp(Q.pace.separated.y, this.drag.pace.separated.y, A, E)
            }
        }
    }
}
var eO = VA(() => {
    VQ();
    _B()
});
class AZ {
    constructor() {
        TQ.rotateUV = `
      vec2 rotateUV(vec2 uv, float rotation) {
        mat2 m = mat2(cos(rotation), -sin(rotation), sin(rotation), cos(rotation));
        return m * uv;
      }
    `
    }
}

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L41231-L41534 =====
// Assets manifest + RQ GL app (41231-41534): webp vs ktx2 fork on innerWidth (Q12)
class BZ {
    constructor() {
        this.gl = new RQ, this.dracoLoader = new wY, this.dracoLoader.setDecoderPath(window.landoGL.assets.draco), this.gltfLoader = new yY, this.gltfLoader.setDRACOLoader(this.dracoLoader), this.rgbeLoader = new fY, this.textureLoader = new OJ, this.ktx2TextureLoader = new hE, this.ktx2TextureLoader.setTranscoderPath(window.landoGL.assets.ktx2), this.ktx2TextureLoader.detectSupport(this.gl.renderer.instance), this.fontLoader = new bY, this.models = {
            head: null,
            helmet: null,
            tracks: null,
            disco: null
        }, this.fonts = {
            brier: {
                atlas: null,
                json: null
            },
            mona: {
                atlas: null,
                json: null
            }
        }, this.textures = {
            head: {
                diffuse: null,
                shadow: {
                    default: null,
                    softerEdit: null,
                    toZipEdit: null
                },
                depth: null,
                alpha: null,
                normal: null,
                roughness: null
            },
            helmet: {
                diffuseLime: null,
                diffuseDark: null,
                diffuseGrid: null,
                diffuseDisco: null,
                diffuseGoogle: null,
                roughness: null,
                metallic: null
            },
            glass: {
                base: null,
                normal: null,
                roughness: null,
                metallic: null
            },
            plastic: {
                matcap: null
            },
            disco: {
                matcap: null,
                mask: null,
                lensFlare: null
            },
            noise: {
                texture: null
            },
            matcaps: {
                track: null
            },
            notFound: {
                diffuse: null
            }
        }, this.hdri = {
            dark: null,
            light: null,
            faded: null
        }, this.activeTextureLoader = window.innerWidth > 991 ? this.customTextureLoader : this.customKtx2TextureLoader
    }
    customTextureLoader(A, Q) {
        return new Promise((B) => {
            this.textureLoader.load(A, (E) => {
                B(), Q(E)
            }, void 0, (E) => {
                console.error(E)
            })
        })
    }
    customKtx2TextureLoader(A, Q) {
        return new Promise((B) => {
            this.ktx2TextureLoader.load(A, (E) => {
                B(), Q(E)
            }, void 0, (E) => {
                console.error(E)
            })
        })
    }
    customModelLoader(A, Q) {
        return new Promise((B) => {
            this.gltfLoader.load(A, (E) => {
                B(), Q(E)
            }, void 0, (E) => {
                console.error(E)
            })
        })
    }
    customHdriLoader(A, Q) {
        return new Promise((B) => {
            this.rgbeLoader.load(A, (E) => {
                B(), Q(E)
            }, void 0, (E) => {
                console.error(E)
            })
        })
    }
    customFontLoader(A, Q) {
        return new Promise((B) => {
            this.fontLoader.load(A, (E) => {
                B(), Q(E)
            }, void 0, (E) => {
                console.error(E)
            })
        })
    }
    setTexturesParams() {
        if (this.textures.head.diffuse.minFilter = K0, this.textures.head.diffuse.magFilter = K0, this.textures.head.shadow.softerEdit.minFilter = K0, this.textures.head.shadow.softerEdit.magFilter = K0, this.gl.isDebug) this.textures.head.shadow.default.minFilter = K0, this.textures.head.shadow.default.magFilter = K0, this.textures.head.shadow.toZipEdit.minFilter = K0, this.textures.head.shadow.toZipEdit.magFilter = K0;
        if (this.textures.head.depth.colorSpace = VB, this.textures.head.diffuse.colorSpace = VB, this.textures.head.shadow.softerEdit.colorSpace = VB, this.gl.isDebug) this.textures.head.shadow.default.colorSpace = VB, this.textures.head.shadow.toZipEdit.colorSpace = VB;
        this.textures.helmet.diffuseLime.colorSpace = VB, this.textures.helmet.diffuseDark.colorSpace = VB, this.textures.helmet.diffuseGrid.colorSpace = VB, this.textures.helmet.diffuseDisco.colorSpace = VB, this.textures.helmet.diffuseGoogle.colorSpace = VB, this.textures.glass.base.colorSpace = VB, this.textures.matcaps.track.colorSpace = VB;
        for (let A in this.textures)
            if (A != "head")
                for (let Q in this.textures[A]) this.textures[A][Q].flipY = !1, this.textures[A][Q].minFilter = K0, this.textures[A][Q].magFilter = K0, this.textures[A][Q].wrapS = DC, this.textures[A][Q].wrapT = DC;
        this.textures.notFound.diffuse.flipY = !0
    }
    setHDRIParams() {
        if (this.hdri.light.mapping = $I, this.hdri.faded.mapping = $I, this.gl.isDebug) this.hdri.dark.mapping = $I
    }
    load() {
        return this.promises = [], new Promise(async (A) => {
            if (this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.head.diffuse, (Q) => {
                    this.textures.head.diffuse = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.head.depth, (Q) => {
                    this.textures.head.depth = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.head.alpha, (Q) => {
                    this.textures.head.alpha = Q
                })), this.promises.push(this.customTextureLoader(window.landoGL.assets.textures.head.normal, (Q) => {
                    this.textures.head.normal = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.head.shadow.default, (Q) => {
                    this.textures.head.shadow.default = Q
                })), this.gl.isDebug) this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.head.shadow.default, (Q) => {
                this.textures.head.shadow.default = Q
            }));
            if (this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.head.shadow.softerEdit, (Q) => {
                    this.textures.head.shadow.softerEdit = Q
                })), this.gl.isDebug) this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.head.shadow.toZipEdit, (Q) => {
                this.textures.head.shadow.toZipEdit = Q
            }));
            if (this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.helmet.diffuseLime, (Q) => {
                    this.textures.helmet.diffuseLime = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.helmet.diffuseDark, (Q) => {
                    this.textures.helmet.diffuseDark = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.helmet.diffuseGrid, (Q) => {
                    this.textures.helmet.diffuseGrid = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.helmet.diffuseDisco, (Q) => {
                    this.textures.helmet.diffuseDisco = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.helmet.diffuseGoogle, (Q) => {
                    this.textures.helmet.diffuseGoogle = Q
                })), this.promises.push(this.customTextureLoader(window.landoGL.assets.textures.helmet.normal, (Q) => {
                    this.textures.helmet.normal = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.helmet.roughness, (Q) => {
                    this.textures.helmet.roughness = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.helmet.metallic, (Q) => {
                    this.textures.helmet.metallic = Q
                })), this.promises.push(this.customTextureLoader(window.landoGL.assets.textures.disco.matcap, (Q) => {
                    this.textures.disco.matcap = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.disco.mask, (Q) => {
                    this.textures.disco.mask = Q
                })), this.promises.push(this.customTextureLoader(window.landoGL.assets.textures.disco.lensFlare, (Q) => {
                    this.textures.disco.lensFlare = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.glass.base, (Q) => {
                    this.textures.glass.base = Q
                })), this.promises.push(this.customTextureLoader(window.landoGL.assets.textures.glass.normal, (Q) => {
                    this.textures.glass.normal = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.glass.roughness, (Q) => {
                    this.textures.glass.roughness = Q
                })), this.promises.push(this.activeTextureLoader(window.landoGL.assets.textures.glass.metallic, (Q) => {
                    this.textures.glass.metallic = Q
                })), this.promises.push(this.customTextureLoader(window.landoGL.assets.textures.matcaps.track, (Q) => {
                    this.textures.matcaps.track = Q
                })), this.promises.push(this.customTextureLoader(window.landoGL.assets.textures.noise.texture, (Q) => {
                    this.textures.noise.texture = Q
                })), this.promises.push(this.customTextureLoader(window.landoGL.assets.textures.plastic.matcap, (Q) => {
                    this.textures.plastic.matcap = Q
                })), this.promises.push(this.customTextureLoader(window.landoGL.assets.textures.notFound.diffuse, (Q) => {
                    this.textures.notFound.diffuse = Q
                })), this.promises.push(this.customModelLoader(window.landoGL.assets.models.helmet, (Q) => {
                    this.models.helmet = Q
                })), this.promises.push(this.customModelLoader(window.landoGL.assets.models.disco, (Q) => {
                    this.models.disco = Q
                })), this.promises.push(this.customModelLoader(window.landoGL.assets.models.tracks, (Q) => {
                    this.models.tracks = Q
                })), this.promises.push(this.customHdriLoader(window.landoGL.assets.hdri.light, (Q) => {
                    this.hdri.light = Q, this.hdri.light.mapping = $I
                })), this.promises.push(this.customHdriLoader(window.landoGL.assets.hdri.faded, (Q) => {
                    this.hdri.faded = Q, this.hdri.faded.mapping = $I
                })), this.gl.isDebug) this.promises.push(this.customHdriLoader(window.landoGL.assets.hdri.dark, (Q) => {
                this.hdri.dark = Q, this.hdri.dark.mapping = $I
            }));
            this.promises.push(this.customTextureLoader(window.landoGL.assets.fonts.brier.atlas, (Q) => {
                this.fonts.brier.atlas = Q
            })), this.promises.push(this.customTextureLoader(window.landoGL.assets.fonts.mona.atlas, (Q) => {
                this.fonts.mona.atlas = Q
            })), this.promises.push(this.customFontLoader(window.landoGL.assets.fonts.brier.json, (Q) => {
                this.fonts.brier.json = Q
            })), this.promises.push(this.customFontLoader(window.landoGL.assets.fonts.mona.json, (Q) => {
                this.fonts.mona.json = Q
            })), await Promise.all(this.promises), A(), this.setTexturesParams()
        })
    }
}
var tM = VA(() => {
    VQ();
    QM();
    jM();
    _M();
    xM();
    rM();
    _B()
});
class RQ {
    constructor(A) {
        if (G$) return G$;
        G$ = this, this.urlParams = new URLSearchParams(window.location.search), this.params = A, this.isLoaded = !1, this.isDebug = this.urlParams.has("debug"), this.canvas = null, this.time = new s9, this.sizes = new o9, this.mouse = new e9(document), this.shaderChunks = new AZ, this.scene = new rB, this.camera = new Y9, this.renderer = new $9, this.assets = new BZ, this.sizes.on("resize", () => {
            this.resize()
        })
    }
    load() {
        return new Promise((A) => {
            Promise.all([this.loadDOM(), this.assets.load()]).then(() => {
                A()
            })
        })
    }
    init() {
        if (this.renderer.add(), this.isDebug) this.debug = new t9;
        if (this.world = new l9, m.ticker.add(this.update.bind(this), !1, !0), this.world.add(), this.isDebug) this.setDebug();
        this.isLoaded = !0, setTimeout(() => {
            TA.refresh(), this.resize(), console.log("resize issue prevention")
        }, 1000)
    }
    loadAssets() {
        return new Promise((A) => {
            this.assets.loadingManager.onStart = (Q, B, E) => {
                console.log("LOADING MANAGER: Started loading file: " + Q + `.
Loaded ` + B + " of " + E + " files.")
            }, this.assets.loadingManager.onError = (Q) => {
                console.log("LOADING MANAGER: There was an error loading " + Q)
            }, this.assets.loadingManager.onProgress = (Q, B, E) => {
                console.log("LOADING MANAGER: Loading file: " + Q + `.
Loaded ` + B + " of " + E + " files.")
            }, this.assets.loadingManager.onLoad = () => {
                A()
            }
        })
    }
    loadDOM() {
        return new Promise((A) => {
            if (document.readyState === "complete") A();
            else window.addEventListener("load", () => {
                A()
            })
        })
    }
    setDebug() {
        this.debug.gui.add(this.world, "add").name("Add Scenes").onFinishChange(() => {
            this.world.setDebug()
        }), this.debug.gui.add(this.world, "destroy").name("Destroy Scenes")
    }
    update() {
        if (this.isLoaded) {
            if (this.renderer.update(), this.world.update(), this.mouse.update(), this.isDebug) this.debug.stats.update()
        }
    }
    resize() {
        if (this.isLoaded) this.camera.resize(), this.renderer.resize(), this.world.resize()
    }
}
var G$ = null;
var _B = VA(() => {
    VQ();
    LB();
    g5();
    u5();
    zO();
    kO();
    wO();
    tO();
    eO();
    AM();
    tM()
});
async function eM() {
    if (bI) await bI.load()
}
async function AL() {
    if (bI) await bI.init()
}

function yU() {
    if (bI) bI.renderer.add(), bI.world.add()
}

function QL() {
    if (bI) bI.world.destroy(), document.documentElement.classList.remove("gl__is-disco")
}
var vQ = "https://lando.itsoffbrand.io/gl",
    iQ, bI = null;

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L41535-L41750 =====
// EZ landoGL config + WebGL2 gate (41535-41685), q$ page-transition rive (41745-41749)
var EZ = VA(() => {
    AX();
    _B();
    iQ = window.innerWidth > 991 ? "webp" : "ktx2";
    window.landoGL = {
        reveal: 1,
        lenis: {
            velocity: 0,
            instance: null
        },
        bounds: {
            helmetScroll: {
                width: 0,
                height: 0,
                left: 0,
                top: 0
            }
        },
        params: {
            headScene: {
                REVEAL_DURATION: 1.1,
                COLOR_OUTLINE: "#CBCBB9",
                COLOR_FOREGROUND: "#D2FF00",
                COLOR_BACKGROUND: "#F8F8F3",
                COLOR_CURSOR_FOREGROUND: "#CFD2C5",
                COLOR_CURSOR_BACKGROUND: "#E8E8DF",
                COLOR_CURSOR_OUTLINE: "#E8E8DF",
                COLOR_FILTER: "#50593F",
                SCALE: 1,
                SPEED: 0.1,
                THICKNESS: 0.000005,
                OUTLINE: !0,
                SHOW_HELMET_PERMANENTLY: !1,
                DISTORT_SCALE: 1,
                DISTORT_INTENSITY: 0.5,
                NOISE_DETAIL: 3,
                CURSOR_INTENSITY: 0.15,
                CURSOR_SCALE: 3,
                CURSOR_BOUNCE: -0.75,
                REVEAL_SIZE: 25,
                IS_WIREFRAME_ANIMATING: !0,
                VARIANT: "Lime"
            },
            backgroundScene: {
                THICKNESS: 0.000005,
                OUTLINE: !0,
                COLOR_BACKGROUND: "#282C20",
                COLOR_FOREGROUND: "#363B25",
                COLOR_CURSOR_BACKGROUND: "#b2c73a",
                COLOR_CURSOR_FOREGROUND: "#b2c73a"
            },
            carouselScene: {
                TEXT_TOP: "WE DID IT AT HOME WE DID IT AT HOME",
                TEXT_BOTTOM: "A BRITISH GP WEEKEND I WILL REMEMBER FOREVER",
                COLOR_TOP: "#b2c73a",
                COLOR_BOTTOM: "#dde1d2"
            },
            tracksScene: {
                CURRENT: "austin",
                TRANSITION_DURATION: 2,
                AUTOROTATE_SPEED: 0.2
            },
            helmetScrollScene: {
                VARIANT: "Lime",
                PROGRESS: 0,
                REVEAL_OUT_PROGRESS: 0
            },
            notFoundScene: {
                VARIANT: "Lime",
                HELMET_AUTOROTATE_SPEED: 1,
                HELMET_ANGLE: -21
            }
        },
        assets: {
            draco: vQ + "/draco/",
            ktx2: vQ + "/basis/",
            fonts: {
                brier: {
                    atlas: vQ + "/fonts/Brier-Bold-02.webp",
                    json: vQ + "/fonts/Brier-Bold-msdf.json"
                },
                mona: {
                    atlas: vQ + "/fonts/MonaSans-Bold-02.webp",
                    json: vQ + "/fonts/MonaSans-Bold-msdf.json"
                }
            },
            hdri: {
                light: vQ + "/hdri/studio_small_08_1k--light.hdr",
                faded: vQ + "/hdri/studio_small_08_1k--faded.hdr",
                dark: vQ + "/hdri/studio_small_08_1k--dark.hdr"
            },
            models: {
                helmet: vQ + "/models/helmet-21.glb",
                disco: vQ + "/models/disco-02.glb",
                tracks: vQ + "/models/tracks/tracks-05.glb",
                sotd: vQ + "/models/sotd.glb"
            },
            textures: {
                head: {
                    diffuse: vQ + `/textures/head/${iQ}/diffuse.${iQ}`,
                    depth: vQ + `/textures/head/${iQ}/depth.${iQ}`,
                    alpha: vQ + `/textures/head/${iQ}/alpha.${iQ}`,
                    normal: vQ + "/textures/head/webp/normal.webp",
                    roughness: vQ + `/textures/head/${iQ}/roughness.${iQ}`,
                    shadow: {
                        default: vQ + `/textures/head/${iQ}/shadow.${iQ}`,
                        softerEdit: vQ + `/textures/head/${iQ}/shadow-softer-edit.${iQ}`,
                        toZipEdit: vQ + `/textures/head/${iQ}/shadow-to-zip-edit.${iQ}`
                    }
                },
                helmet: {
                    diffuseLime: vQ + `/textures/helmet/${iQ}/gold/Norris_Helmet_mat_BaseColor.${iQ}`,
                    diffuseDark: vQ + `/textures/helmet/${iQ}/gold/Norris_Helmet_mat_BaseColor.${iQ}`,
                    diffuseGrid: vQ + `/textures/helmet/${iQ}/gold/Norris_Helmet_mat_BaseColor.${iQ}`,
                    diffuseDisco: vQ + `/textures/helmet/${iQ}/disco/Norris_Helmet_mat_BaseColor.${iQ}`,
                    diffuseGoogle: vQ + `/textures/helmet/${iQ}/gold/Norris_Helmet_mat_BaseColor.${iQ}`,
                    normal: vQ + "/textures/helmet/webp/Norris_Helmet_mat_Normal.webp",
                    roughness: vQ + `/textures/helmet/${iQ}/Norris_Helmet_mat_Roughness.${iQ}`,
                    metallic: vQ + `/textures/helmet/${iQ}/Norris_Helmet_mat_Metallic.${iQ}`
                },
                glass: {
                    base: vQ + `/textures/glass/${iQ}/Norris_Glass_mat_BaseColor.${iQ}`,
                    normal: vQ + "/textures/glass/webp/Norris_Glass_mat_Normal.webp",
                    roughness: vQ + `/textures/glass/${iQ}/Norris_Glass_mat_Roughness.${iQ}`,
                    metallic: vQ + `/textures/glass/${iQ}/Norris_Glass_mat_Metallic.${iQ}`
                },
                plastic: {
                    matcap: vQ + "/textures/plastic/plastic__matcap-02.webp"
                },
                disco: {
                    matcap: vQ + "/textures/helmet/webp/disco/disco_matcap-01.webp",
                    mask: vQ + `/textures/helmet/${iQ}/disco/disco_mask-01.${iQ}`,
                    lensFlare: vQ + "/textures/helmet/webp/disco/disco_lens-flare-15.webp"
                },
                noise: {
                    texture: vQ + "/textures/noise/noise-03.webp"
                },
                matcaps: {
                    track: vQ + "/textures/tracks/lando__matcap-02.webp"
                },
                notFound: {
                    diffuse: vQ + "/textures/not-found/webp/not-found-alpha-6.webp"
                }
            }
        }
    };
    if (eR.isWebGL2Available()) bI = new RQ({
        canvas: "canvas.gl"
    });
    else console.log("WebGL 2.0 is not available - initializing fallback."), document.documentElement.classList.add("gl-fallback")
});
async function BL() {
    return new Promise((A, Q) => {
        let B = new xU.default.Rive({
            src: cj,
            canvas: IZ,
            artboard: "page-transition",
            stateMachines: "page-transition",
            autoplay: !0,
            layout: new xU.default.Layout({
                fit: xU.default.Fit.Cover,
                alignment: xU.default.Alignment.Center
            }),
            onLoad: () => {
                B.resizeDrawingSurfaceToCanvas();
                let E = B.stateMachineInputs("page-transition");
                if (!E) {
                    console.error("No inputs found for page transition state machine"), Q("No inputs found");
                    return
                }
                let I = E.find((J) => J.name === "initial"),
                    C = E.find((J) => J.name === "transition-out"),
                    K = E.find((J) => J.name === "transition-in");
                if (!I || !C || !K) {
                    console.error("Required transition inputs not found:", E.map((J) => J.name)), Q("Required inputs not found");
                    return
                }
                xJ.initialInput = I, xJ.transitionOutInput = C, xJ.transitionInInput = K, I.value = !0, C.value = !1, K.value = !1, IZ.riveInstance = B, lj.push(B), ZK.style.backgroundColor = "transparent", A(xJ)
            },
            onLoadError: (E) => {
                console.error("Failed to load Page Transition Rive file:", E), Q(E)
            }
        });
        window.addEventListener("resize", () => {
            if (IZ.riveInstance) IZ.riveInstance.resizeDrawingSurfaceToCanvas()
        })
    })
}

function EL({
    initialInput: A,
    transitionInInput: Q,
    transitionOutInput: B
} = xJ) {
    ZK.style.visibility = "visible", ZK.style.pointerEvents = "auto", A.value = !1, Q.value = !1, B.value = !0
}

function H$({
    initialInput: A,
    transitionInInput: Q,
    transitionOutInput: B
} = xJ) {
    A.value = !1, B.value = !1, Q.value = !0, setTimeout(() => {
        Z$.style.opacity = "0"
    }, 100), setTimeout(() => {
        ZK.style.visibility = "hidden", ZK.style.pointerEvents = "none", Z$.style.display = "none"
    }, 500)
}
var xU, mj = "https://lando.itsoffbrand.io/rive/",
    cj, ZK, IZ, Z$, xJ, lj;
var q$ = VA(() => {
    xU = dJ(cU(), 1), cj = mj + "page-transition.riv", ZK = document.querySelector(".transition-w"), IZ = ZK?.querySelector("canvas[data-rive-primary]"), Z$ = ZK?.querySelector(".transition-btn");
    Z$.style.transition = "opacity 300ms";
    xJ = {}, lj = []
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L42434-L43157 =====
// FL init wrapper + text components (42438-43156): SplitText N$, reveal, counters
var FL = VA(() => {
    XL();
    X$()
});
class JZ {
    constructor(A = {}) {
        if (!m || !CI) {
            console.error("GSAP and SplitText plugin must be loaded first");
            return
        }
        this.instances = [], this.defaultOptions = {
            tag: A.tagName || "span",
            linesClass: A.lineClass || "line",
            wordsClass: A.wordClass || "word",
            charsClass: A.charClass || "char",
            aria: "auto",
            ...A
        }, this.init()
    }
    init() {
        if (this.elements = document.querySelectorAll("[split-text]"), this.elements.length) this.splitAll()
    }
    splitAll() {
        this.elements.forEach((A) => {
            if (A.splitTextInstance) {
                if (!this.instances.includes(A.splitTextInstance)) this.instances.push(A.splitTextInstance);
                return
            }
            this._ensureScreenReaderClone(A);
            let Q = A.getAttribute("split-text"),
                B = this._formatTypes(Q || "lines,words,chars"),
                E = CI.create(A, {
                    ...this.defaultOptions,
                    type: B
                });
            E.elements = [A], this.instances.push(E), A.splitTextInstance = E
        })
    }
    _formatTypes(A) {
        return A.replace(/,/g, ", ").trim()
    }
    split(A = null) {
        if (A) {
            let Q = this.instances.find((B) => B.elements && B.elements.includes(A));
            if (Q) {
                Q.revert(), this._ensureScreenReaderClone(A);
                let B = A.getAttribute("split-text"),
                    E = this._formatTypes(B || "lines,words,chars"),
                    I = CI.create(A, {
                        ...this.defaultOptions,
                        type: E
                    });
                I.elements = [A], this.instances = this.instances.map((C) => C === Q ? I : C), A.splitTextInstance = I
            } else {
                this._ensureScreenReaderClone(A);
                let B = A.getAttribute("split-text"),
                    E = this._formatTypes(B || "lines,words,chars"),
                    I = CI.create(A, {
                        ...this.defaultOptions,
                        type: E
                    });
                I.elements = [A], this.instances.push(I), A.splitTextInstance = I
            }
        } else this.instances.forEach((Q, B) => {
            let E = Q.elements[0];
            if (E) {
                Q.revert(), this._ensureScreenReaderClone(E);
                let I = E.getAttribute("split-text"),
                    C = this._formatTypes(I || "lines,words,chars"),
                    K = CI.create(E, {
                        ...this.defaultOptions,
                        type: C
                    });
                K.elements = [E], this.instances[B] = K, E.splitTextInstance = K
            }
        })
    }
    revert(A = null) {
        if (A) {
            let Q = this.instances.find((B) => B.elements && B.elements.includes(A));
            if (Q) Q.revert(), this.instances = this.instances.filter((B) => B !== Q), delete A.splitTextInstance, this._removeScreenReaderClone(A)
        } else if (this.instances.forEach((Q) => {
                if (Q && typeof Q.revert === "function") Q.revert()
            }), this.instances = [], this.elements) this.elements.forEach((Q) => {
            delete Q.splitTextInstance, this._removeScreenReaderClone(Q)
        })
    }
    getAllElements(A) {
        let Q = [],
            B = A === "lines" ? "lines" : A === "words" ? "words" : A === "chars" ? "chars" : null;
        if (!B) return console.warn(`Invalid type: ${A}. Expected 'lines', 'words', or 'chars'.`), Q;
        return this.instances.forEach((E) => {
            if (E[B]) Q = Q.concat(Array.from(E[B]))
        }), Q
    }
    enableResponsive(A = 200) {
        this.instances.forEach((E, I) => {
            let C = E.elements[0];
            if (C) {
                E.revert();
                let K = C.getAttribute("split-text"),
                    J = this._formatTypes(K || "lines,words,chars"),
                    U = CI.create(C, {
                        ...this.defaultOptions,
                        type: J,
                        autoSplit: !0
                    });
                U.elements = [C], this.instances[I] = U, C.splitTextInstance = U
            }
        });
        let Q, B = () => {
            clearTimeout(Q), Q = setTimeout(() => {
                this.split()
            }, A)
        };
        return window.addEventListener("resize", B), () => {
            window.removeEventListener("resize", B)
        }
    }
}

function e0() {
    let A = document.querySelectorAll("[data-oval-scroll]");
    if (A.length) A.forEach((B) => {
        if (B._ovalScrollInstance) B._ovalScrollInstance.cleanup()
    });
    let Q = document.querySelectorAll("[data-oval-scroll]");
    if (!Q.length) return;
    Q.forEach((B) => {
        let E = B.getAttribute("data-oval-scroll") || "top",
            I = B.querySelectorAll(".line");
        if (!I.length) return;
        I.forEach((Z) => {
            if (Z.parentElement.classList.contains("oval-line-clip-wrap")) return;
            let q = document.createElement("div");
            q.classList.add("oval-line-clip-wrap"), Z.parentNode.insertBefore(q, Z), q.appendChild(Z)
        });
        let C = B.querySelectorAll(".oval-line-clip-wrap");
        if (!C.length) return;
        let K = document.createElement("div");
        K.classList.add("oval-outer-wrapper"), K.style.overflow = "clip";
        let J = C[0];
        J.parentNode.insertBefore(K, J), C.forEach((Z) => {
            K.appendChild(Z)
        }), C.forEach((Z) => {
            let q = Z.querySelector(".line"),
                Y = q.querySelectorAll(".char");
            if (E === "top") {
                if (m.set(Z, {
                        clipPath: "ellipse(20% 0% at 50% 0%)"
                    }), m.set(q, {
                        y: "-40%"
                    }), Y.length) m.set(Y, {
                    y: "-40%"
                })
            } else if (m.set(Z, {
                    clipPath: "ellipse(20% 0% at 50% 100%)"
                }), m.set(q, {
                    y: "40%"
                }), Y.length) m.set(Y, {
                y: "40%"
            })
        });
        let U = 0.15,
            G = m.timeline({
                paused: !0
            });
        if (G.to(C, {
                clipPath: E === "top" ? "ellipse(100% 120% at 50% 0%)" : "ellipse(100% 120% at 50% 100%)",
                delay: 0,
                duration: 1.5,
                ease: "power2.inOut",
                stagger: U
            }), C.forEach((Z, q) => {
                let Y = Z.querySelector(".line"),
                    W = Y.querySelectorAll(".char");
                if (m.to(Y, {
                        y: "0%",
                        duration: 1.5,
                        ease: "power2.inOut",
                        delay: U + 0.1 * q
                    }), W.length) m.to(W, {
                    y: "0%",
                    ease: "power2.inOut",
                    duration: 1.5,
                    delay: U * q,
                    stagger: {
                        amount: 0.015 * W.length,
                        from: "center"
                    }
                })
            }), B._ovalScrollInstance = {
                timeline: G,
                cleanup: () => {
                    G.kill()
                }
            }, !B.closest("[data-hero-animation-container]")) {
            let Z = TA.create({
                trigger: B,
                start: "top 95%",
                once: !0,
                onEnter: () => G.play()
            });
            B._ovalScrollInstance.scrollTrigger = Z
        }
    })
}

function AE() {
    document.querySelectorAll("[data-anim-high]").forEach((C) => {
        if (C._highLinesInstance) C._highLinesInstance.cleanup()
    });
    let Q = 0.6,
        B = 0.6,
        E = 0.15,
        I = document.querySelectorAll("[data-anim-high]");
    if (!I.length) return;
    I.forEach((C) => {
        let K = C.getAttribute("data-anim-high") || "right, lime, 0",
            [J, U, G] = K.split(",").map((N) => N.trim()),
            H = J === "left" ? "left" : "right",
            Z = U || "lime",
            q = parseFloat(G || "0") / 1000,
            W = {
                lime: "var(--color--lime, #d2ff00)",
                "lime-off": "var(--color--lime-off, #b2c73a)",
                "dark-green": "var(--color--dark-green, #282c20)",
                "dark-green-tint-1": "var(--color--dark-green-tint-1, #3b3c38)",
                black: "var(--color--black, #111112)",
                white: "var(--color--white, #f4f4ed)"
            } [Z] || "var(--color--lime, #d2ff00)",
            $ = C.querySelectorAll(".line"),
            D = m.timeline({
                paused: !0
            });
        if ($.forEach((N, X) => {
                if (N.style.position = "relative", H === "right") m.set(N, {
                    clipPath: "inset(0 100% 0 0)",
                    autoAlpha: 1
                });
                else m.set(N, {
                    clipPath: "inset(0 0 0 100%)",
                    autoAlpha: 1
                });
                let F = document.createElement("div");
                F.className = "high-line-reveal", F.style.position = "absolute", F.style.top = "0", F.style.left = "0", F.style.width = "100%", F.style.height = "100%", F.style.backgroundColor = W, F.style.transformOrigin = H === "right" ? "right center" : "left center", F.style.zIndex = "5", N.appendChild(F), m.set(F, {
                    scaleX: 1
                });
                let P = X * E;
                if (H === "right") D.to(N, {
                    clipPath: "inset(0 0% 0 0)",
                    duration: Q,
                    ease: "power2.out"
                }, P);
                else D.to(N, {
                    clipPath: "inset(0 0 0 0%)",
                    duration: Q,
                    ease: "power2.out"
                }, P);
                D.to(F, {
                    scaleX: 0,
                    duration: B,
                    ease: "power2.inOut"
                }, P + Q / 2)
            }), C._highLinesInstance = {
                timeline: D,
                cleanup: () => {
                    D.kill()
                }
            }, !C.closest("[data-hero-animation-container]")) {
            let N = TA.create({
                trigger: C,
                start: "top 90%",
                once: !0,
                onEnter: () => {
                    D.play()
                }
            });
            C._highLinesInstance.scrollTrigger = N
        }
    })
}

function UZ() {
    let A = document.querySelectorAll("[data-stat-list]"),
        Q = 0.6,
        B = 0.6,
        E = 0.05;
    A.forEach((I) => {
        let C = I.querySelectorAll("[data-stat-item]"),
            K = I.getAttribute("data-reveal-direction") || "right",
            J = I.getAttribute("data-reveal-color") || "lime",
            U = parseFloat(I.getAttribute("data-reveal-delay") || "0") / 1000,
            H = {
                lime: "var(--color--lime, #d2ff00)",
                "dark-green": "var(--color--dark-green, #282c20)",
                "dark-green-tint-1": "var(--color--dark-green-tint-1, #3b3c38)",
                black: "var(--color--black, #111112)",
                white: "var(--color--white, #f4f4ed)"
            } [J] || "var(--color--lime, #d2ff00)",
            Z = m.timeline({
                scrollTrigger: {
                    trigger: I,
                    start: "top 90%",
                    once: !0
                },
                delay: U
            });
        C.forEach((q, Y) => {
            if (q.style.position = "relative", q.style.overflow = "hidden", K === "right") m.set(q, {
                clipPath: "inset(0 100% 0 0)",
                autoAlpha: 1
            });
            else m.set(q, {
                clipPath: "inset(0 0 0 100%)",
                autoAlpha: 1
            });
            let W = document.createElement("div");
            W.className = "item-reveal", W.style.position = "absolute", W.style.top = "0", W.style.left = "0", W.style.width = "100%", W.style.height = "100%", W.style.backgroundColor = H, W.style.transformOrigin = K === "right" ? "right center" : "left center", W.style.zIndex = "5", q.appendChild(W), m.set(W, {
                scaleX: 1
            });
            let $ = Y * 0.05;
            if (K === "right") Z.to(q, {
                clipPath: "inset(0 0% 0 0)",
                duration: 0.6,
                ease: "power2.out"
            }, $);
            else Z.to(q, {
                clipPath: "inset(0 0 0 0%)",
                duration: 0.6,
                ease: "power2.out"
            }, $);
            Z.to(W, {
                scaleX: 0,
                duration: 0.6,
                ease: "power2.inOut"
            }, $ + 0.3)
        })
    })
}

function QE() {
    let A = document.querySelectorAll("[data-anim='text-hover']"),
        Q = 0.6;
    A.forEach((B) => {
        let E = B.querySelectorAll(".line"),
            I = B.classList.contains("nav-menu-link-w"),
            C = () => {
                if (I) return window.innerWidth < 480 ? "-4.3rem" : "-5.25rem";
                return "-100%"
            };
        if (E.length > 0) E.forEach((K, J) => {
            let U = K.querySelectorAll(".char");
            B.addEventListener("mouseenter", () => {
                m.to(U, {
                    y: C(),
                    duration: 0.6,
                    stagger: 0.02,
                    ease: "power3.out",
                    overwrite: !0
                })
            }), B.addEventListener("mouseleave", () => {
                m.to(U, {
                    y: 0,
                    duration: 0.6,
                    stagger: 0.02,
                    ease: "power3.out",
                    overwrite: !0
                })
            })
        });
        else {
            let K = B.querySelectorAll(".char");
            if (K.length > 0) B.addEventListener("mouseenter", () => {
                m.to(K, {
                    y: C(),
                    duration: 0.6,
                    stagger: 0.02,
                    ease: "power3.out",
                    overwrite: !0
                })
            }), B.addEventListener("mouseleave", () => {
                m.to(K, {
                    y: 0,
                    duration: 0.6,
                    stagger: 0.02,
                    ease: "power3.out",
                    overwrite: !0
                })
            })
        }
    })
}

function fJ() {
    let A = document.querySelectorAll("[data-img-highlight]"),
        Q = 0.8,
        B = 0.6;
    A.forEach((E) => {
        let I = E.getAttribute("data-img-highlight") || "top, lime, 0",
            [C, K, J] = I.split(",").map((D) => D.trim()),
            U = C === "bottom" ? "bottom" : "top",
            G = K || "lime",
            H = parseFloat(J || "0") / 1000,
            q = {
                lime: "var(--color--lime, #d2ff00)",
                "lime-off": "var(--color--lime-off, #b2c73a)",
                "dark-green": "var(--color--dark-green, #282c20)",
                "dark-green-tint-1": "var(--color--dark-green-tint-1, #3b3c38)",
                black: "var(--color--black, #111112)",
                white: "var(--color--white, #f4f4ed)"
            } [G] || "var(--color--lime, #d2ff00)",
            Y = document.createElement("div");
        Y.style.position = "relative", Y.style.overflow = "hidden", Y.style.width = "100%", Y.style.height = "100%", E.parentNode.insertBefore(Y, E), Y.appendChild(E), E.style.width = "100%", E.style.height = "100%", E.style.display = "block";
        let W = document.createElement("div");
        if (W.className = "img-highlight-reveal", W.style.position = "absolute", W.style.top = "0", W.style.left = "0", W.style.width = "100%", W.style.height = "100%", W.style.backgroundColor = q, W.style.zIndex = "5", Y.appendChild(W), U === "top") m.set(Y, {
            clipPath: "ellipse(120% 0% at 50% 0%)"
        }), m.set(E, {
            autoAlpha: 1
        }), m.set(W, {
            clipPath: "ellipse(120% 120% at 50% 100%)"
        });
        else m.set(Y, {
            clipPath: "ellipse(120% 0% at 50% 100%)"
        }), m.set(E, {
            autoAlpha: 1
        }), m.set(W, {
            clipPath: "ellipse(120% 120% at 50% 0%)"
        });
        let $ = m.timeline({
            scrollTrigger: {
                trigger: Y,
                start: "top 80%",
                once: !0
            },
            delay: H
        });
        if (U === "top") $.to(Y, {
            clipPath: "ellipse(120% 120% at 50% 0%)",
            duration: 0.8,
            ease: "power2.out"
        }), $.to(W, {
            clipPath: "ellipse(120% 0% at 50% 100%)",
            duration: 0.6,
            ease: "power2.out"
        }, "-=0.4");
        else $.to(Y, {
            clipPath: "ellipse(120% 120% at 50% 0%)",
            duration: 0.8,
            ease: "power2.out"
        }), $.to(W, {
            clipPath: "ellipse(120% 0% at 50% 0%)",
            duration: 0.6,
            ease: "power2.out"
        }, "-=0.4")
    })
}

function NL() {
    let A = document.querySelectorAll("[data-car-counter]"),
        Q = 1;
    A.forEach((B) => {
        let E = B.textContent.trim(),
            I = parseInt(E.replace(/,/g, ""), 10),
            C = E.replace(/[^0-9]/g, "").length,
            K = "0";
        if (C > 1) K = "0".repeat(C);
        let J = E.includes(",");
        B.textContent = K, TA.create({
            trigger: B,
            start: "top 90%",
            once: !0,
            onEnter: () => {
                m.to({
                    val: 0
                }, {
                    val: I,
                    duration: 1,
                    ease: "power1.out",
                    onUpdate: function() {
                        let U = Math.round(this.targets()[0].val),
                            G;
                        if (J) G = U.toLocaleString();
                        else {
                            G = U.toString();
                            while (G.length < C) G = "0" + G
                        }
                        B.textContent = G
                    }
                })
            }
        })
    })
}

function k0() {
    let A = document.querySelectorAll("[data-hero-animation-container]");
    if (!A.length) return;
    A.forEach((Q) => {
        Q.querySelectorAll("[data-oval-scroll]").forEach((I) => {
            let C = I._ovalScrollInstance;
            if (C?.timeline) C.timeline.play()
        }), Q.querySelectorAll("[data-anim-high]").forEach((I) => {
            let C = I._highLinesInstance;
            if (C?.timeline) C.timeline.play()
        })
    })
}

function GZ() {
    Q_(), A_(), B_(), E_()
}

function A_() {
    document.querySelectorAll("[data-text-format]").forEach((Q) => {
        let B = Q.style.display;
        Q.style.display = "flex";
        let E = Q._formattingCleanup;
        Q._formattingCleanup = () => {
            if (Q.style.display = B, E) E()
        }
    })
}

function Q_() {
    document.querySelectorAll('[data-text-format="position"]').forEach((A) => {
        if (A._formattingCleanup) A._formattingCleanup(), delete A._formattingCleanup
    }), document.querySelectorAll('[data-text-format="round"]').forEach((A) => {
        if (A._formattingCleanup) A._formattingCleanup(), delete A._formattingCleanup
    })
}

function B_() {
    document.querySelectorAll('[data-text-format="position"]').forEach((Q) => {
        let B = Q.textContent.trim(),
            E = B.match(/^(\d+)([a-zA-Z]+)$/);
        if (E) {
            let [I, C, K] = E, J = C === "1" ? "text-on-t-stat-label-sm is-super c-lime" : "text-on-t-stat-label-sm is-super c-grey-on-track";
            Q.innerHTML = `${C}<span class="${J}">${K}</span>`, Q._formattingCleanup = () => {
                Q.textContent = B
            }
        }
    })
}

function E_() {
    document.querySelectorAll('[data-text-format="round"]').forEach((Q) => {
        let B = Q.textContent.trim(),
            E = parseInt(B, 10);
        if (!isNaN(E)) {
            let I = E < 10 ? `0${E}` : `${E}`;
            Q.textContent = I, Q._formattingCleanup = () => {
                Q.textContent = B
            }
        }
    })
}

function OL() {
    F$();
    let A = 0.6,
        Q = 0.6,
        B = 0.15,
        E = document.querySelectorAll("[split-rich-text]");
    if (!E.length) return;
    E.forEach((I) => {
        let C = I.getAttribute("split-rich-text") || "right, lime, 0",
            [K, J, U] = C.split(",").map((T) => T.trim()),
            G = K === "left" ? "left" : "right",
            H = J || "lime",
            Z = parseFloat(U || "0") / 1000,
            Y = {
                lime: "var(--color--lime, #d2ff00)",
                "lime-off": "var(--color--lime-off, #b2c73a)",
                "dark-green": "var(--color--dark-green, #282c20)",
                "dark-green-tint-1": "var(--color--dark-green-tint-1, #3b3c38)",
                black: "var(--color--black, #111112)",
                white: "var(--color--white, #f4f4ed)"
            } [H] || "var(--color--lime, #d2ff00)",
            W = I.querySelectorAll("h1, h2, h3, h4, h5, h6, p, span, div"),
            $ = Array.from(W).filter((T) => {
                return T.textContent.trim().length > 0 && !T.querySelector("h1, h2, h3, h4, h5, h6, p")
            });
        if (!$.length) return;
        let D = [],
            R = [],
            N = new Map,
            X = I.hasAttribute("split-center"),
            F = new Map,
            P = new Map;
        $.forEach((T) => {
            try {
                if (!T._srClone) {
                    let O = T.cloneNode(!0);
                    while (O.attributes && O.attributes.length > 0) O.removeAttribute(O.attributes[0].name);
                    if (O.setAttribute("screen-reader", ""), O.removeAttribute("split-text"), T.parentNode) T.parentNode.insertBefore(O, T);
                    F.set(T, O), P.set(T, T.getAttribute("aria-hidden")), T.setAttribute("aria-hidden", "true"), T._srClone = O
                }
            } catch (O) {
                console.error("Failed to create screen-reader clone for rich text:", O)
            }
            if (X) N.set(T, {
                display: T.style.display,
                flexDirection: T.style.flexDirection,
                alignItems: T.style.alignItems
            });
            T.setAttribute("split-text", "lines, words");
            let V = CI.create(T, {
                type: "lines, words",
                tag: "div",
                linesClass: "line",
                wordsClass: "word"
            });
            if (X) T.style.display = "flex", T.style.flexDirection = "column", T.style.alignItems = "center";
            if (D.push(V), V.lines) R.push(...V.lines)
        });
        let L = m.timeline({
            paused: !0
        });
        if (R.forEach((T, V) => {
                if (T.style.position = "relative", G === "right") m.set(T, {
                    clipPath: "inset(0 100% 0 0)",
                    autoAlpha: 1
                });
                else m.set(T, {
                    clipPath: "inset(0 0 0 100%)",
                    autoAlpha: 1
                });
                let O = document.createElement("div");
                O.className = "rich-text-line-reveal", O.style.position = "absolute", O.style.top = "0", O.style.left = "0", O.style.width = "100%", O.style.height = "100%", O.style.backgroundColor = Y, O.style.transformOrigin = G === "right" ? "right center" : "left center", O.style.zIndex = "5", T.appendChild(O), m.set(O, {
                    scaleX: 1
                });
                let x = V * B;
                if (G === "right") L.to(T, {
                    clipPath: "inset(0 0% 0 0)",
                    duration: A,
                    ease: "power2.out"
                }, x);
                else L.to(T, {
                    clipPath: "inset(0 0 0 0%)",
                    duration: A,
                    ease: "power2.out"
                }, x);
                L.to(O, {
                    scaleX: 0,
                    duration: Q,
                    ease: "power2.inOut"
                }, x + A / 2)
            }), I._richTextInstance = {
                timeline: L,
                splitInstances: D,
                originalStyles: N,
                textElementsWithContent: $,
                shouldCenter: X,
                cleanup: () => {
                    if (L.kill(), D.forEach((V) => {
                            if (V && typeof V.revert === "function") V.revert()
                        }), X) $.forEach((V) => {
                        let O = N.get(V);
                        if (O) V.style.display = O.display, V.style.flexDirection = O.flexDirection, V.style.alignItems = O.alignItems
                    });
                    $.forEach((V) => {
                        let O = F.get(V) || V._srClone;
                        if (O && O.parentNode) O.parentNode.removeChild(O);
                        let x = P.get(V);
                        if (x === null || x === void 0) V.removeAttribute("aria-hidden");
                        else V.setAttribute("aria-hidden", x);
                        if (V._srClone) delete V._srClone
                    }), I.querySelectorAll(".rich-text-line-reveal").forEach((V) => V.remove()), $.forEach((V) => {
                        V.removeAttribute("split-text")
                    })
                }
            }, !I.closest("[data-hero-animation-container]")) {
            let T = TA.create({
                trigger: I,
                start: "top 90%",
                once: !0,
                onEnter: () => {
                    L.play()
                }
            });
            I._richTextInstance.scrollTrigger = T
        }
    })
}

function F$() {
    document.querySelectorAll("[split-rich-text]").forEach((Q) => {
        if (Q._richTextInstance) {
            if (Q._richTextInstance.cleanup(), Q._richTextInstance.scrollTrigger) Q._richTextInstance.scrollTrigger.kill();
            delete Q._richTextInstance
        }
    })
}
var N$;
var VC = VA(() => {
    LB();
    JZ.prototype._ensureScreenReaderClone = function(A) {
        if (!A || A._srClone) return;
        try {
            let Q = A.cloneNode(!0);
            while (Q.attributes && Q.attributes.length > 0) Q.removeAttribute(Q.attributes[0].name);
            if (Q.setAttribute("screen-reader", ""), Q.removeAttribute("split-text"), A.parentNode) A.parentNode.insertBefore(Q, A);
            A._srPrevAriaHidden = A.getAttribute("aria-hidden"), A.setAttribute("aria-hidden", "true"), A._srClone = Q
        } catch (Q) {
            console.error("Failed to create screen-reader clone:", Q)
        }
    };
    JZ.prototype._removeScreenReaderClone = function(A) {
        if (!A) return;
        try {
            if (A._srClone && A._srClone.parentNode) A._srClone.parentNode.removeChild(A._srClone);
            delete A._srClone;
            let Q = A._srPrevAriaHidden;
            if (Q === null || Q === void 0) A.removeAttribute("aria-hidden");
            else A.setAttribute("aria-hidden", Q);
            delete A._srPrevAriaHidden
        } catch (Q) {
            console.error("Failed to remove screen-reader clone:", Q)
        }
    };
    N$ = JZ
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L43158-L43846 =====
// horizontal / marquee / social cards (43158-43754) + scroll indicator qZ (43755-43845)
function I_() {
    document.querySelectorAll("[data-horizontal-section]").forEach((Q) => {
        let B = Q.querySelector(".horizontal-pin-sticky"),
            E = Q.querySelector(".horizontal-track"),
            I = Q.querySelectorAll(".image.is-horizontal-scroll");
        if (!E || !B) return;
        let C = E.offsetWidth;

        function K() {
            E.offsetHeight, C = E.offsetWidth;
            let H = C - window.innerWidth;
            if (H <= 0) {
                let Z = 0;
                if (Array.from(E.children).forEach((q) => {
                        Z += q.offsetWidth
                    }), Z > C) C = Z
            } else Q.style.height = `${H}px`;
            TA.refresh()
        }
        K(), window.addEventListener("resize", () => {
            setTimeout(K, 200)
        });

        function J(H) {
            let Z = H.getBoundingClientRect();
            return Z.left < window.innerWidth && Z.right > 0
        }

        function U() {
            I.forEach((H) => {
                let Z = H.closest(".horizontal-item-w");
                if (!Z) return;
                if (J(Z)) {
                    let W = -4 * (1 - Z.getBoundingClientRect().left / window.innerWidth);
                    m.set(H, {
                        x: `${W}rem`
                    })
                }
            })
        }
        let G = m.to(E, {
            x: () => -(C - window.innerWidth),
            ease: "none",
            scrollTrigger: {
                trigger: Q,
                start: "top bottom",
                end: "bottom bottom",
                scrub: 1,
                invalidateOnRefresh: !0,
                onEnter: U
            }
        });
        ML.set(Q, G), I.forEach((H) => {
            let Z = H.closest(".horizontal-item-w");
            if (!Z) return;
            m.timeline({
                scrollTrigger: {
                    trigger: Z,
                    containerAnimation: G,
                    start: "left right",
                    end: "right left",
                    scrub: !0,
                    onUpdate: (q) => {
                        let W = 4 * q.progress;
                        m.set(H, {
                            x: `${W}rem`
                        })
                    }
                }
            })
        })
    })
}

function C_() {
    let A = document.querySelector(".s.is-horizontal-track");
    if (!A) return;
    let Q = A.querySelectorAll(".text-eyebrow, .horizontal-item-text"),
        B = document.querySelector("[data-horizontal-section]");
    if (!B) return;
    let E = ML.get(B);
    if (!E) {
        console.warn("Could not find the main horizontal scroll animation");
        return
    }
    let I = 0.6,
        C = 0.6,
        K = 0.15,
        J = "var(--color--lime, #d2ff00)";
    Q.forEach((U, G) => {
        let H = U.querySelectorAll(".line");
        if (!H.length) return;
        let Z = G < 2,
            q = m.timeline({
                paused: !0,
                onComplete: () => {}
            });
        H.forEach((W, $) => {
            if (getComputedStyle(W).position === "static") W.style.position = "relative";
            m.set(W, {
                clipPath: "inset(0 100% 0 0)",
                autoAlpha: 1
            });
            let D = document.createElement("div");
            D.className = "high-line-reveal", D.style.position = "absolute", D.style.top = "0", D.style.left = "0", D.style.width = "100%", D.style.height = "100%", D.style.backgroundColor = J, D.style.transformOrigin = "right center", D.style.zIndex = "5", W.appendChild(D), m.set(D, {
                scaleX: 1
            });
            let R = $ * K;
            q.to(W, {
                clipPath: "inset(0 0% 0 0)",
                duration: I,
                ease: "power2.out"
            }, R), q.to(D, {
                scaleX: 0,
                duration: C,
                ease: "power2.inOut"
            }, R + I / 2)
        });
        let Y = U.closest(".horizontal-item-w") || U;
        if (Z) TA.create({
            trigger: Y,
            start: "top 90%",
            once: !0,
            onEnter: () => q.play()
        });
        else TA.create({
            trigger: Y,
            containerAnimation: E,
            start: "left 95%",
            once: !0,
            onEnter: () => q.play()
        })
    })
}

function Z8() {
    if (window.innerWidth >= 992) I_(), C_()
}

function H8() {
    if (window.innerWidth >= 992) TA.getAll().forEach((A) => {
        if (A.trigger?.matches("[data-horizontal-section]")) A.kill()
    })
}

function O$() {
    let A = new Map,
        Q = new IntersectionObserver((E) => {
            E.forEach((I) => {
                let C = I.target;
                if (I.isIntersecting) {
                    if (!A.has(C)) B(C)
                }
            })
        }, {
            rootMargin: "200px 0px",
            threshold: 0
        });
    document.querySelectorAll("[data-marquee-scroll-direction-target]").forEach((E) => {
        Q.observe(E)
    });

    function B(E) {
        let I = E.querySelector("[data-marquee-collection-target]"),
            C = E.querySelector("[data-marquee-scroll-target]");
        if (!I || !C) return;
        let {
            marqueeSpeed: K,
            marqueeDirection: J,
            marqueeDuplicate: U,
            marqueeScrollSpeed: G
        } = E.dataset, H = parseFloat(K), Z = J === "right" ? 1 : -1, q = parseInt(U || 0), Y = parseFloat(G), W = window.innerWidth < 479 ? 0.25 : window.innerWidth < 991 ? 0.5 : 1, $ = H * (I.offsetWidth / window.innerWidth) * W;
        if (C.style.marginLeft = `${Y*-1}%`, C.style.width = `${Y*2+100}%`, q > 0) {
            let P = document.createDocumentFragment();
            for (let L = 0; L < q; L++) P.appendChild(I.cloneNode(!0));
            C.appendChild(P)
        }
        let D = E.querySelectorAll("[data-marquee-collection-target]"),
            R = m.to(D, {
                xPercent: -100,
                repeat: -1,
                duration: $,
                ease: "linear"
            }).totalProgress(0.5);
        m.set(D, {
            xPercent: Z === 1 ? 100 : -100
        }), R.timeScale(Z), R.play(), A.set(E, R), E.setAttribute("data-marquee-status", "normal"), TA.create({
            trigger: E,
            start: "top bottom",
            end: "bottom top",
            onUpdate: (P) => {
                let L = P.direction === 1,
                    z = L ? -Z : Z;
                R.timeScale(z), E.setAttribute("data-marquee-status", L ? "normal" : "inverted")
            }
        });
        let N = m.timeline({
                scrollTrigger: {
                    trigger: E,
                    start: "0% 100%",
                    end: "100% 0%",
                    scrub: 0
                }
            }),
            X = Z === -1 ? Y : -Y,
            F = -X;
        N.fromTo(C, {
            x: `${X}vw`
        }, {
            x: `${F}vw`,
            ease: "none"
        })
    }
    window._marqueeObservers = window._marqueeObservers || [], window._marqueeObservers.push(Q)
}

function LL() {
    if (window._marqueeObservers && window._marqueeObservers.length) window._marqueeObservers.forEach((A) => {
        A.disconnect()
    }), window._marqueeObservers = [];
    document.querySelectorAll("[data-marquee-scroll-direction-target]").forEach((A) => {
        let Q = A.querySelectorAll("[data-marquee-collection-target]"),
            B = A.querySelector("[data-marquee-scroll-target]");
        if (m.getTweensOf(Q).forEach((E) => {
                E.kill()
            }), B) m.getTweensOf(B).forEach((E) => {
            E.kill()
        });
        TA.getAll().forEach((E) => {
            if (E.vars && E.vars.trigger === A) E.kill()
        })
    })
}

function ZZ() {
    let A = document.querySelector("[data-helmet-grid]");
    if (!A) return;
    let Q = A.querySelectorAll(".helmet-grid-item-w");
    if (!Q.length) return;
    let B = 5,
        E = [
            [],
            [],
            [],
            []
        ];
    Q.forEach((I, C) => {
        let K = C % 4;
        E[K].push(I)
    }), E.forEach((I, C) => {
        if (C > 0) m.set(I, {
            y: `${C*B}rem`
        })
    }), E.forEach((I, C) => {
        if (C > 0) m.to(I, {
            y: 0,
            ease: "none",
            scrollTrigger: {
                trigger: A,
                start: "top bottom",
                end: "bottom top",
                scrub: !0
            }
        })
    })
}

function HZ() {
    let A = document.querySelector("[data-helmet-grid]");
    if (!A) return;
    TA.getAll().forEach((B) => {
        if (B.trigger === A) B.kill()
    });
    let Q = A.querySelectorAll(".helmet-grid-item-w");
    if (Q.length) m.set(Q, {
        clearProps: "y"
    })
}

function q8() {
    if (!m || !TA) {
        console.error("GSAP or ScrollTrigger not loaded");
        return
    }
    let A = document.querySelector('[data-social-callout="wrap"]');
    if (A) {
        TA.getAll().forEach((q) => {
            if (q.trigger === A) q.kill()
        }), m.killTweensOf(A.querySelectorAll(".callout-socials-card-w"));
        let Z = A.cloneNode(!0);
        A.parentNode.replaceChild(Z, A)
    }
    let Q = document.querySelector('[data-social-callout="wrap"]');
    if (!Q) return;
    let B = Array.from(Q.querySelectorAll(".callout-socials-card-w"));
    if (!B.length) return;
    let E = Math.floor(B.length / 2),
        I = [{
            scale: 0.7756,
            rotation: -21,
            x: -30,
            y: 7.3,
            zIndex: 1
        }, {
            scale: 0.8498,
            rotation: -14,
            x: -22,
            y: 4,
            zIndex: 2
        }, {
            scale: 0.9346,
            rotation: -7,
            x: -11,
            y: 1.3,
            zIndex: 3
        }, {
            scale: 1,
            rotation: 0,
            x: 0,
            y: 0,
            zIndex: 10
        }, {
            scale: 0.9346,
            rotation: 7,
            x: 11,
            y: 1.3,
            zIndex: 3
        }, {
            scale: 0.8498,
            rotation: 14,
            x: 22,
            y: 4,
            zIndex: 2
        }, {
            scale: 0.7756,
            rotation: 21,
            x: 30,
            y: 7.3,
            zIndex: 1
        }],
        C = [{
            scale: 0.7756,
            rotation: -21,
            x: -15,
            y: 7.3,
            zIndex: 1
        }, {
            scale: 0.8498,
            rotation: -14,
            x: -11,
            y: 4,
            zIndex: 2
        }, {
            scale: 0.9346,
            rotation: -7,
            x: -6,
            y: 1.3,
            zIndex: 3
        }, {
            scale: 1,
            rotation: 0,
            x: 0,
            y: 0,
            zIndex: 10
        }, {
            scale: 0.9346,
            rotation: 7,
            x: 6,
            y: 1.3,
            zIndex: 3
        }, {
            scale: 0.8498,
            rotation: 14,
            x: 11,
            y: 4,
            zIndex: 2
        }, {
            scale: 0.7756,
            rotation: 21,
            x: 15,
            y: 7.3,
            zIndex: 1
        }];

    function K() {
        return window.innerWidth <= 991 ? C : I
    }
    let J = K(),
        U = [...J];
    m.set(B, {
        x: 0,
        y: "10rem",
        scale: 1,
        rotation: 0,
        transformOrigin: "center center",
        opacity: 1
    }), B.forEach((Z, q) => {
        Z.style.zIndex = J[q].zIndex
    });

    function G() {
        let Z = null,
            q = null,
            Y = new Map,
            W = () => {
                if (J = K(), Z === null) D()
            };
        window.addEventListener("resize", W);

        function $(N) {
            J = K();
            let X = m.timeline(),
                F = B.map((L, z) => ({
                    card: L,
                    index: z,
                    distance: Math.abs(z - N)
                }));
            F.sort((L, z) => {
                if (L.index === N) return -1;
                if (z.index === N) return 1;
                return L.distance - z.distance
            });
            let P = B.length - 1;
            return F.forEach(({
                card: L,
                index: z,
                distance: T
            }) => {
                let V = z < N,
                    O = z > N,
                    x = z === N,
                    j = z === P,
                    h = (z - E) / E,
                    p = 1 - Math.abs(h),
                    i = 8,
                    c = 1 + 0.2 * Math.max(0, 3 - T),
                    t = {};
                if (x) t = {
                    y: U[z].y - 2.5 + "rem",
                    x: U[z].x + "rem",
                    scale: U[z].scale * 1.08,
                    rotation: U[z].rotation,
                    duration: 0.5,
                    ease: "elastic.out(1, 0.75)",
                    overwrite: "auto"
                };
                else if (V) {
                    let a = 8 * p * c,
                        KA = -3 * (1 / (T + 1));
                    t = {
                        x: U[z].x - a + "rem",
                        y: U[z].y + "rem",
                        scale: U[z].scale,
                        rotation: U[z].rotation + KA,
                        duration: 0.5,
                        ease: "elastic.out(1, 0.75)",
                        overwrite: "auto"
                    }
                } else if (O) {
                    let a = j ? 0 : 8 * p * c,
                        KA = 3 * (1 / (T + 1));
                    t = {
                        x: U[z].x + a + "rem",
                        y: U[z].y + (j ? -1 : 0) + "rem",
                        scale: U[z].scale,
                        rotation: U[z].rotation + KA,
                        duration: 0.5,
                        ease: "elastic.out(1, 0.75)",
                        overwrite: "auto"
                    }
                }
                X.to(L, t, T * 0.02)
            }), X
        }

        function D() {
            J = K();
            let N = m.timeline(),
                X = B.map((F, P) => ({
                    card: F,
                    index: P,
                    distance: Math.abs(P - E)
                }));
            return X.sort((F, P) => F.distance - P.distance), X.forEach(({
                card: F,
                index: P,
                distance: L
            }) => {
                N.to(F, {
                    x: U[P].x + "rem",
                    y: U[P].y + "rem",
                    scale: U[P].scale,
                    rotation: U[P].rotation,
                    duration: 0.5,
                    ease: "elastic.out(1, 0.75)",
                    overwrite: "auto"
                }, L * 0.02)
            }), N
        }
        B.forEach((N, X) => {
            let F = () => {
                    if (q) clearTimeout(q), q = null;
                    Z = X, $(X)
                },
                P = () => {
                    if (Z === X) q = setTimeout(() => {
                        if (Z === X) Z = null, D();
                        q = null
                    }, 50)
                };
            Y.set(N, {
                enter: F,
                leave: P
            }), N.addEventListener("mouseenter", F), N.addEventListener("mouseleave", P)
        });
        let R = () => {
            if (q) clearTimeout(q), q = null;
            Z = null, D()
        };
        Q.addEventListener("mouseleave", R), Q.cleanup = () => {
            if (Y.forEach((N, X) => {
                    X.removeEventListener("mouseenter", N.enter), X.removeEventListener("mouseleave", N.leave)
                }), Q.removeEventListener("mouseleave", R), window.removeEventListener("resize", W), q) clearTimeout(q);
            m.killTweensOf(B), TA.getAll().forEach((N) => {
                if (N.trigger === Q) N.kill()
            })
        }
    }
    m.timeline({
        scrollTrigger: {
            trigger: Q,
            start: "top 90%",
            once: !0
        },
        onComplete: G
    }).to(B, {
        y: 0,
        duration: 0.8,
        ease: "power2.out",
        stagger: {
            amount: 0.5,
            from: "end"
        }
    }).to(B, {
        x: (Z) => J[Z].x + "rem",
        y: (Z) => J[Z].y + "rem",
        scale: (Z) => J[Z].scale,
        rotation: (Z) => J[Z].rotation,
        duration: 1.2,
        ease: "elastic.out(1, 0.75)",
        stagger: {
            amount: 0.2,
            from: "center"
        }
    }, "-=0.4")
}

function bJ() {
    K_();
    let A = document.querySelector(".camp-collection-w");
    if (!A) return;
    let Q = A.querySelectorAll(".camp-item");
    if (!Q.length) return;
    Q.forEach((B) => {
        let E = B.querySelectorAll(".camp-item-img-w");
        if (E.length !== 2) return;
        m.set(E[0], {
            x: "-4rem"
        }), m.set(E[1], {
            x: "4rem"
        }), m.to(E, {
            x: 0,
            ease: "power2.out",
            scrollTrigger: {
                trigger: B,
                start: "top bottom",
                end: "bottom center",
                scrub: !0
            }
        })
    })
}

function K_() {
    TA.getAll().filter((Q) => {
        if (Q.vars && Q.vars.trigger) {
            let B = Q.vars.trigger;
            return B.closest && B.closest(".camp-collection-w")
        }
        return !1
    }).forEach((Q) => Q.kill())
}
var ML;
var qK = VA(() => {
    LB();
    ML = new Map
});
var VL, vU = !1,
    J_ = 500,
    qZ = () => {
        let A = document.querySelector(".scroll-indicator"),
            Q = document.querySelector(".scroll-indicator-bar");
        if (!A || !Q) return console.warn("Scroll indicator elements not found"), {
            cleanup: () => {},
            reinit: () => {}
        };
        if (!vU) m.set(A, {
            autoAlpha: 0
        });
        let B = () => {
                if (!A || !Q) return 0;
                let U = window.innerHeight,
                    G = document.documentElement.scrollHeight,
                    H = U / G,
                    Z = m.utils.clamp(10, 25, H * 100);
                return m.set(Q, {
                    height: `${Z}%`
                }), Z
            },
            E = B(),
            I = () => {
                if (!A || !Q) return;
                let U = window.innerHeight,
                    H = document.documentElement.scrollHeight - U,
                    q = window.scrollY / H,
                    Y = A.offsetHeight,
                    W = E / 100 * Y,
                    $ = Y - W,
                    D = q * $;
                m.set(Q, {
                    y: D
                })
            },
            C = () => {
                if (!A || !Q) return;
                if (!vU) vU = !0, m.to(A, {
                    autoAlpha: 1,
                    duration: 0.5
                });
                clearTimeout(VL);
                let U = window.innerHeight,
                    H = document.documentElement.scrollHeight - U,
                    q = window.scrollY / H,
                    Y = A.offsetHeight,
                    W = E / 100 * Y,
                    $ = Y - W,
                    D = q * $;
                m.to(Q, {
                    y: D,
                    duration: 0.3,
                    ease: "power2.out"
                }), VL = setTimeout(() => {
                    if (!A) return;
                    vU = !1, m.to(A, {
                        autoAlpha: 0,
                        duration: 0.5
                    })
                }, J_)
            };
        window.addEventListener("scroll", C), window.addEventListener("resize", () => {
            B(), I()
        }), I();
        let K = () => {
            if (window.removeEventListener("scroll", C), window.removeEventListener("resize", B), A) m.set(A, {
                autoAlpha: 0,
                duration: 0.5
            });
            if (Q) m.set(Q, {
                clearProps: "all"
            })
        };
        return {
            cleanup: K,
            reinit: () => {
                K();
                let U = document.querySelector(".scroll-indicator"),
                    G = U ? m.getProperty(U, "autoAlpha") : vU ? 1 : 0;
                qZ();
                let H = document.querySelector(".scroll-indicator");
                if (H) m.set(H, {
                    autoAlpha: G
                })
            }
        }
    };
var zL = VA(() => {
    LB()
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L43847-L44405 =====
// navigation (43847-44404): open/close, window.closeNavigation
function L$() {
    if (hJ && hJ.scrollTrigger) hJ.scrollTrigger.kill(), hJ.kill();
    TA.getAll().forEach((K) => K.kill()), TA.clearMatchMedia();
    let A = document.querySelectorAll('[data-nav-group="brand"]'),
        Q = document.querySelectorAll('[data-nav-group="btns"]'),
        B = [...A, ...Q];
    m.set(B, {
        scale: 1
    });
    let E = document.querySelector(".home-hero-next-race-w"),
        I = document.querySelector(".top-marker");
    if (I) I.remove();
    let C = document.createElement("div");
    if (C.className = "top-marker", C.style.position = "absolute", C.style.top = "0", C.style.left = "0", C.style.width = "100%", C.style.height = "10vh", C.style.pointerEvents = "none", C.style.zIndex = "-1", document.body.appendChild(C), B.length === 0) return console.warn('No elements found with data-nav-group="brand" or data-nav-group="btns"'), null;
    if (window.lenis) window.lenis.scrollTo(0, {
        immediate: !0
    });
    else window.scrollTo(0, 0);
    if (TA.refresh(!0), window.innerWidth > 991) m.set(B, {
        scale: 1.2
    });
    else m.set(B, {
        scale: 1
    });
    return pK(!0), setTimeout(() => {
        if (window.innerWidth > 991) {
            if (hJ = m.to(B, {
                    scale: 1,
                    ease: "power2.out",
                    scrollTrigger: {
                        trigger: ".top-marker",
                        start: "top top",
                        end: "bottom top",
                        scrub: !0,
                        markers: !1,
                        invalidateOnRefresh: !0,
                        onEnter: () => {
                            pK(!1)
                        },
                        onLeaveBack: () => {
                            pK(!0)
                        }
                    }
                }), E) m.to(E, {
                scrollTrigger: {
                    trigger: "body",
                    start: "1px top",
                    onEnter: () => E.classList.add("hidden"),
                    onLeaveBack: () => E.classList.remove("hidden"),
                    markers: !1
                }
            });
            TA.refresh()
        } else {
            let K = () => {
                if (window.scrollY <= 10) pK(!0);
                else pK(!1)
            };
            window.removeEventListener("scroll", K), window.addEventListener("scroll", K), K()
        }
    }, 50), hJ
}

function V$() {
    let A = document.querySelector("[data-nav-theme]"),
        Q = document.querySelectorAll("[data-nav-theme-target]");
    if (!window.themeScrollTriggers) window.themeScrollTriggers = [];
    window.homeLogoColorSet = !1;
    let B = A ? A.getAttribute("data-nav-theme") : "light",
        E = !1,
        I = 200;

    function C() {
        if (window.themeScrollTriggers && window.themeScrollTriggers.length) window.themeScrollTriggers.forEach((Z) => {
            if (Z && typeof Z.kill === "function") Z.kill()
        }), window.themeScrollTriggers = [];
        window.removeEventListener("riveAllLoaded", H), document.removeEventListener("scroll", J)
    }

    function K(Z) {
        if (!A) return;
        if (Z === B || E) return;
        E = !0, setTimeout(() => {
            E = !1
        }, I), B = Z, A.setAttribute("data-nav-theme", Z);
        let q = document.body.classList.contains("is-home") || window.location.pathname === "/" || window.location.pathname === "/index.html";
        if (Z === "light") {
            if (HE("white"), !q || !window.homeLogoColorSet) {
                if (AC("white"), q) window.homeLogoColorSet = !0
            }
        } else if (HE("transparent"), !q || !window.homeLogoColorSet) {
            if (AC("black"), q) window.homeLogoColorSet = !0
        }
    }

    function J() {
        if (!Q.length) {
            K("light");
            return
        }
        let Z = null,
            q = 1 / 0;
        if (Q.forEach((Y) => {
                let W = Y.getBoundingClientRect(),
                    $ = Math.abs(W.top);
                if ($ < q) q = $, Z = Y
            }), Z) {
            let Y = Z.getAttribute("data-nav-theme-target");
            K(Y)
        } else K("light")
    }
    let U;

    function G() {
        if (!U) U = setTimeout(() => {
            J(), U = null
        }, 100)
    }

    function H() {
        C();
        let Z = window.ScrollTrigger || window.gsap && window.gsap.ScrollTrigger;
        if (Z) {
            Q.forEach((q) => {
                let Y = q.getAttribute("data-nav-theme-target");
                try {
                    let W = Z.create({
                        trigger: q,
                        start: "top top",
                        end: "bottom top",
                        onEnter: () => K(Y),
                        onEnterBack: () => K(Y),
                        markers: !1
                    });
                    window.themeScrollTriggers.push(W)
                } catch (W) {
                    console.error("Error creating ScrollTrigger:", W), document.addEventListener("scroll", G)
                }
            });
            try {
                setTimeout(() => {
                    Z.refresh()
                }, 1000)
            } catch (q) {
                console.error("Error refreshing ScrollTrigger:", q)
            }
        } else document.addEventListener("scroll", G);
        setTimeout(J, 100)
    }
    return C(), H(), window.addEventListener("riveAllLoaded", H), {
        destroy: C
    }
}

function kL() {
    let A = document.querySelector("[data-nav-wrap]");
    if (!A) {
        console.log("Navigation wrapper not found");
        return
    }
    let Q = null,
        B = document.querySelector("[data-nav-theme]"),
        E = A?.querySelector("[data-nav-ham]"),
        I = A?.querySelector("[data-nav-m]");
    if (!E || !I) {
        console.log("Hamburger or navigation menu not found");
        return
    }
    let C = I?.querySelector("canvas[data-rive-nav-object]"),
        K = document.querySelector(".nav-middle"),
        J = I?.querySelectorAll("[data-nav-img]"),
        U = I?.querySelectorAll("[data-nav-img] .menu-img-top"),
        G = I?.querySelectorAll(".nav-menu-link-w"),
        H = I?.querySelectorAll("[data-nav-link-highlight]"),
        Z = I?.querySelector(".nav-menu-bg"),
        q = I?.querySelectorAll(".nav-menu-images-col"),
        Y = !1,
        W = !1;
    E.setAttribute("aria-expanded", "false"), E.setAttribute("aria-label", "Open navigation menu");

    function $(O) {
        if (!Y || !q || q.length < 2) return;
        let x = q[0],
            j = q[1],
            i = (O.clientY / window.innerHeight - 0.5) * 2 * 6;
        m.to(x, {
            y: -i + "rem",
            duration: 2,
            ease: "power2.out",
            overwrite: "auto"
        }), m.to(j, {
            y: i + "rem",
            duration: 2,
            ease: "power2.out",
            overwrite: "auto"
        })
    }
    if (document.addEventListener("mousemove", $), q && q.length === 2) m.set(q, {
        y: 0
    });
    m.set(I, {
        clipPath: "ellipse(120% 0% at 50% 0%)",
        display: "none"
    }), m.set(J, {
        clipPath: "ellipse(120% 0% at 50% 0%)",
        y: 25
    }), m.set(G, {
        clipPath: "ellipse(30% 0% at 50% 0%)",
        y: 20
    }), m.set(Z, {
        autoAlpha: 0
    });
    let D = Array.from(J).sort((O, x) => {
            let j = parseInt(O.getAttribute("data-nav-img")?.split("/")[0] || "0"),
                h = parseInt(x.getAttribute("data-nav-img")?.split("/")[0] || "0");
            return j - h
        }),
        R = Array.from(G);
    if (C) {
        let O = C.getAttribute("data-rive-file") || "reef",
            x = C.getAttribute("data-rive-artboard") || "helmet-reef",
            j = C.getAttribute("data-rive-state-machine") || "helmet-reef_scroll",
            h = C.getAttribute("data-rive-fit") || "contain",
            p = C.getAttribute("data-rive-color-input");
        if (window.loadedRiveFiles && window.loadedRiveFiles[O]) N();
        else window.addEventListener("riveAllLoaded", N, {
            once: !0
        })
    }

    function N() {
        if (!window.rive && M$.default) window.rive = M$.default;
        let O = C.getAttribute("data-rive-file") || "reef",
            x = C.getAttribute("data-rive-artboard") || "helmet-reef",
            j = C.getAttribute("data-rive-state-machine") || "helmet-reef_scroll",
            h = C.getAttribute("data-rive-color-input");
        if (!window.rive || !window.loadedRiveFiles || !window.loadedRiveFiles[O]) {
            if (console.error("Required Rive dependencies not available:", {
                    rive: !!window.rive,
                    loadedRiveFiles: !!window.loadedRiveFiles,
                    riveFile: !!(window.loadedRiveFiles && window.loadedRiveFiles[O])
                }), !window.riveRetryAttempt) window.riveRetryAttempt = 1, setTimeout(N, 500);
            else if (window.riveRetryAttempt < 3) window.riveRetryAttempt++, setTimeout(N, 500);
            else console.error("Failed to initialize Rive after multiple attempts");
            return
        }
        window.riveRetryAttempt = 0;
        try {
            let p = new window.rive.Rive({
                riveFile: window.loadedRiveFiles[O],
                canvas: C,
                artboard: x,
                stateMachines: j,
                autoplay: !0,
                layout: new window.rive.Layout({
                    fit: window.rive.Fit.Contain,
                    alignment: window.rive.Alignment.center
                }),
                onLoad: () => {
                    p.resizeDrawingSurfaceToCanvas();
                    let i = p.stateMachineInputs(j);
                    if (i && i.length > 0) {
                        i.forEach((a) => {});
                        let c = i.filter((a) => a.type === window.rive.StateMachineInputType.Boolean),
                            t = i.find((a) => a.name === "scroll");
                        if (t) t.value = 0, C.scrollInput = t;
                        if (h) {
                            let a = i.find((KA) => KA.name === h);
                            if (a) a.value = !0;
                            else console.warn(`Specified color input ${h} not found`)
                        }
                        C.riveInputs = i, C.booleanInputs = c
                    }
                    C.riveInstance = p, p.pause()
                }
            });
            if (window.riveInstances && Array.isArray(window.riveInstances)) window.riveInstances.push(p)
        } catch (p) {
            console.error("Error initializing nav menu Rive animation:", p)
        }
    }
    let X = m.timeline({
        paused: !0,
        onStart: () => {
            if (window.lenisStop(), m.set(I, {
                    display: "flex"
                }), F.value = 0, C) {
                if (C.scrollInput) C.scrollInput.value = 0;
                if (C.riveInstance) C.riveInstance.play();
                else if (console.warn("No Rive instance available on menu open"), typeof N === "function") N()
            }
            Y = !0
        },
        onReverseComplete: () => {
            if (window.lenisStart(), m.set(I, {
                    display: "none"
                }), C && C.riveInstance) {
                if (C.scrollInput) C.scrollInput.value = 0;
                C.riveInstance.pause()
            }
            if (Y = !1, q && q.length === 2) m.to(q, {
                y: 0,
                duration: 0.3,
                ease: "power2.inOut"
            })
        }
    });
    X.to(I, {
        clipPath: "ellipse(120% 100% at 50% 20%)",
        duration: 0.8,
        ease: "power3.out"
    }, 0), X.to(D, {
        clipPath: "ellipse(120% 100% at 50% 20%)",
        y: 0,
        duration: 0.8,
        stagger: 0.06,
        ease: "power3.out"
    }, 0.15), X.to(R, {
        clipPath: "ellipse(120% 100% at 50% 20%)",
        y: 0,
        duration: 0.6,
        stagger: 0.08,
        ease: "back.out(1.2)"
    }, 0.35), m.set(R, {
        y: 20,
        clipPath: "ellipse(30% 0% at 50% 0%)"
    });
    let F = {
        value: 0
    };

    function P() {
        console.log("Rive debug:", {
            canvasExists: !!C,
            instanceExists: !!(C && C.riveInstance),
            inputExists: !!(C && C.scrollInput),
            inputValue: C?.scrollInput?.value
        })
    }

    function L() {
        if (!C || !C.riveInstance) return console.warn("Rive instance not available"), !1;
        if (!C.riveInstance.isPlaying) C.riveInstance.play();
        if (C.scrollInput) C.scrollInput.value = 0;
        return !0
    }
    X.call(L, [], 0.15), X.to(F, {
        value: 1000,
        duration: 1.7,
        immediateRender: !0,
        onStart: function() {
            if (!L()) setTimeout(L, 50), setTimeout(L, 150)
        },
        onUpdate: function() {
            let O = Math.round(F.value);
            if (O % 200 === 0);
            if (C && C.scrollInput) {
                if (C.scrollInput.value = O, C.riveInstance && !C.riveInstance.isPlaying) C.riveInstance.play()
            }
        },
        onComplete: function() {
            if (C && C.scrollInput) C.scrollInput.value = 1000
        },
        onReverseComplete: function() {
            if (C && C.scrollInput) C.scrollInput.value = 0
        }
    }, 0.2), X.call(() => {
        if (C && (!C.riveInstance || !C.riveInstance.isPlaying)) N(), setTimeout(L, 100)
    }, [], 1), X.to(Z, {
        autoAlpha: 0.12,
        duration: 0.6
    }, 0.6);
    let z = I?.querySelectorAll(".nav-menu-link-current-svg path");
    if (z && z.length > 0) z.forEach((O) => {
        let x = O.getTotalLength();
        m.set(O, {
            strokeDasharray: x,
            strokeDashoffset: x
        })
    }), X.to(z, {
        strokeDashoffset: 0,
        duration: 0.6,
        stagger: 0.05,
        ease: "power2.inOut"
    }, 0.4);
    if (X.to(z, {
            strokeDashoffset: function(O, x) {
                return x.getTotalLength()
            },
            duration: 0.4,
            stagger: 0.03,
            ease: "power2.in"
        }, 0, 1), H && H.length > 0) {
        H.forEach((x) => {
            x.style.position = "relative", m.set(x, {
                clipPath: "inset(0 100% 0 0)",
                y: 15
            });
            let j = document.createElement("div");
            j.className = "high-line-reveal", j.style.position = "absolute", j.style.top = "0", j.style.left = "0", j.style.width = "100%", j.style.height = "100%", j.style.backgroundColor = "var(--color--lime, #d2ff00)", j.style.transformOrigin = "right center", j.style.zIndex = "5", x.appendChild(j), m.set(j, {
                scaleX: 1
            })
        }), X.to(H, {
            clipPath: "inset(0 0% 0 0)",
            y: 0,
            duration: 0.7,
            stagger: 0.04,
            ease: "back.out(1.1)"
        }, 0.5);
        let O = [];
        H.forEach((x) => {
            let j = x.querySelector(".high-line-reveal");
            if (j) O.push(j)
        }), X.to(O, {
            scaleX: 0,
            duration: 0.6,
            stagger: 0.05,
            ease: "power2.inOut"
        }, 0.7), X.to(O, {
            scaleX: 1,
            duration: 0.2,
            stagger: 0.03,
            ease: "power2.in"
        }, 0, 1), X.to(H, {
            clipPath: "inset(0 100% 0 0)",
            y: 15,
            duration: 0.3,
            stagger: 0.03,
            ease: "power3.in"
        }, 0.1, 1)
    }
    X.to(R, {
        clipPath: "ellipse(30% 0% at 50% 0%)",
        y: 20,
        duration: 0.3,
        stagger: 0.02,
        ease: "power2.in"
    }, 0.1, 1);

    function T() {
        if (W = !W, E.setAttribute("aria-expanded", W ? "true" : "false"), E.setAttribute("aria-label", W ? "Close navigation menu" : "Open navigation menu"), W) {
            if (B) {
                if (Q = B.getAttribute("data-nav-theme"), B.setAttribute("data-nav-theme", "light"), typeof HE === "function") HE("transparent");
                if (K) m.to(K, {
                    opacity: 0,
                    duration: 0.4,
                    ease: "power2.out"
                })
            }
            if (typeof g8 === "function") g8(!0);
            X.timeScale(1).play()
        } else {
            if (B && Q) {
                if (B.setAttribute("data-nav-theme", Q), typeof HE === "function") HE(Q === "light" ? "white" : "transparent");
                if (K) m.to(K, {
                    opacity: 1,
                    duration: 0.4,
                    ease: "power2.out"
                })
            }
            if (typeof g8 === "function") g8(!1);
            X.timeScale(1.5).reverse()
        }
    }
    E.addEventListener("click", function(O) {
        E.focus(), T()
    }), E.addEventListener("touchend", function(O) {
        if (O.cancelable) O.preventDefault();
        T()
    }, {
        passive: !1
    }), E.setAttribute("role", "button"), E.setAttribute("tabindex", "0"), E.addEventListener("keydown", function(O) {
        if (O.key === "Enter" || O.key === " ") O.preventDefault(), T()
    });

    function V() {
        if (W) {
            if (W = !1, E.setAttribute("aria-expanded", "false"), E.setAttribute("aria-label", "Open navigation menu"), B && Q) {
                if (B.setAttribute("data-nav-theme", Q), typeof HE === "function") HE(Q === "light" ? "white" : "transparent");
                if (K) m.to(K, {
                    opacity: 1,
                    duration: 0.4,
                    ease: "power2.out"
                })
            }
            if (typeof g8 === "function") g8(!1);
            X.timeScale(1.5).reverse()
        }
    }
    if (window.closeNavigation = V, G.length > 0 && D.length > 0) {
        let O = {},
            x = -1;
        R.forEach((p, i) => {
            if (p.classList.contains("w--current")) x = i
        }), D.forEach((p) => {
            let i = p.querySelector(".menu-img-top");
            if (i) {
                let c = p.getAttribute("data-nav-img")?.split("/")[0];
                if (c) {
                    let t = parseInt(c) - 1;
                    if (O[t] = i, t === x) m.set(i, {
                        opacity: 0.5
                    });
                    else m.set(i, {
                        opacity: 0
                    })
                }
            }
        });
        let j = -1,
            h = !1;
        if (R.forEach((p, i) => {
                p.addEventListener("mouseenter", () => {
                    let c = O[i];
                    if (c) Object.keys(O).forEach((t) => {
                        if (parseInt(t) !== i) m.to(O[t], {
                            opacity: 0,
                            duration: 0.3,
                            overwrite: "auto"
                        })
                    }), j = i, h = !0, m.to(c, {
                        opacity: 1,
                        duration: 0.2,
                        overwrite: "auto",
                        ease: "power2.inOut"
                    })
                }), p.addEventListener("mouseleave", () => {
                    h = !1, setTimeout(() => {
                        if (!h && j === i) Object.keys(O).forEach((c) => {
                            let t = parseInt(c) === x ? 0.5 : 0;
                            m.to(O[c], {
                                opacity: t,
                                duration: 0.2,
                                ease: "power2.inOut",
                                overwrite: "auto"
                            })
                        }), j = -1
                    }, 50)
                })
            }), I) I.addEventListener("mouseleave", () => {
            h = !1, Object.keys(O).forEach((p) => {
                let i = parseInt(p) === x ? 0.5 : 0;
                m.to(O[p], {
                    opacity: i,
                    duration: 0.2,
                    ease: "power2.inOut",
                    overwrite: "auto"
                })
            }), j = -1
        })
    }
}
var M$, hJ = null;
var wL = VA(() => {
    M$ = dJ(cU(), 1);
    LB();
    vC()
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L44406-L44585 =====
// Vimeo (44406-44584): init has no call site in the bundle
function YZ() {
    let A = U_.destroyAll()
}
var U_;
var z$ = VA(() => {
    U_ = (() => {
        let A = [],
            Q = !1,
            B = (J = {}) => {
                let U = {
                    selector: "[data-video-stream]",
                    forceReload: !1,
                    onReady: null,
                    ...J
                };
                return new Promise((G, H) => {
                    C(U.forceReload).then(() => {
                        let Z = document.querySelectorAll(U.selector);
                        if (Z.length === 0) {
                            console.warn("No video elements found with selector:", U.selector), G({
                                players: []
                            });
                            return
                        }
                        if (Z.forEach((q, Y) => {
                                try {
                                    let W = K(q, Y);
                                    if (W) A.push(W)
                                } catch (W) {
                                    console.error("Error initializing video player:", W)
                                }
                            }), !Q) document.addEventListener("visibilitychange", I), Q = !0;
                        if (typeof U.onReady === "function") U.onReady(A);
                        G({
                            players: A
                        })
                    }).catch((Z) => {
                        console.error("Failed to load Vimeo API:", Z), H(Z)
                    })
                })
            },
            E = () => {
                if (A.forEach((J) => {
                        J.destroy()
                    }), A = [], Q) document.removeEventListener("visibilitychange", I), Q = !1;
                return {
                    success: !0
                }
            };

        function I() {
            if (document.hidden) A.forEach((J) => {
                if (J.hoverControl) J.pause()
            })
        }

        function C(J = !1) {
            return new Promise((U, G) => {
                if (window.Vimeo && window.Vimeo.Player && !J) {
                    U();
                    return
                }
                if (J) {
                    let Z = document.querySelector('script[src*="player.vimeo.com/api/player.js"]');
                    if (Z) Z.parentNode.removeChild(Z)
                }
                let H = document.createElement("script");
                H.src = "https://player.vimeo.com/api/player.js", H.async = !0, H.onload = () => {
                    if (window.Vimeo && window.Vimeo.Player) U();
                    else setTimeout(() => {
                        if (window.Vimeo && window.Vimeo.Player) U();
                        else G(new Error("Vimeo Player API failed to initialize"))
                    }, 100)
                }, H.onerror = () => {
                    G(new Error("Failed to load Vimeo Player API script"))
                }, document.body.appendChild(H)
            })
        }

        function K(J, U) {
            let G = J.getAttribute("data-stream-url"),
                H = J.getAttribute("data-stream-source") || "vimeo",
                Z = J.getAttribute("data-stream-muted") === "true",
                q = J.getAttribute("data-stream-loop") === "true",
                Y = J.getAttribute("data-stream-autoplay") === "true",
                W = J.getAttribute("data-stream-hover") === "true";
            if (H !== "vimeo" || !G) return null;
            let $ = J.querySelector(".iframe-wrapper");
            if (!$) $ = document.createElement("div"), $.className = "iframe-wrapper", J.appendChild($);
            let D = document.createElement("iframe");
            D.allow = "autoplay", D.frameBorder = "0";
            let R = `https://player.vimeo.com/video/${G}?`;
            R += Y && !W ? "autoplay=1&" : "autoplay=0&", R += q ? "loop=1&" : "", R += Z ? "muted=1&" : "", R += "background=1&transparent=0&dnt=1", D.src = R, $.appendChild(D);
            let N = "vimeo-player-" + U;
            J.id = N;
            let X = new Vimeo.Player(D),
                F;
            X.getVideoWidth().then(function(V) {
                X.getVideoHeight().then(function(O) {
                    F = O / V, P()
                })
            });

            function P() {
                if (!F) return;
                let V = J.closest("[data-video-stream-wrap]");
                if (!V) return;
                let O = V.offsetHeight / V.offsetWidth;
                if (O > F) {
                    let x = O / F * 100;
                    $.style.width = x + "%", $.style.height = "100%"
                } else {
                    let x = F / O * 100;
                    $.style.width = "100%", $.style.height = x + "%"
                }
            }
            let L = () => P();
            if (window.addEventListener("resize", L), X.setVolume(0), W) X.pause();
            else if (Y) X.play();
            let z, T;
            if (W) {
                let V = J.closest("[data-video-stream-wrap]"),
                    O = null;
                if (V) z = function() {
                    if (!O) O = X.play().catch((h) => {
                        if (h && h.name !== "PlayInterrupted") console.error("Play error:", h)
                    });
                    let x = V.querySelector("[data-video-stream-placeholder]");
                    if (x) x.style.transition = "opacity 0.3s ease", x.style.opacity = "0";
                    let j = V.querySelector(".hover-indicator");
                    if (j) j.textContent = "Playing..."
                }, T = function() {
                    if (O) O.then(() => {
                        X.pause(), O = null
                    }).catch(() => {
                        O = null
                    });
                    else X.pause();
                    let x = V.querySelector("[data-video-stream-placeholder]");
                    if (x) x.style.transition = "opacity 0.3s ease", x.style.opacity = "1";
                    let j = V.querySelector(".hover-indicator");
                    if (j) j.textContent = "Hover to play"
                }, V.addEventListener("mouseenter", z), V.addEventListener("mouseleave", T)
            }
            return {
                id: N,
                player: X,
                element: J,
                wrapper: J.closest("[data-video-stream-wrap]"),
                hoverControl: W,
                adjustVideoSizing: P,
                play: () => X.play(),
                pause: () => X.pause(),
                restart: () => {
                    X.setCurrentTime(0), X.play()
                },
                setVolume: (V) => X.setVolume(V),
                mute: () => X.setVolume(0),
                unmute: () => X.setVolume(1),
                destroy: () => {
                    if (window.removeEventListener("resize", L), W) {
                        let V = J.closest("[data-video-stream-wrap]");
                        if (V) {
                            if (z) V.removeEventListener("mouseenter", z);
                            if (T) V.removeEventListener("mouseleave", T)
                        }
                    }
                    if (X.destroy(), $ && $.parentNode) $.parentNode.removeChild($);
                    J.removeAttribute("id")
                }
            }
        }
        return {
            init: B,
            destroyAll: E,
            getPlayers: () => A
        }
    })()
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L44586-L44664 =====
// PL (44586-44663)
function PL() {
    if (typeof TA === "undefined" && m) m.registerPlugin(TA);
    if ($Z.length > 0) $Z.forEach((I) => I.kill()), $Z = [];
    let A = {
        black: "#111112",
        "dark-green": "#282c20",
        "dark-green-tint-1": "#363B25",
        "dark-green-tint-1-low": "#D4D7CA",
        white: "#f4f4ed",
        lime: "#D2FF00"
    };
    if (window.landoGL && window.landoGL.params && window.landoGL.params.backgroundScene) {
        if (window.landoGL.params.backgroundScene.COLOR_BACKGROUND = A["dark-green"], window.landoGL.params.backgroundScene.COLOR_FOREGROUND = A["dark-green-tint-1"], document.body.style.backgroundColor = A["dark-green"], window.landoGL.updateColors) window.landoGL.updateColors()
    }
    let Q = (I) => {
            if (!I) return null;
            return A[I.trim().toLowerCase()] || I.trim()
        },
        B = (I) => {
            if (!I) return [null, null];
            let C = I.split(",").map((K) => K.trim());
            return [Q(C[0]), Q(C[1] || C[0])]
        };
    document.querySelectorAll("[data-gl-change-track]").forEach((I) => {
        let C = I.getAttribute("data-gl-change-from"),
            K = I.getAttribute("data-gl-change-to"),
            J = I.getAttribute("data-gl-change-trigger-start") || "top top",
            U = I.getAttribute("data-gl-change-trigger-end") || "bottom bottom",
            [G, H] = B(C),
            [Z, q] = B(K);
        if (!G || !Z) {
            console.warn("Invalid color values for element:", I);
            return
        }
        let Y = {
                progress: 0
            },
            W = m.fromTo(Y, {
                progress: 0
            }, {
                progress: 1,
                scrollTrigger: {
                    trigger: I,
                    start: J,
                    end: U,
                    scrub: !0,
                    ease: "cubic-bezier(1, 0, 0.37, 1)",
                    invalidateOnRefresh: !0,
                    onUpdate: ($) => {
                        if (window.debugGLColors) console.log(`Element ${I.id||"unknown"} - Progress: ${$.progress.toFixed(3)}`);
                        let D = m.utils.interpolate(G, Z, Y.progress);
                        if (window.landoGL.params.backgroundScene.COLOR_BACKGROUND = D, H && q) {
                            let R = m.utils.interpolate(H, q, Y.progress);
                            window.landoGL.params.backgroundScene.COLOR_FOREGROUND = R
                        }
                        if (window.landoGL.updateColors) window.landoGL.updateColors();
                        if (document.body.style.backgroundColor = D, window.debugGLColors) console.log(`
                  Element: ${I.id||"unknown"}
                  Trigger start: ${J}
                  Trigger end: ${U}
                  Progress: ${(Y.progress*100).toFixed(2)}%
                  BG Color: ${D}
                  FG Color: ${H&&q?m.utils.interpolate(H,q,Y.progress):"N/A"}
                `)
                    }
                },
                immediateRender: !0
            });
        $Z.push(W.scrollTrigger)
    }), window.debugGLColors = !1, window.toggleGLColorDebug = function(I) {
        window.debugGLColors = I !== void 0 ? I : !window.debugGLColors, console.log(`GL Color debug is now ${window.debugGLColors?"enabled":"disabled"}`)
    }
}
var $Z;
var SL = VA(() => {
    LB();
    $Z = []
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L44665-L45001 =====
// home page w$/TL (44665-45000)
function Z_() {
    let A = document.querySelector('[data-sticky-hero="track"]');
    if (!A) return;
    let Q = A.querySelector(".hero-eyebrow-tracker"),
        E = A.querySelector('[data-hero-anim="msg"]')?.querySelectorAll(".char"),
        I = A.querySelectorAll('[data-hero-anim="img"]'),
        C = A.querySelector('[data-hero-anim="mob1"]'),
        J = A.querySelector('[data-hero-anim="mob2"]')?.querySelectorAll(".char");
    if (!Q || !E || !I) return;
    let U = 0.8;
    if (m.set(E, {
            y: "100%"
        }), m.set(I, {
            autoAlpha: 0,
            y: "1rem"
        }), m.set(C, {
            clipPath: "ellipse(110% 110% at 50% 0%)"
        }), J) m.set(J, {
        y: "0%"
    });
    let G = m.timeline({
        scrollTrigger: {
            trigger: Q,
            start: "top bottom",
            end: "center bottom",
            scrub: !0
        }
    });
    if (G.to(C, {
            clipPath: "ellipse(100% 0% at 50% 0%)",
            duration: U,
            ease: "power3.out"
        }), J && J.length) G.to(J, {
        y: "-100%",
        duration: U,
        stagger: 0.02,
        ease: "power3.out"
    }, "<");
    m.timeline({
        scrollTrigger: {
            trigger: Q,
            start: "top bottom",
            end: "bottom bottom",
            scrub: !0
        }
    }).to(E, {
        y: "0",
        duration: U,
        stagger: 0.02,
        ease: "power3.out"
    }).to(I, {
        autoAlpha: 1,
        y: 0,
        duration: U,
        ease: "power3.out"
    }, "<0.3")
}

function H_() {
    let A = document.querySelector("[data-otot-section]");
    if (!A) return;
    let Q = A.querySelector(".otot-home-img-w.is-1 img"),
        B = A.querySelector(".otot-home-img-w.is-2 img"),
        E = A.querySelector(".otot-home-text-col.is-1"),
        I = A.querySelector(".otot-home-text-col.is-2");
    if (!Q || !B || !E || !I) return;
    m.set(Q, {
        x: "-20rem"
    }), m.set(B, {
        x: "20rem"
    }), m.set(E, {
        x: "-5rem"
    }), m.set(I, {
        x: "5rem"
    }), m.to([Q, B], {
        x: 0,
        ease: "power2.out",
        scrollTrigger: {
            trigger: A,
            start: "top bottom",
            end: "bottom bottom",
            scrub: !0
        }
    }), m.to([E, I], {
        x: 0,
        ease: "none",
        scrollTrigger: {
            trigger: A,
            start: "top bottom",
            end: "60% bottom",
            scrub: !0
        }
    })
}

function k$() {
    let A = document.querySelector("[data-otot-bottom]");
    if (!A) return;
    let Q = A.querySelector("img");
    if (!Q) return;
    m.to(Q, {
        y: "-20vh",
        scale: 1.1,
        ease: "none",
        scrollTrigger: {
            trigger: A,
            start: "top bottom",
            end: "bottom top",
            scrub: !0
        }
    })
}

function q_() {
    let A = document.querySelector("[data-exe-visor]");
    if (!A) return;
    m.set(A, {
        clipPath: "ellipse(70% 0% at 50% 0%)"
    }), m.to(A, {
        clipPath: "ellipse(70% 100% at 50% 0%)",
        ease: "none",
        scrollTrigger: {
            trigger: A,
            start: "top bottom",
            end: "bottom center",
            scrub: !0
        }
    })
}

function Y_() {
    let A = document.querySelector("[data-exe-section]");
    if (!A) return;
    let Q = A.querySelectorAll(".exe-cta-img-w");
    if (!Q.length) return;
    let B = [];
    Q.forEach((E, I) => {
        let C = E.querySelector("img");
        if (!C) return;
        let K = I % 2 === 0 ? -3 : 3;
        B.push({
            element: C,
            fromY: 0,
            toY: "-4rem"
        })
    }), B.forEach((E) => {
        m.set(E.element, {
            y: E.fromY
        }), m.to(E.element, {
            y: E.toY,
            ease: "none",
            scrollTrigger: {
                trigger: A,
                start: "top bottom",
                end: "bottom top",
                scrub: !0
            }
        })
    })
}

function $_() {
    let A = document.querySelector("[data-home-swipe-toggle]");
    if (!A) {
        console.warn("Home swipe toggle button [data-home-swipe-toggle] not found");
        return
    }
    let Q = A.querySelector(".btn-rive-w.is-unlocked"),
        B = A.querySelector(".btn-rive-w.is-locked");
    if (!Q || !B) {
        console.warn("Swipe toggle icons not found within button");
        return
    }
    let E = document.querySelector('[data-home-swipe-desc="unlocked"]'),
        I = document.querySelector('[data-home-swipe-desc="locked"]');
    if (!E || !I) {
        console.warn("Swipe toggle description elements not found");
        return
    }
    let C = E.querySelector(".text-eyebrow"),
        K = I.querySelector(".text-eyebrow");
    if (!C || !K) {
        console.warn("Eyebrow elements not found within description elements");
        return
    }
    w0.button = A, A.setAttribute("aria-label", "Disable smooth scrolling and scroll to top"), A.setAttribute("aria-pressed", "false"), A.setAttribute("role", "switch"), A.setAttribute("data-scroll-disabled", "false"), Q.style.display = "flex", B.style.display = "none", m.set(C, {
        y: "0%"
    }), m.set(K, {
        y: "100%"
    }), w0.handler = function(G) {
        if (G.preventDefault(), !w0.isScrollDisabled) {
            if (typeof window.lenisStop === "function") window.lenisStop();
            if (window.lenis && typeof window.lenis.scrollTo === "function") window.lenis.scrollTo(0, {
                duration: 0
            });
            w0.isScrollDisabled = !0, A.setAttribute("aria-pressed", "true"), A.setAttribute("aria-label", "Enable smooth scrolling"), A.setAttribute("data-scroll-disabled", "true"), Q.style.display = "none", B.style.display = "flex", m.to(C, {
                y: "100%",
                duration: 0.3,
                ease: "power2.out"
            }), m.to(K, {
                y: "0%",
                duration: 0.3,
                ease: "power2.out"
            })
        } else {
            if (typeof window.lenisStart === "function") window.lenisStart();
            w0.isScrollDisabled = !1, A.setAttribute("aria-pressed", "false"), A.setAttribute("aria-label", "Disable smooth scrolling and scroll to top"), A.setAttribute("data-scroll-disabled", "false"), Q.style.display = "block", B.style.display = "none", m.to(K, {
                y: "100%",
                duration: 0.3,
                ease: "power2.out"
            }), m.to(C, {
                y: "0%",
                duration: 0.3,
                ease: "power2.out"
            })
        }
    }, A.addEventListener("click", w0.handler);
    let J = document.querySelector(".top-marker"),
        U = document.querySelector("[data-home-swipe-wrap]");
    if (J && U) m.set(U, {
        autoAlpha: 1,
        pointerEvents: "auto"
    }), w0.scrollTrigger = TA.create({
        trigger: J,
        start: "top 95%",
        end: "bottom top",
        onToggle: (G) => {
            if (G.isActive) m.to(U, {
                autoAlpha: 1,
                pointerEvents: "auto",
                duration: 0.3,
                ease: "power2.out"
            });
            else m.to(U, {
                autoAlpha: 0,
                pointerEvents: "none",
                duration: 0.3,
                ease: "power2.out"
            })
        }
    });
    else {
        if (!J) console.warn(".top-marker element not found");
        if (!U) console.warn("[data-home-swipe-wrap] element not found")
    }
}

function D_() {
    if (w0.button && w0.handler) w0.button.removeEventListener("click", w0.handler);
    if (w0.scrollTrigger) w0.scrollTrigger.kill();
    let A = document.querySelector("[data-home-swipe-wrap]");
    if (A) m.set(A, {
        autoAlpha: 1,
        pointerEvents: "auto"
    });
    if (w0.isScrollDisabled && typeof window.lenisStart === "function") window.lenisStart();
    w0.button = null, w0.handler = null, w0.isScrollDisabled = !1, w0.scrollTrigger = null
}

function w$() {
    if (q8(), Z_(), Z8(), H_(), k$(), window.innerWidth >= 992) ZZ();
    if (q_(), Y_(), window.innerWidth < 992) $_();
    if (window.innerWidth < 992) m.set(".nav-middle", {
        y: "3.5rem"
    });
    if (PL(), window.innerWidth >= 992) e0(), AE(), QE()
}

function W_() {
    TA.getAll().forEach((A) => {
        if (A.trigger?.querySelector('[data-sticky-hero="canvas"]') || A.trigger?.matches('[data-sticky-hero="track"]')) A.kill()
    })
}

function R_() {
    TA.getAll().forEach((A) => {
        if (A.trigger?.querySelector("[data-hero-anim]")) A.kill()
    })
}

function X_() {
    TA.getAll().forEach((A) => {
        if (A.trigger?.matches("[data-otot-section]")) A.kill()
    })
}

function F_() {
    TA.getAll().forEach((A) => {
        if (A.trigger?.matches("[data-otot-bottom]")) A.kill()
    })
}

function N_() {
    TA.getAll().forEach((A) => {
        if (A.trigger?.matches("[data-exe-visor]")) A.kill()
    })
}

function O_() {
    TA.getAll().forEach((A) => {
        if (A.trigger?.matches("[data-exe-section]")) A.kill()
    })
}

function M_() {
    TA.getAll().forEach((Q) => {
        if (Q.trigger?.matches('[data-social-callout="wrap"]')) Q.kill()
    });
    let A = document.querySelector('[data-social-callout="wrap"]');
    if (A) A.querySelectorAll(".callout-socials-card-w").forEach((B) => {
        B.replaceWith(B.cloneNode(!0))
    }), A.replaceWith(A.cloneNode(!0))
}

function TL() {
    if (W_(), R_(), H8(), M_(), X_(), F_(), HZ(), N_(), O_(), window.innerWidth < 992) D_();
    YZ(), m.set(".nav-middle", {
        y: "0rem"
    }), TA.getAll().forEach((A) => {
        if (A.trigger?.querySelector("[data-oval-scroll]") || A.trigger?.querySelector("[data-text-highlight]") || A.trigger?.querySelector("[data-text-hover-chars]")) A.kill()
    })
}
var w0;
var P$ = VA(() => {
    LB();
    VC();
    qK();
    z$();
    SL();
    w0 = {
        button: null,
        handler: null,
        isScrollDisabled: !1,
        scrollTrigger: null
    }
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L45002-L45829 =====
// on-track component group _$/yL (45002-45828): heroflip (Q10)
function S$() {
    let A = document.querySelectorAll("[data-countdown-wrap]");
    if (!A || A.length === 0) return;
    let Q = [];
    return A.forEach((B) => {
        let E = B.querySelector("[data-countdown-date-target]");
        if (!E) return;
        let I = B.querySelectorAll("[data-countdown-digit]");
        if (!I || I.length !== 4) return;
        let C = E.textContent.trim(),
            [K, J] = C.split(" "),
            [U, G, H] = K.split("/"),
            [Z, q] = J.split(":"),
            Y = parseInt(U, 10),
            W = parseInt(G, 10) - 1,
            $ = parseInt(H, 10),
            D = parseInt(Z, 10),
            R = parseInt(q, 10),
            N = new Date(Date.UTC($, W, Y, D, R, 0));

        function X() {
            let L = N - new Date;
            if (L < 0) {
                I.forEach((j) => {
                    j.textContent = "00"
                });
                return
            }
            let z = Math.floor(L / 86400000),
                T = Math.floor(L % 86400000 / 3600000),
                V = Math.floor(L % 3600000 / 60000),
                O = Math.floor(L % 60000 / 1000),
                x = [z.toString().padStart(2, "0"), T.toString().padStart(2, "0"), V.toString().padStart(2, "0"), O.toString().padStart(2, "0")];
            I.forEach((j, h) => {
                j.textContent = x[h]
            })
        }
        X();
        let F = setInterval(X, 1000);
        B.dataset.countdownInterval = F, Q.push(F)
    }), Q
}

function jL() {
    document.querySelectorAll("[data-countdown-wrap]").forEach((Q) => {
        let B = Q.dataset.countdownInterval;
        if (B) clearInterval(Number(B)), delete Q.dataset.countdownInterval
    })
}

function L_() {
    let A = document.querySelector('[data-podium="wrap"]'),
        Q = document.querySelectorAll(".text-on-t-stat-label-gigantic .char")[1],
        B = document.querySelector('[data-podium="text"]');
    if (window.innerWidth >= 992) m.set(B, {
        y: "17.5rem"
    });
    else m.set(B, {
        y: "4rem"
    });
    m.to(B, {
        scrollTrigger: {
            trigger: A,
            start: "top bottom",
            end: "bottom center",
            scrub: !0
        },
        y: 0,
        ease: "power1.in"
    }), m.to(Q, {
        scrollTrigger: {
            trigger: A,
            start: "top bottom",
            end: "bottom center",
            scrub: !0
        },
        y: window.innerWidth >= 480 ? "-17.5rem" : "-6rem",
        ease: "power1.in"
    })
}

function T$() {
    let A = document.querySelectorAll("[data-stat-list]"),
        Q = 0.8,
        B = 0.6,
        E = new Map,
        I = 0,
        C = 0,
        K = !1,
        J = new Set,
        U = new IntersectionObserver((H) => {
            H.forEach((Z) => {
                if (Z.isIntersecting) J.add(Z.target);
                else J.delete(Z.target)
            })
        }, {
            threshold: 0.1
        });
    document.addEventListener("mousemove", (H) => {
        I = H.clientX, C = H.clientY, G()
    }, {
        passive: !0
    });

    function G() {
        J.forEach((H) => {
            let Z = E.get(H);
            if (Z) {
                let q = H.getBoundingClientRect(),
                    Y = I >= q.left && I <= q.right && C >= q.top && C <= q.bottom;
                if (Y && !Z.isHovered) Z.isHovered = !0, Z.tl.timeScale(1).play();
                else if (!Y && Z.isHovered) Z.isHovered = !1, Z.tl.timeScale(2).reverse();
                if (Y) m.to(Z.hoverObject, {
                    x: I - q.left + 20,
                    y: C - q.top - 20,
                    duration: 0.5,
                    ease: "power2.out"
                })
            }
        }), K = !1
    }
    return A.forEach((H) => {
            let Z = H.querySelector("[data-mouse-track]");
            if (!Z) return;
            let q = Z.querySelector(".f1-highlight-mouse-over-reveal");
            if (!q) return;
            let Y = Z.querySelector("img");
            m.set(Z, {
                position: "absolute",
                overflow: "hidden",
                clipPath: "ellipse(120% 0% at 50% 0%)",
                zIndex: 10,
                pointerEvents: "none",
                x: 0,
                y: 0
            }), m.set(q, {
                position: "absolute",
                top: 0,
                left: 0,
                width: "100%",
                height: "100%",
                backgroundColor: "var(--color--lime, #d2ff00)",
                zIndex: 5,
                clipPath: "ellipse(120% 120% at 50% 100%)"
            });
            let W = m.timeline({
                paused: !0
            });
            W.to(Z, {
                clipPath: "ellipse(120% 120% at 50% 0%)",
                autoAlpha: 1,
                duration: 0.8,
                ease: "power2.out"
            }), W.to(q, {
                clipPath: "ellipse(120% 0% at 50% 100%)",
                duration: 0.6,
                ease: "power2.out"
            }, "-=0.4"), H.querySelectorAll("[data-stat-item]").forEach((D, R) => {
                let N = D.querySelector(".display-none"),
                    X = N ? N.querySelector("[data-stat-hover-img]") : null;
                if (X && Y) D.addEventListener("mouseover", () => {
                    let F = X.getAttribute("src");
                    if (F) Y.src = F
                })
            }), U.observe(H), E.set(H, {
                hoverObject: Z,
                tl: W,
                isHovered: !1
            }), H.addEventListener("mouseenter", () => {
                let D = E.get(H);
                D.isHovered = !0, D.tl.play()
            }), H.addEventListener("mousemove", (D) => {
                let R = H.getBoundingClientRect(),
                    N = D.clientX - R.left,
                    X = D.clientY - R.top;
                m.to(Z, {
                    x: N + 20,
                    y: X - 20,
                    duration: 0.5,
                    ease: "power2.out"
                })
            }), H.addEventListener("mouseleave", () => {
                let D = E.get(H);
                D.isHovered = !1, D.tl.timeScale(2).reverse()
            })
        }), window.addEventListener("scroll", () => {
            G()
        }, {
            passive: !0
        }),
        function H() {
            U.disconnect(), J.clear()
        }
}

function V_() {
    let A = document.querySelector('[data-podium="wrap"]');
    if (!A) return;
    let Q = A.querySelector("[data-mouse-track]");
    if (!Q) return;
    let B = Q.querySelector(".f1-highlight-mouse-over-reveal");
    if (!B) return;
    let E = Q.querySelector("img");
    if (!E) return;
    let I = document.querySelector("[data-podium-media]");
    if (!I) return;
    let C = Array.from(I.querySelectorAll("img"));
    if (!C.length) return;
    let K = 0.8,
        J = 0.6,
        U = 0,
        G = 0,
        H = !1,
        Z = !1,
        q = new IntersectionObserver((N) => {
            Z = N[0].isIntersecting
        }, {
            threshold: 0.1
        });
    q.observe(A), m.set(Q, {
        position: "absolute",
        overflow: "hidden",
        clipPath: "ellipse(120% 0% at 50% 0%)",
        zIndex: 10,
        pointerEvents: "none",
        x: 0,
        y: 0
    }), m.set(B, {
        position: "absolute",
        top: 0,
        left: 0,
        width: "100%",
        height: "100%",
        backgroundColor: "var(--color--lime, #d2ff00)",
        zIndex: 5,
        clipPath: "ellipse(120% 120% at 50% 100%)"
    });
    let Y = m.timeline({
        paused: !0
    });
    Y.to(Q, {
        clipPath: "ellipse(120% 120% at 50% 0%)",
        autoAlpha: 1,
        duration: K,
        ease: "power2.out"
    }), Y.to(B, {
        clipPath: "ellipse(120% 0% at 50% 100%)",
        duration: J,
        ease: "power2.out"
    }, `-=${K/2}`);

    function W(N, X) {
        let F = Math.min(Math.max(0, N / X), 1),
            P = Math.min(Math.floor(F * C.length), C.length - 1),
            L = C[P].getAttribute("src");
        if (L && E.src !== L) E.src = L
    }

    function $() {
        if (!Z) return;
        let N = A.getBoundingClientRect(),
            X = U >= N.left && U <= N.right && G >= N.top && G <= N.bottom;
        if (X && !H) H = !0, Y.timeScale(1).play();
        else if (!X && H) H = !1, Y.timeScale(2).reverse();
        if (X) m.to(Q, {
            x: U - N.left + 20,
            y: G - N.top - 20,
            duration: 0.5,
            ease: "power2.out"
        }), W(U - N.left, N.width)
    }
    let D = (N) => {
            U = N.clientX, G = N.clientY, $()
        },
        R = () => {
            $()
        };
    return document.addEventListener("mousemove", D, {
            passive: !0
        }), window.addEventListener("scroll", R, {
            passive: !0
        }),
        function N() {
            q.disconnect(), document.removeEventListener("mousemove", D), window.removeEventListener("scroll", R)
        }
}

function j$(A = !1) {
    let Q = (X, ...F) => {
            if (A) console.log(X, ...F)
        },
        B = (X, ...F) => {
            console.error(X, ...F)
        };
    Q("\uD83D\uDD04 Initializing calendar sync");
    let E = document.querySelector("[data-cal-wrap]");
    if (!E) {
        B("❌ Calendar wrapper not found");
        return
    }
    let I = Array.from(E.querySelectorAll("[data-cal-item]"));
    if (!I.length) {
        B("❌ No calendar items found");
        return
    }
    Q("\uD83D\uDCC5 Found calendar items:", I.length);
    let C = document.querySelector("[data-cal-track-wrap]");
    if (!C) {
        B("❌ Track wrapper not found");
        return
    }
    let K = Array.from(C.querySelectorAll("[data-cal-target]"));
    if (!K.length) {
        B("❌ No target elements found");
        return
    }
    Q("\uD83C\uDFAF Found target elements:", K.length);
    let J = Array.from(C.querySelectorAll('[data-cal-target="transition-item"]'));
    Q("\uD83D\uDD04 Found transition items:", J.length);
    let U = C.querySelector('[data-cal-control="next-item"]'),
        G = C.querySelector('[data-cal-control="previous-item"]'),
        H = new Date;
    H.setHours(0, 0, 0, 0), Q("\uD83D\uDCC6 Today is:", H.toDateString());
    let Z = I.map((X) => {
        let F = X.querySelector('[data-cal-list="date-actual"]'),
            P = F ? F.textContent.trim() : null,
            L = null;
        if (P) try {
            let z = P.split(/[\/ :]/);
            if (z.length >= 3) L = new Date(z[2], z[1] - 1, z[0]), Q(`\uD83D\uDCC6 Parsed date: ${P} -> ${L.toDateString()}`);
            else B(`❌ Invalid date format: ${P}`)
        } catch (z) {
            B(`❌ Error parsing date: ${P}`, z), L = null
        }
        if (Q(`\uD83D\uDCCC Item date: ${P} -> ${L?.toDateString()||"Invalid date"}`), L && L < H) X.classList.add("is-in-past"), Q("⏪ Marked item as past");
        return {
            element: X,
            date: L
        }
    });
    Z.sort((X, F) => {
        if (!X.date) return 1;
        if (!F.date) return -1;
        return X.date - F.date
    }), Q("\uD83D\uDD04 Sorted items by date");
    let q = Z.findIndex((X) => X.date && X.date >= H);
    if (q === -1) q = Z.length - 1;
    Q("\uD83D\uDD0D Current item index:", q), K.forEach((X) => {
        if (X.dataset.animProcessed) return;
        let F = document.createElement("div");
        F.className = "cal-target-wrapper", F.style.position = "relative", F.style.overflow = "hidden";
        let P = document.createElement("div");
        P.className = "cal-target-reveal", P.style.position = "absolute", P.style.top = "0", P.style.left = "0", P.style.width = "100%", P.style.height = "100%", P.style.backgroundColor = "var(--color--lime, #d2ff00)", P.style.transformOrigin = "right center", P.style.zIndex = "15", X.parentNode.insertBefore(F, X), F.appendChild(X), F.appendChild(P), m.set(P, {
            scaleX: 1
        }), m.set(X, {
            position: "relative",
            zIndex: 10,
            autoAlpha: 1
        }), m.set(F, {
            clipPath: "inset(0 100% 0 0)"
        }), X.dataset.animProcessed = "true"
    });
    let Y = C.querySelectorAll(".cal-target-wrapper"),
        W = C.querySelectorAll(".cal-target-reveal"),
        $ = Z[q].element,
        D = $.querySelector('[data-cal-list="circuit-id"]');
    if (D && window.landoGL && window.landoGL.params && window.landoGL.params.tracksScene) {
        let X = D.textContent.trim();
        window.landoGL.params.tracksScene.CURRENT = X, Q("\uD83C\uDF10 Updated WebGL track to:", X)
    }
    if (K.forEach((X) => {
            let F = X.getAttribute("data-cal-target");
            if (!F || F === "transition-item") return;
            if (F === "past-reveal") {
                if ($.classList.contains("is-in-past")) m.set(X, {
                    display: "block",
                    autoAlpha: 1
                }), Q("\uD83D\uDCCA Showing past-reveal element for past event");
                else m.set(X, {
                    display: "none",
                    autoAlpha: 0
                }), Q("\uD83D\uDCCA Hiding past-reveal element for upcoming event");
                return
            }
            if (F === "past-hide") {
                if ($.classList.contains("is-in-past")) m.set(X, {
                    display: "none",
                    autoAlpha: 0
                }), Q("\uD83D\uDCCA Hiding past-hide element for past event");
                else m.set(X, {
                    display: "block",
                    autoAlpha: 1
                }), Q("\uD83D\uDCCA Showing past-hide element for upcoming event");
                return
            }
            if (F === "circuit-flag") {
                let L = $.querySelector('[data-cal-list="circuit-flag"]');
                if (L && L.src) {
                    if (X.tagName === "IMG") X.src = L.src;
                    else {
                        let z = X.querySelector("img");
                        if (z) z.src = L.src
                    }
                    Q(`\uD83C\uDFC1 Updated flag image to: ${L.src}`)
                } else Q("⚠️ Could not update flag - source image not found");
                return
            }
            if (F === "circuit-about") {
                let L = $.querySelector('[data-cal-list="circuit-about"]');
                if (L) X.innerHTML = L.innerHTML, Q(`\uD83D\uDCC4 Updated rich text content for: ${F}`);
                return
            }
            if (F === "results" && $.classList.contains("is-in-past")) {
                X.textContent = "Schedule", Q("\uD83D\uDCCA Changed Schedule to Results for past event");
                return
            }
            if ($.classList.contains("is-in-past")) {
                let L = {
                    "prac1-time": "prac1-time-result",
                    "prac1-date": "prac1-time-pos",
                    "prac2-time": "prac2-time-result",
                    "prac2-date": "prac2-time-pos",
                    "prac3-time": "prac3-time-result",
                    "prac3-date": "prac3-time-pos",
                    "qual-time": "qual-time-result",
                    "qual-date": "qual-time-pos",
                    "race-time": "race-time-result",
                    "race-date": "race-time-pos"
                };
                if (L[F]) {
                    let z = $.querySelector(`[data-cal-list="${L[F]}"]`);
                    if (z) {
                        X.textContent = z.textContent, Q(`✅ Updated ${F} with past event result: ${z.textContent}`);
                        return
                    }
                }
            }
            let P = $.querySelector(`[data-cal-list="${F}"]`);
            if (P) X.textContent = P.textContent, Q(`✅ Updated ${F} with: ${P.textContent}`);
            else Q(`⚠️ Source element not found for target: ${F}`)
        }), $.querySelector('[data-cal-sprint="true"]:not(.w-condition-invisible)')) {
        let X = C.querySelector('[data-cal-target-label="practice2"]'),
            F = C.querySelector('[data-cal-target-label="practice3"]');
        if (X) X.textContent = "Sprint Quali", Q("\uD83C\uDFCE️ Changed Practice 2 label to Sprint Qualifying");
        if (F) F.textContent = "Sprint", Q("\uD83C\uDFCE️ Changed Practice 3 label to Sprint")
    } else {
        let X = C.querySelector('[data-cal-target-label="practice2"]'),
            F = C.querySelector('[data-cal-target-label="practice3"]');
        if (X) X.textContent = "Practice 2", Q("\uD83C\uDFCE️ Reset to Practice 2 label");
        if (F) F.textContent = "Practice 3", Q("\uD83C\uDFCE️ Reset to Practice 3 label")
    }
    I.forEach((X) => X.classList.remove("is-active")), $.classList.add("is-active"), I.forEach((X, F) => {
        X.addEventListener("click", () => {
            Q("\uD83D\uDCC5 Schedule item clicked, scrolling to track section"), q = F, N(q);
            let P = C.getBoundingClientRect(),
                L = 8,
                z = L * parseFloat(getComputedStyle(document.documentElement).fontSize),
                T = window.scrollY + P.top - z;
            if (window.lenis) window.lenis.scrollTo(T, {
                duration: 1.2,
                easing: (V) => V < 0.5 ? 2 * V * V : 1 - Math.pow(-2 * V + 2, 2) / 2
            }), Q(`\uD83D\uDD04 Scrolling to ${T}px (track wrap with ${L}rem offset)`);
            else window.scrollTo({
                top: T,
                behavior: "smooth"
            }), Q(`\uD83D\uDD04 Fallback scroll to ${T}px`)
        }), X.style.cursor = "pointer"
    });
    let R = m.timeline({
        scrollTrigger: {
            trigger: C,
            start: "top 90%",
            once: !0
        }
    });
    R.to(Y, {
        clipPath: "inset(0 0% 0 0)",
        duration: 0.5,
        stagger: 0.015,
        ease: "power2.out"
    }), R.to(W, {
        scaleX: 0,
        duration: 0.5,
        stagger: 0.015,
        ease: "power2.inOut"
    }, "-=0.2");

    function N(X) {
        if (X < 0 || X >= Z.length) {
            B("❌ Invalid item index:", X);
            return
        }
        Q("\uD83D\uDD04 Updating display with item at index:", X);
        let F = Z[X].element,
            P = m.timeline();
        P.call(() => {
            let L = F.querySelector('[data-cal-list="circuit-id"]');
            if (L && window.landoGL && window.landoGL.params && window.landoGL.params.tracksScene) {
                let z = L.textContent.trim();
                window.landoGL.params.tracksScene.CURRENT = z, Q("\uD83C\uDF10 Updated WebGL track to:", z)
            }
        }), P.to(Y, {
            clipPath: "inset(0 100% 0 0)",
            duration: 0.5,
            stagger: 0.015,
            ease: "power2.in"
        }), P.call(() => {
            if (K.forEach((L) => {
                    let z = L.getAttribute("data-cal-target");
                    if (!z || z === "transition-item") return;
                    if (z === "past-reveal") {
                        if (F.classList.contains("is-in-past")) m.set(L, {
                            display: "block",
                            autoAlpha: 1
                        }), Q("\uD83D\uDCCA Showing past-reveal element for past event");
                        else m.set(L, {
                            display: "none",
                            autoAlpha: 0
                        }), Q("\uD83D\uDCCA Hiding past-reveal element for upcoming event");
                        return
                    }
                    if (z === "past-hide") {
                        if (F.classList.contains("is-in-past")) m.set(L, {
                            display: "none",
                            autoAlpha: 0
                        }), Q("\uD83D\uDCCA Hiding past-hide element for past event");
                        else m.set(L, {
                            display: "block",
                            autoAlpha: 1
                        }), Q("\uD83D\uDCCA Showing past-hide element for upcoming event");
                        return
                    }
                    if (z === "circuit-flag") {
                        let V = F.querySelector('[data-cal-list="circuit-flag"]');
                        if (V && V.src) {
                            if (L.tagName === "IMG") L.src = V.src;
                            else {
                                let O = L.querySelector("img");
                                if (O) O.src = V.src
                            }
                            Q(`\uD83C\uDFC1 Updated flag image to: ${V.src}`)
                        } else Q("⚠️ Could not update flag - source image not found");
                        return
                    }
                    if (z === "circuit-about") {
                        let V = F.querySelector('[data-cal-list="circuit-about"]');
                        if (V) L.innerHTML = V.innerHTML, Q(`\uD83D\uDCC4 Updated rich text content for: ${z}`);
                        return
                    }
                    if (z === "results" && F.classList.contains("is-in-past")) {
                        L.textContent = "Schedule", Q("\uD83D\uDCCA Changed Schedule to Results for past event");
                        return
                    }
                    if (F.classList.contains("is-in-past")) {
                        let V = {
                            "prac1-time": "prac1-time-result",
                            "prac1-date": "prac1-time-pos",
                            "prac2-time": "prac2-time-result",
                            "prac2-date": "prac2-time-pos",
                            "prac3-time": "prac3-time-result",
                            "prac3-date": "prac3-time-pos",
                            "qual-time": "qual-time-result",
                            "qual-date": "qual-time-pos",
                            "race-time": "race-time-result",
                            "race-date": "race-time-pos"
                        };
                        if (V[z]) {
                            let O = F.querySelector(`[data-cal-list="${V[z]}"]`);
                            if (O) {
                                L.textContent = O.textContent, Q(`✅ Updated ${z} with past event result: ${O.textContent}`);
                                return
                            }
                        }
                    }
                    let T = F.querySelector(`[data-cal-list="${z}"]`);
                    if (T) L.textContent = T.textContent, Q(`✅ Updated ${z} with: ${T.textContent}`);
                    else Q(`⚠️ Source element not found for target: ${z}`)
                }), F.querySelector('[data-cal-sprint="true"]:not(.w-condition-invisible)')) {
                let L = C.querySelector('[data-cal-target-label="practice2"]'),
                    z = C.querySelector('[data-cal-target-label="practice3"]');
                if (L) L.textContent = "Sprint Quali", Q("\uD83C\uDFCE️ Changed Practice 2 label to Sprint Qualifying");
                if (z) z.textContent = "Sprint", Q("\uD83C\uDFCE️ Changed Practice 3 label to Sprint")
            } else {
                let L = C.querySelector('[data-cal-target-label="practice2"]'),
                    z = C.querySelector('[data-cal-target-label="practice3"]');
                if (L) L.textContent = "Practice 2", Q("\uD83C\uDFCE️ Reset to Practice 2 label");
                if (z) z.textContent = "Practice 3", Q("\uD83C\uDFCE️ Reset to Practice 3 label")
            }
            I.forEach((L) => L.classList.remove("is-active")), F.classList.add("is-active")
        }), P.set(W, {
            scaleX: 1
        }), P.to(Y, {
            clipPath: "inset(0 0% 0 0)",
            duration: 0.5,
            stagger: 0.015,
            ease: "power2.out"
        }), P.to(W, {
            scaleX: 0,
            duration: 0.5,
            stagger: 0.015,
            ease: "power2.inOut"
        }, "-=0.2")
    }
    if (U) U.addEventListener("click", () => {
        if (Q("\uD83D\uDC46 Next button clicked"), q < Z.length - 1) q++;
        else q = 0, Q("\uD83D\uDD04 Looping back to first item");
        N(q)
    }), Q("✅ Next button handler attached");
    else Q("⚠️ Next button not found");
    if (G) G.addEventListener("click", () => {
        if (Q("\uD83D\uDC46 Previous button clicked"), q > 0) q--;
        else q = Z.length - 1, Q("\uD83D\uDD04 Looping back to last item");
        N(q)
    }), Q("✅ Previous button handler attached");
    else Q("⚠️ Previous button not found");
    Q("✅ Calendar sync initialized successfully")
}

function _L(A = !1) {
    let Q = document.querySelector('[data-heroflip="track"]'),
        B = document.querySelector('[data-heroflip="pos1"]'),
        E = document.querySelector('[data-heroflip="pos2"]'),
        I = document.querySelector('[data-heroflip="pos3"]'),
        C = document.querySelector('[data-gl="helmet-scroll"]');
    if (!Q || !B || !E || !I || !C) {
        console.error("Missing required elements for hero scroll animation");
        return
    }
    let K = {
        position: C.style.position,
        top: C.style.top,
        left: C.style.left,
        width: C.style.width,
        height: C.style.height,
        transform: C.style.transform,
        zIndex: C.style.zIndex
    };
    if (getComputedStyle(Q).position === "static") Q.style.position = "relative";
    if (C.parentNode !== Q) C.parentNode.removeChild(C), Q.appendChild(C);
    C.style.position = "absolute", C.style.objectFit = "cover", C.style.zIndex = "10";
    let J = document.createElementNS("http://www.w3.org/2000/svg", "svg");
    J.style.position = "absolute", J.style.top = 0, J.style.left = 0, J.style.width = "100%", J.style.height = "100%", J.style.pointerEvents = "none", J.style.zIndex = 5, J.style.display = A ? "block" : "none", Q.appendChild(J);
    let U = document.createElementNS("http://www.w3.org/2000/svg", "path");
    U.setAttribute("stroke", "red"), U.setAttribute("stroke-width", "2"), U.setAttribute("fill", "none"), J.appendChild(U);
    let G = () => {
        Q.querySelectorAll(".debug-point").forEach((N) => N.remove())
    };

    function H() {
        let R = Q.getBoundingClientRect();
        return [B, E, I].map((N) => {
            let X = N.getBoundingClientRect(),
                F = X.left - R.left + X.width / 2,
                P = X.top - R.top + X.height / 2;
            return {
                x: F,
                y: P,
                width: X.width,
                height: X.height
            }
        })
    }
    let Z;

    function q() {
        if (Z && Z.scrollTrigger) Z.scrollTrigger.kill();
        if (Z) Z.kill();
        G();
        let R = H();
        if (A) console.log("Positions for helmet animation:", R);
        C.style.left = R[0].x + "px", C.style.top = R[0].y + "px", C.style.width = R[0].width + "px", C.style.height = R[0].height + "px", C.style.transform = "translate(-50%, -50%)";
        let N = {
                x: R[0].x,
                y: R[0].y + (R[1].y - R[0].y) * 0.4
            },
            X = {
                x: R[1].x,
                y: R[1].y - (R[1].y - R[0].y) * 0.3
            },
            F = {
                x: R[1].x,
                y: R[1].y + (R[2].y - R[1].y) * 0.3
            },
            P = {
                x: R[1].x + (R[2].x - R[1].x) * 0.6,
                y: R[2].y - (R[2].y - R[1].y) * 0.2
            },
            L = `M${R[0].x},${R[0].y} C${N.x},${N.y} ${X.x},${X.y} ${R[1].x},${R[1].y} C${F.x},${F.y} ${P.x},${P.y} ${R[2].x},${R[2].y}`;
        U.setAttribute("d", L);

        function z(V, O, x, j, h) {
            let p = 1 - h;
            return {
                x: p * p * p * V.x + 3 * p * p * h * O.x + 3 * p * h * h * x.x + h * h * h * j.x,
                y: p * p * p * V.y + 3 * p * p * h * O.y + 3 * p * h * h * x.y + h * h * h * j.y
            }
        }

        function T(V) {
            let O = V.progress;
            if (window.landoGL && window.landoGL.params && window.landoGL.params.helmetScrollScene) window.landoGL.params.helmetScrollScene.PROGRESS = O;
            let x;
            if (O <= 0.5) {
                let j = O * 2;
                x = z(R[0], N, X, R[1], j);
                let h = R[0].width + (R[1].width - R[0].width) * j,
                    p = R[0].height + (R[1].height - R[0].height) * j;
                window.landoGL.bounds.helmetScroll.width = h, window.landoGL.bounds.helmetScroll.height = p
            } else {
                let j = (O - 0.5) * 2;
                x = z(R[1], F, P, R[2], j);
                let h = R[1].width + (R[2].width - R[1].width) * j,
                    p = R[1].height + (R[2].height - R[1].height) * j;
                window.landoGL.bounds.helmetScroll.width = h, window.landoGL.bounds.helmetScroll.height = h
            }
            window.landoGL.bounds.helmetScroll.left = x.x, window.landoGL.bounds.helmetScroll.top = x.y
        }
        if (window.landoGL.params.helmetScrollScene.PROGRESS = 0, TA.create({
                trigger: Q,
                start: () => "top top",
                end: () => "bottom 25%",
                scrub: !0,
                invalidateOnRefresh: !0,
                onRefresh: T,
                onEnter: (V) => {
                    if (A) console.log("ScrollTrigger entered")
                },
                onUpdate: T
            }), TA.create({
                trigger: Q,
                start: () => "bottom center",
                end: () => `bottom+=${window.innerHeight} center`,
                invalidateOnRefresh: !0,
                onRefresh: (V) => {
                    window.landoGL.params.helmetScrollScene.REVEAL_OUT_PROGRESS = V.progress
                },
                onUpdate: (V) => {
                    window.landoGL.params.helmetScrollScene.REVEAL_OUT_PROGRESS = V.progress
                }
            }), A) R.forEach((O, x) => {
            let j = document.createElement("div");
            j.className = "debug-point", j.style.position = "absolute", j.style.left = O.x + "px", j.style.top = O.y + "px", j.style.width = "10px", j.style.height = "10px", j.style.borderRadius = "50%", j.style.transform = "translate(-50%, -50%)", j.style.zIndex = "30", j.style.backgroundColor = ["blue", "purple", "green"][x], Q.appendChild(j);
            let h = document.createElement("div");
            h.className = "debug-point", h.style.position = "absolute", h.style.left = O.x + 15 + "px", h.style.top = O.y + 15 + "px", h.style.color = "white", h.style.background = "rgba(0,0,0,0.5)", h.style.padding = "2px 5px", h.style.borderRadius = "3px", h.style.fontSize = "12px", h.style.zIndex = "30", h.textContent = `Pos${x+1}: ${Math.round(O.x)},${Math.round(O.y)}`, Q.appendChild(h)
        }), [N, X, F, P].forEach((O, x) => {
            let j = document.createElement("div");
            j.className = "debug-point", j.style.position = "absolute", j.style.left = O.x + "px", j.style.top = O.y + "px", j.style.width = "6px", j.style.height = "6px", j.style.borderRadius = "50%", j.style.transform = "translate(-50%, -50%)", j.style.zIndex = "30", j.style.backgroundColor = "orange", Q.appendChild(j);
            let h = document.createElement("div");
            h.className = "debug-point", h.style.position = "absolute", h.style.left = O.x + 10 + "px", h.style.top = O.y + 10 + "px", h.style.color = "white", h.style.background = "rgba(0,0,0,0.5)", h.style.padding = "2px 5px", h.style.borderRadius = "3px", h.style.fontSize = "10px", h.style.zIndex = "30", h.textContent = `CP${x+1}`, Q.appendChild(h)
        })
    }
    q();
    let Y;

    function W() {
        clearTimeout(Y), Y = setTimeout(q, 200)
    }
    window.addEventListener("resize", W);

    function $() {
        let R = J.style.display !== "none";
        return J.style.display = R ? "none" : "block", !R
    }

    function D() {
        if (Z && Z.scrollTrigger) Z.scrollTrigger.kill();
        if (window.removeEventListener("resize", W), J.parentNode) J.parentNode.removeChild(J);
        G(), Object.keys(K).forEach((R) => {
            C.style[R] = K[R]
        })
    }
    return {
        cleanup: D,
        toggleDebug: $
    }
}

function _$(A = !1) {
    if (S$(), Z8(), L_(), k$(), window.innerWidth >= 992) T$(), V_();
    if (ZZ(), setTimeout(() => {
            _L()
        }, 0), j$(A), q8(), [200, 200].forEach((Q) => {
            setTimeout(() => {
                HE("white"), AC("white")
            }, Q)
        }), window.innerWidth >= 992) e0(), AE(), QE();
    GZ(), fJ(), NL(), UZ()
}

function yL() {
    jL(), H8(), HZ(), TA.getAll().forEach((K) => {
        if (K.trigger?.matches('[data-podium="wrap"]') || K.trigger?.matches("[data-stat-list]") || K.trigger?.matches("[data-image-highlight]") || K.trigger?.matches("[data-list-reveal]")) K.kill()
    });
    let A = _L();
    if (A && A.cleanup) A.cleanup();
    document.querySelectorAll("[data-stat-list]").forEach((K) => {
        let J = K.cloneNode(!0);
        K.parentNode.replaceChild(J, K)
    });
    let B = document.querySelector('[data-podium="wrap"]');
    if (B) {
        let K = B.cloneNode(!0);
        B.parentNode.replaceChild(K, B)
    }
    document.querySelectorAll("[data-cal-item]").forEach((K) => {
        let J = K.cloneNode(!0);
        K.parentNode.replaceChild(J, K)
    });
    let I = document.querySelector('[data-cal-control="next-item"]'),
        C = document.querySelector('[data-cal-control="previous-item"]');
    if (I) {
        let K = I.cloneNode(!0);
        I.parentNode.replaceChild(K, I)
    }
    if (C) {
        let K = C.cloneNode(!0);
        C.parentNode.replaceChild(K, C)
    }
    m.killTweensOf("[data-stat-list] [data-mouse-track]"), m.killTweensOf('[data-podium="wrap"] [data-mouse-track]'), m.killTweensOf('[data-podium="text"]'), m.killTweensOf(".text-on-t-stat-label-gigantic .char")
}
var y$ = VA(() => {
    LB();
    VC();
    P$();
    vC();
    qK()
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L45830-L46001 =====
// off-track x$/xL (45830-46000)
function z_(A = !1) {
    let Q = document.querySelector('[data-heroflip="track"]'),
        B = document.querySelector('[data-heroflip="pos1"]'),
        E = document.querySelector('[data-heroflip="pos2"]'),
        I = document.querySelector('[data-heroflip="pos3"]'),
        C = document.querySelector('[data-heroflip="img"]'),
        K = document.querySelector(".is-off-t-hero-scroll-media"),
        J = [],
        U = [],
        G = 0;
    if (K) {
        if (J = K.querySelectorAll(".off-t-hero-scroll-meda-img"), U = Array.from(J).map((F) => F.src), K.style.display = "none", U.length > 0) C.innerHTML = "", U.forEach((F, P) => {
            let L = document.createElement("div");
            L.classList.add("hero-img-layer"), L.setAttribute("data-index", P), L.style.position = "absolute", L.style.top = "0", L.style.left = "0", L.style.width = "100%", L.style.height = "100%", L.style.backgroundImage = `url(${F})`, L.style.backgroundSize = "cover", L.style.backgroundPosition = "center", L.style.zIndex = U.length - P, L.style.opacity = P === 0 ? 1 : 0, C.appendChild(L)
        })
    }
    if (!Q || !B || !E || !I || !C) {
        console.error("Missing required elements for hero scroll animation");
        return
    }
    C.style.backgroundImage = "none";
    let H = {
        position: C.style.position,
        top: C.style.top,
        left: C.style.left,
        width: C.style.width,
        height: C.style.height,
        transform: C.style.transform,
        zIndex: C.style.zIndex,
        backgroundImage: C.style.backgroundImage,
        backgroundSize: C.style.backgroundSize,
        backgroundPosition: C.style.backgroundPosition
    };
    if (getComputedStyle(Q).position === "static") Q.style.position = "relative";
    if (C.parentNode !== Q) C.parentNode.removeChild(C), Q.appendChild(C);
    C.style.position = "absolute", C.style.objectFit = "cover", C.style.zIndex = "10", m.set(C, {
        transformOrigin: "50% 50%",
        xPercent: -50,
        yPercent: -50
    });
    let Z = document.createElementNS("http://www.w3.org/2000/svg", "svg");
    Z.style.position = "absolute", Z.style.top = 0, Z.style.left = 0, Z.style.width = "100%", Z.style.height = "100%", Z.style.pointerEvents = "none", Z.style.zIndex = 5, Z.style.display = A ? "block" : "none", Q.appendChild(Z);
    let q = document.createElementNS("http://www.w3.org/2000/svg", "path");
    q.setAttribute("stroke", "red"), q.setAttribute("stroke-width", "2"), q.setAttribute("fill", "none"), Z.appendChild(q);

    function Y() {
        let F = Q.getBoundingClientRect();
        return [B, E, I].map((P) => {
            let L = P.getBoundingClientRect(),
                z = L.left - F.left + L.width / 2,
                T = L.top - F.top + L.height / 2;
            return {
                x: z,
                y: T,
                width: L.width,
                height: L.height
            }
        })
    }
    let W;

    function $() {
        if (W) W.kill();
        let F = Y();
        m.set(C, {
            x: F[0].x,
            y: F[0].y,
            width: F[0].width,
            height: F[0].height
        });
        let P = {
                x: F[0].x,
                y: F[0].y + (F[1].y - F[0].y) * 0.8
            },
            L = {
                x: F[1].x,
                y: F[1].y - Math.min(800, (F[1].y - F[0].y) * 0.3)
            },
            z = {
                x: F[1].x,
                y: F[1].y + Math.min(80, (F[2].y - F[1].y) * 0.3)
            },
            T = {
                x: F[1].x + (F[2].x - F[1].x) * 0.6,
                y: F[2].y - (F[2].y - F[1].y) * 0.2
            },
            V = `M${F[0].x},${F[0].y} C${P.x},${P.y} ${L.x},${L.y} ${F[1].x},${F[1].y} C${z.x},${z.y} ${T.x},${T.y} ${F[2].x},${F[2].y}`;
        q.setAttribute("d", V), W = m.timeline({
            scrollTrigger: {
                trigger: Q,
                start: window.innerWidth <= 991 ? "20% top" : "top top",
                end: window.innerWidth <= 991 ? "bottom 80%" : "bottom center",
                scrub: !0,
                invalidateOnRefresh: !0
            }
        }), W.to(C, {
            duration: 1.5,
            motionPath: {
                path: V,
                autoRotate: !1
            },
            ease: "none",
            onUpdate: function() {
                let O = this.progress(),
                    x = O > G;
                if (G = O, U.length > 0) {
                    let j = Math.min(Math.floor(O * U.length), U.length - 1);
                    C.querySelectorAll(".hero-img-layer").forEach((p) => {
                        let i = parseInt(p.getAttribute("data-index"));
                        p.style.opacity = i === j ? 1 : 0
                    })
                }
                if (O <= 0.5) {
                    let j = O * 2,
                        h = F[0].width + (F[1].width - F[0].width) * j,
                        p = F[0].height + (F[1].height - F[0].height) * j;
                    C.style.width = `${h}px`, C.style.height = `${p}px`
                } else {
                    let j = (O - 0.5) * 2,
                        h = F[1].width + (F[2].width - F[1].width) * j,
                        p = F[1].height + (F[2].height - F[1].height) * j;
                    C.style.width = `${h}px`, C.style.height = `${p}px`
                }
            }
        })
    }
    $();
    let D;

    function R() {
        clearTimeout(D), D = setTimeout($, 200)
    }
    window.addEventListener("resize", R);

    function N() {
        let F = Z.style.display !== "none";
        return Z.style.display = F ? "none" : "block", !F
    }

    function X() {
        if (W && W.scrollTrigger) W.scrollTrigger.kill();
        if (window.removeEventListener("resize", R), Z.parentNode) Z.parentNode.removeChild(Z);
        if (C.querySelectorAll(".hero-img-layer").forEach((P) => P.remove()), Object.keys(H).forEach((P) => {
                C.style[P] = H[P]
            }), K) K.style.display = ""
    }
    return {
        cleanup: X,
        toggleDebug: N
    }
}

function x$() {
    if (Z8(), bJ(), setTimeout(() => {
            let A = z_();
            if (A && A.cleanup) DZ = A.cleanup
        }, 0), q8(), window.innerWidth >= 992) e0(), AE(), QE()
}

function xL() {
    if (H8(), DZ) DZ(), DZ = null;
    YZ()
}
var DZ = null;
var fL = VA(() => {
    LB();
    VC();
    vC();
    qK();
    z$()
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L46002-L46165 =====
// partnerships f$/bL, partnerships-item WZ/vL, calendar b$, not-found h$/dL (46002-46164) + hL/gL/uL/pL wrappers
function f$() {
    if (window.innerWidth >= 992) setTimeout(() => {
        e0(), AE(), QE()
    }, 0);
    bJ(), q8()
}

function bL() {}
var hL = VA(() => {
    LB();
    VC();
    qK()
});

function k_() {
    let A = document.querySelector('[data-nav-group="brand"]');
    if (A) m.set(A, {
        visibility: "hidden",
        pointerEvents: "none"
    })
}

function w_() {
    let A = document.querySelector('[data-nav-group="brand"]');
    if (A) m.set(A, {
        visibility: "visible",
        pointerEvents: "auto"
    })
}

function P_() {
    let A = document.querySelector("[data-more-list]");
    if (!A) return;
    if (A.querySelectorAll(".camp-item").length === 0) {
        let B = document.querySelector("[data-more-section]");
        if (B) B.remove()
    }
}

function WZ() {
    if (window.innerWidth >= 992) setTimeout(() => {
        e0(), AE(), QE(), Z8(), OL(), fJ()
    }, 10);
    bJ(), k_(), setTimeout(() => {
        P_(), TA.refresh()
    }, 100)
}

function vL() {
    H8(), F$(), w_()
}
var gL = VA(() => {
    LB();
    VC();
    qK()
});

function S_() {
    document.querySelectorAll('[data-calendar-history="wrap"]').forEach((E) => {
        E.querySelectorAll('[data-calendar-history="item"]').forEach((C) => {
            let K = C.querySelector('[data-calendar-history="trigger"]'),
                J = C.querySelector('[data-calendar-history="content"]');
            if (!K || !J) return;
            if (J.style.display = "none", K.setAttribute("aria-expanded", "false"), !J.id) J.id = `calendar-history-content-${Math.random().toString(36).substring(2,10)}`;
            if (K.setAttribute("aria-controls", J.id), J.setAttribute("aria-hidden", "true"), K.setAttribute("role", "button"), J.setAttribute("role", "region"), !K.hasAttribute("aria-label")) {
                let U = K.textContent.trim();
                K.setAttribute("aria-label", U || "Toggle calendar history item")
            }
            if (K.tagName !== "BUTTON" && K.tagName !== "A") K.setAttribute("tabindex", "0");
            K.addEventListener("click", () => B(C, K, J)), K.addEventListener("keydown", (U) => {
                if (U.key === "Enter" || U.key === " ") U.preventDefault(), B(C, K, J)
            })
        })
    });

    function Q(E, I) {
        I.querySelectorAll('[data-calendar-history="item"]').forEach((K) => {
            if (K !== E) {
                let J = K.querySelector('[data-calendar-history="trigger"]'),
                    U = K.querySelector('[data-calendar-history="content"]');
                if (K.classList.contains("is-open")) K.classList.remove("is-open"), U.style.display = "none", J.setAttribute("aria-expanded", "false"), U.setAttribute("aria-hidden", "true")
            }
        })
    }

    function B(E, I, C) {
        let K = E.classList.contains("is-open"),
            J = E.closest('[data-calendar-history="wrap"]');
        if (Q(E, J), K) E.classList.remove("is-open"), C.style.display = "none", I.setAttribute("aria-expanded", "false"), C.setAttribute("aria-hidden", "true");
        else E.classList.add("is-open"), C.style.display = "block", I.setAttribute("aria-expanded", "true"), C.setAttribute("aria-hidden", "false"), setTimeout(() => {
            let U = E.getBoundingClientRect();
            if (U.top < 0 || U.top > window.innerHeight * 0.3)
                if (window.lenis) window.lenis.scrollTo(E, {
                    offset: -100,
                    duration: 0.5
                });
                else E.scrollIntoView({
                    behavior: "smooth",
                    block: "start"
                })
        }, 10)
    }
}

function b$() {
    if (window.innerWidth >= 992) e0(), AE(), QE();
    fJ(), UZ(), GZ(), S$(), S_(), [200, 200].forEach((A) => {
        setTimeout(() => {
            HE("white"), AC("white")
        }, A)
    }), T$(), j$(debug = !1)
}
var uL = VA(() => {
    LB();
    VC();
    y$();
    vC()
});

function T_() {
    let A = document.querySelectorAll("[data-gl-switcher]");
    console.log("switchers", A), console.log("gl", window.landoGL.params.notFoundScene), A.forEach((Q) => {
        Q.addEventListener("click", () => {
            let B = Q.getAttribute("data-gl-switcher"),
                E = B.charAt(0).toUpperCase() + B.slice(1);
            window.landoGL.params.notFoundScene.VARIANT = E, Q.classList.add("is-active"), A.forEach((I) => {
                if (I !== Q) I.classList.remove("is-active")
            })
        })
    })
}

function j_() {
    let A = document.querySelector('[data-nav-group="brand"]');
    if (A) m.set(A, {
        visibility: "hidden",
        pointerEvents: "none"
    })
}

function __() {
    let A = document.querySelector('[data-nav-group="brand"]');
    if (A) m.set(A, {
        visibility: "visible",
        pointerEvents: "auto"
    })
}

function h$() {
    j_(), T_(), [200, 200].forEach((A) => {
        setTimeout(() => {
            HE("white"), AC("white")
        }, A)
    })
}

function dL() {
    __()
}
var pL = VA(() => {
    LB();
    vC()
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L46166-L46376 =====
// sL lifecycle (46166-46375): mL initial load, cL enter, lL leave; oL/iL/y_ hoisted
function mL() {
    let Q = document.querySelector("[data-page]");
    if (!Q) throw new Error("No data-page element found");
    let B = Q.dataset.page;

    function E() {
        return document.readyState === "complete"
    }
    let I = () => {
        if (E()) {
            let C = function() {
                console.debug(`Initializing page: ${B}`), (async (J) => {
                    try {
                        switch (J) {
                            case oB.HOME:
                                w$(), setTimeout(() => {
                                    k0(), o0()
                                }, 750);
                                break;
                            case oB.ON_TRACK:
                                _$(), setTimeout(() => {
                                    k0(), o0()
                                }, 750);
                                break;
                            case oB.OFF_TRACK:
                                x$(), setTimeout(() => {
                                    k0(), o0()
                                }, 750);
                                break;
                            case oB.PARTNERSHIPS:
                                f$(), console.log("Partnerships page IN"), setTimeout(() => {
                                    k0(), o0()
                                }, 750);
                                break;
                            case oB.PARTNERSHIPS_ITEM:
                                WZ(), setTimeout(() => {
                                    k0(), o0()
                                }, 750);
                                break;
                            case oB.CALENDAR:
                                b$(), setTimeout(() => {
                                    k0(), o0()
                                }, 750);
                                break;
                            case oB.NOT_FOUND:
                                h$();
                                break;
                            case oB.STYLE:
                                break;
                            default:
                                console.warn(`No specific initialization for page: ${J}, initializing defaults`), e0(), AE(), QE()
                        }
                    } catch (U) {
                        console.error(`Error loading module for page ${J}:`, U), e0(), AE(), QE(), S1()
                    }
                })(B)
            };
            if (console.debug("Starting basic initialization..."), BD(), KD(), window.innerWidth <= 991) rR();
            if (!YK) YK = qZ();
            if (RZ) RZ.split();
            else RZ = new N$;
            L$(), nR(), S1(), iR(), setTimeout(() => {
                kL(), V$()
            }, 500), AL(), C(), O$(), setTimeout(() => {
                H$()
            }, 1000)
        } else requestAnimationFrame(I)
    };
    I()
}

function cL(A) {
    if (setTimeout(() => {
            H$()
        }, 500), !A) {
        console.error("No page name provided for transition in");
        return
    }
    try {
        window.scrollTo(0, 0), (() => {
            if (console.debug("Performing common initialization..."), ID(), !YK) YK = qZ();
            else YK.reinit();
            RZ = new N$, V$(), L$(), S1(), O$(), ED()
        })(), (async () => {
            try {
                switch (A) {
                    case oB.HOME:
                        w$(), setTimeout(() => {
                            k0(), o0()
                        }, 50), yU();
                        break;
                    case oB.ON_TRACK:
                        _$(), setTimeout(() => {
                            k0(), o0()
                        }, 50), yU();
                        break;
                    case oB.OFF_TRACK:
                        x$(), setTimeout(() => {
                            k0(), o0()
                        }, 50);
                        break;
                    case oB.PARTNERSHIPS:
                        f$(), setTimeout(() => {
                            k0(), o0()
                        }, 50);
                        break;
                    case oB.PARTNERSHIPS_ITEM:
                        console.log(WZ), console.log(oB.PARTNERSHIPS_ITEM), WZ(), setTimeout(() => {
                            k0(), o0()
                        }, 50);
                        break;
                    case oB.CALENDAR:
                        b$(), setTimeout(() => {
                            k0(), o0()
                        }, 50), yU();
                        break;
                    case oB.NOT_FOUND:
                        h$(), yU();
                        break;
                    default:
                        console.warn(`No specific initialization for page: ${A}, initializing defaults`), e0(), AE(), QE(), S1()
                }
            } catch (I) {
                console.error(`Error in transition to ${A}:`, I)
            }
        })()
    } catch (B) {
        console.error("Transition in error:", B)
    }
}

function lL(A) {
    if (!A) {
        console.error("No page name provided for transition out");
        return
    }
    try {
        if (console.debug(`Transitioning out of ${A}`), window.closeNavigation(), YK) YK.cleanup();
        LL(), EL(), setTimeout(() => {
            QL();
            try {
                switch (A) {
                    case oB.HOME:
                        TL();
                        break;
                    case oB.ON_TRACK:
                        yL();
                        break;
                    case oB.OFF_TRACK:
                        xL();
                        break;
                    case oB.PARTNERSHIPS:
                        bL();
                        break;
                    case oB.PARTNERSHIPS_ITEM:
                        vL();
                        break;
                    case oB.NOT_FOUND:
                        dL();
                        break;
                    default:
                        console.warn(`No specific cleanup for page: ${A}, initializing defaults`)
                }
            } catch (Q) {
                console.error(`Error in transition out from ${A}:`, Q)
            }
        }, v$)
    } catch (Q) {
        console.error("Transition out error:", Q)
    }
}
var v$ = 1000,
    YK, RZ = null,
    oB, g$;
var sL = VA(() => {
    LB();
    VC();
    VC();
    qK();
    zL();
    wL();
    EZ();
    P$();
    y$();
    fL();
    hL();
    gL();
    uL();
    pL();
    vC();
    q$();
    oB = {
        HOME: "home",
        ON_TRACK: "on-track",
        OFF_TRACK: "off-track",
        PARTNERSHIPS: "partnerships",
        PARTNERSHIPS_ITEM: "partnerships-item",
        CALENDAR: "calendar",
        BLOG: "blog",
        STYLE: "style",
        NOT_FOUND: "not-found"
    }, g$ = {
        PAGE_TRANSITION_START: "page-transition-start",
        PAGE_TRANSITION_END: "page-transition-end"
    }
});
var nL = {};
QD(nL, {
    taxi: () => y_
});
var oL, iL, y_;

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L46377-L46468 =====
// taxi assembly aL (46377-46467): oL Transition, iL Renderer, y_ Core
var aL = VA(() => {
    FL();
    LB();
    sL();
    oL = class oL extends LC {
        onLeave({
            from: A,
            trigger: Q,
            done: B
        }) {
            try {
                if (window.dispatchEvent(new CustomEvent(g$.PAGE_TRANSITION_START)), !A?.dataset?.page) {
                    console.warn("Missing page data attribute on exit"), B();
                    return
                }
                document.body.classList.add("is-transitioning"), lL(A.dataset.page), setTimeout(() => {
                    B()
                }, v$)
            } catch (E) {
                console.error("Transition out error:", E), document.body.classList.remove("is-transitioning"), B()
            }
        }
        onEnter({
            to: A,
            trigger: Q,
            done: B
        }) {
            try {
                if (!A?.dataset?.page) {
                    console.warn("Missing page data attribute on enter"), B();
                    return
                }
                let E = performance.now();
                cL(A.dataset.page);
                let I = performance.now();
                console.debug(`Page transition took ${I-E}ms`), document.body.classList.remove("is-transitioning"), window.dispatchEvent(new CustomEvent(g$.PAGE_TRANSITION_END)), B()
            } catch (E) {
                console.error("Transition in error:", E), document.body.classList.remove("is-transitioning"), B()
            }
        }
    };
    iL = class iL extends G8 {
        initialLoad() {
            try {
                console.debug("Starting initial load..."), mL(), this.onEnter(), this.onEnterCompleted()
            } catch (A) {
                console.error("Initial load error:", A)
            }
        }
        onLeave() {
            window.trackingStyles = void 0;
            try {
                console.debug("Page leave started")
            } catch (A) {
                console.error("Leave error:", A)
            }
        }
        onLeaveCompleted() {
            try {
                console.debug("Page leave completed")
            } catch (A) {
                console.error("Leave completion error:", A)
            }
        }
        onEnter() {
            try {
                console.debug("Page enter started")
            } catch (A) {
                console.error("Enter error:", A)
            }
        }
        onEnterCompleted() {
            try {
                console.debug("Page enter completed")
            } catch (A) {
                console.error("Enter completion error:", A)
            }
        }
    };
    y_ = new hU({
        renderers: {
            default: iL
        },
        transitions: {
            default: oL
        },
        removeOldContent: !0,
        allowInterruption: !1,
        bypassCache: !1
    })
});


// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L47011-L47099 =====
// Lenis manager QV (47011-47099)
var AV = VA(() => {
    Y8 = {
        passive: !1
    }
});
var BV = {};
QD(BV, {
    default: () => p_
});
class QV {
    constructor() {
        if (!window.landoGL) window.landoGL = {};
        this.isDesktop = window.innerWidth > 991, this.lenis = this.createLenisInstance(), window.lenis = this.lenis, this.init(), window.addEventListener("resize", this.handleResize.bind(this))
    }
    createLenisInstance() {
        let A = {
            infinite: !1,
            lerp: 0.1,
            smoothWheel: !0,
            touchMultiplier: 1.25,
            autoResize: !0,
            syncTouch: !0
        };
        if (this.isDesktop) return new u$({
            ...A,
            wrapper: document.documentElement,
            content: document.body
        });
        else return new u$({
            ...A,
            wrapper: document.documentElement,
            content: document.body
        })
    }
    handleResize() {
        let A = this.isDesktop;
        if (this.isDesktop = window.innerWidth > 991, A !== this.isDesktop) {
            let Q = this.lenis.scroll;
            if (this.lenis.destroy(), this.lenis = this.createLenisInstance(), window.lenis = this.lenis, window.landoGL.lenis = this.lenis, this.init(), Q) this.lenis.scrollTo(Q, {
                immediate: !0
            });
            TA.refresh()
        }
    }
    init() {
        this.lenis.on("scroll", (A) => {
            TA.update()
        }), m.ticker.add((A) => {
            this.lenis.raf(A * 1000)
        }, !1, !0), m.ticker.lagSmoothing(0), window.lenis.resize = this.resize.bind(this)
    }
    scrollTo(A, Q = {}) {
        let B = {
            duration: 0,
            ...Q
        };
        this.lenis.scrollTo(A, B)
    }
    stop() {
        this.lenis.stop()
    }
    start() {
        this.lenis.start()
    }
    resize() {
        if (this.lenis) this.lenis.resize(), TA.refresh()
    }
    destroy() {
        if (window.removeEventListener("resize", this.handleResize.bind(this)), this.lenis) this.lenis.destroy(), m.ticker.remove(this.lenis.raf), this.lenis = null
    }
    getCurrentScroll() {
        return this.lenis.scroll
    }
    getScrollProgress() {
        return this.lenis.progress
    }
    getVelocity() {
        return this.lenis.velocity
    }
}
var d$, p_;
var EV = VA(() => {
    AV();
    LB();
    EZ();
    d$ = new QV, p_ = d$;
    window.lenisStart = () => d$.start();
    window.lenisStop = () => d$.stop()
});

// ===== mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.js L47100-L47120 =====
// entry (47100-47120): vC() EZ() q$() -> c_() boot sequence
vC();
EZ();
q$();
console.time("debug");
history.scrollRestoration = "manual";
window.scrollTo(0, 0);
async function m_() {
    return new Promise((A, Q) => {
        window.addEventListener("allriveloaded", () => {
            A()
        }), kI()
    })
}
async function c_() {
    let A = await BL();
    await Promise.all([eM(), m_()]);
    let [Q, B] = await Promise.all([Promise.resolve().then(() => (aL(), nL)), Promise.resolve().then(() => (EV(), BV))]);
    CD()
}
c_();
