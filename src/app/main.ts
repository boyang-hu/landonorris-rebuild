/**
 * Rebuild entry — replaces lando.OFF+BRAND.gold-android-fix-03.js.
 *
 * Source oracle: legacy-mirror/_pretty/lando.OFF+BRAND.gold-android-fix-03.pretty.js
 * Boot pipeline (pretty 46990-47120):
 *   c_(): await BL()                    — page-transition Rive ready
 *         await Promise.all([eM(), m_()]) — GL load + all Rive files preloaded
 *         import scroll (Lenis) + app modules
 *         CD()                          — component boot
 */
console.log('[rebuild] entry loaded — implementation lands with M3+');
